# Form 1040 line 25a — Federal income tax withheld from Form(s) W-2 — e2e test catalog (audit)

Source specs (per `XLS/Computations/25a.md` and the line-25a dependency map):
- `C:\us-tax\us-tax-be\e2e\tests\line25abcd-withholding.spec.ts` (9 tests)
- `C:\us-tax\us-tax-be\e2e\tests\line1a-form4852.spec.ts` (5 tests)
- `C:\us-tax\us-tax-be\e2e\tests\employment-form4852-inmate-link.spec.ts` (21 tests)
- `C:\us-tax\us-tax-be\e2e\tests\w2-document-extraction.spec.ts` (8 tests, all `test.skip` — Azure Document Intelligence required)
- `C:\us-tax\us-tax-be\e2e\tests\w2-required-employment.spec.ts` (2 tests)

Audit date: 2026-06-10. Result of the full run: **45 tests; 37 passed, 0 failed, 8 skipped** (the 8 skipped tests in `w2-document-extraction.spec.ts` are gated by `test.skip(() => true, 'Disabled — requires Azure Document Intelligence service running')` at the top of the file).

Method: for every `test(...)` in the five specs — (1) read the inputs the test seeds, (2) compute by hand what the Internal Revenue Service rule requires from those inputs per the 2025 Form 1040 instructions, 2025 Form W-2 + W-2 box instructions, the 2025 Form 4852 (Substitute for Form W-2) instructions, IRS Publications 17 / 525 / 575, the 2025 Form 8959 instructions, and the relevant Internal Revenue Code sections, (3) read the Java compute path in `TaxReturnComputeService.java` and the Form 4852 helpers (`extractForm4852Line25aWithholding`, `extractForm4852WagesForPerson`, `validateEmploymentIncomeW2ForPerson`), (4) compare against the `expect(...).toBe(...)` (or `.toBeTruthy()`, `.toBeFalsy()`, `.toBe(true/false)`, `.toBeVisible()`, `.toHaveCount(N)`, etc.) assertions, (5) classify each assertion **✅ OK** / **⚠️ MISMATCH** / **🕳️ GAP**.

Backend entry points relevant to line 25a:
- `sumFederalWithholdingFromEntries(...)` — generic statement-entry summation helper (used for W-2 line 25a, 1099-series line 25b, W-2G line 25c)
- `extractForm4852Line25aWithholding(...)` — Form 4852 line 7e summation, gated by `substituteFormType === "w2"`
- `extractForm4852WagesForPerson(...)` — Form 4852 line 7a summation that feeds Form 1040 line 1a wages
- `validateEmploymentIncomeW2ForPerson(...)` — emits `MISSING_W2_EMPLOYMENT_INCOME_*` and `MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_*` blocking flags
- `validateDuplicateW2EmployerEin(...)` (G4 closure 2026-06-10) — non-blocking advisory when 2+ W-2 entries share an EIN
- `validateForm4852SubstituteTypeExplicit(...)` (G5 closure 2026-06-10) — blocking flag when a Form 4852 row has both line-7 and line-8 inputs without an explicit `substituteFormType`
- `applyInmateWagesReclassification(...)` — clamps line 1a and routes to Schedule 1 line 8u (the `otherIncomeWagesWhileIncarcerated` field)
- `computeLine31ThroughLine38(...)` — composer that writes `Payments.withholdingW2`, `Payments.withholding1099`, `Payments.withholdingOther`, and the line 25d total

See `C:\us-tax\XLS\Computations\25a.md` for the full IRS-canonical line 25a derivation and the §8 + §9 closure log (G1 policy-aligned; G2/G4/G5/G6 fixed; G3 withdrawn).

Shared 2025 reference data (centralized in `ReferenceData.java`):
- **None directly for line 25a** — line 25a has no rate, no threshold, no inflation-indexed constant. The withholding amounts arrive already-computed in W-2 box 2 and Form 4852 line 7e.
- For Form 8959 cross-line tests: Additional Medicare Tax rate `0.9%` (IRC §3101(b)(2)); regular Medicare rate `1.45%` (IRC §3101(b)(1)); Single threshold `$200,000`.

Recent changes affecting these tests:
- **G2 / G4 / G5 / G6 closures (2026-06-10)** — `XLS/Computations/25a.md` §8 and §9 closure log.
  - G2: `withholdingW2 = withholdingW2 == null ? null : roundMoney(withholdingW2);` inserted at `TaxReturnComputeService.java:26621`.
  - G4: new `validateDuplicateW2EmployerEin` helper at line 4587 (advisory `DUPLICATE_W2_EMPLOYER_EIN`).
  - G5: new `validateForm4852SubstituteTypeExplicit` helper at line 4638 (blocking `FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_*`).
  - G6: `extractForm4852Line25aWithholding` positive-filter added at line 24601–24609 (was `addNonNull`).
- **1099-DA mapper output alias (2026-06-10)** — `Form1099DaMapper.loadOne(...)` now exposes `federalIncomeTaxWithheldAmount` alongside the prefixed `copyOther_*` / `copyA_*` names so `sumFederalWithholdingFromEntries` finds it. Backfills G1 (1099-DA exclusion) closure for line 25b.
- **INMATE_WAGES_EXCEEDS_WAGE_SOURCES §17 blocker (date unknown, in scope at audit time)** — non-overrideable; fires when `inmateWagesAmount > sum(W-2 + Form 4852 line 7a)`.
- **MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER / _SPOUSE blocking flags** — fire when `isMissingW2=true` but no `substituteFormType === "w2"` Form 4852 entry exists.

How to run **all 45 line-25a-related e2e tests** from a normal PowerShell terminal (never from inside the Claude Code sandbox — Playwright worker spawn fails with `EPERM`):

```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a `
  line25abcd-withholding.spec.ts `
  line1a-form4852.spec.ts `
  employment-form4852-inmate-link.spec.ts `
  w2-document-extraction.spec.ts `
  w2-required-employment.spec.ts
```

Convention used in every test in this catalog: `seedRequiredDob(page)` writes the default taxpayer identification (`firstName="E2E"`, `lastName="Tester"`, `ssn="111-11-1111"`, `dateOfBirth="1990-01-01"`, `occupation="Analyst"`) and the default address (1 Main St, Albany, NY 12207). Filing status defaults to Single unless the test calls `setFilingStatusApi(...)`.

---

## Spec file: line25abcd-withholding.spec.ts (9 tests)

## Test 1 — `W-2 box 2 federal withholding flows to line 25a`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "W-2 box 2 federal withholding flows to line 25a" line25abcd-withholding.spec.ts
```

### Purpose
The canonical happy-path test for Form 1040 line 25a: a single W-2 with `federalIncomeTaxWithheldAmount = $10,000` (box 2) flows verbatim to `Payments.withholdingW2`. Verifies that box 2 is the field the backend reads and that nothing else (line 25b, line 25c) is accidentally populated.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single filer |
| `seedW2WithWithholding` (`w-2`) | `wagesTipsOtherCompAmount` | `80000` |
| same | `socialSecurityWagesAmount` | `80000` |
| same | `medicareWagesAndTipsAmount` | `80000` |
| same | `federalIncomeTaxWithheldAmount` | `10000` |
| same | `employeeSSN` | `111-11-1111` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments` | truthy |
| `form1040.payments.withholdingW2` | `10000` |
| `form1040.payments.withholding1099` | null or 0 |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `10000` |

### Hand computation per Internal Revenue Service rule
- Per the 2025 Instructions for Form 1040, line 25a: "Federal income tax withheld from Form(s) W-2. Add the amounts shown as federal income tax withheld on your Form(s) W-2."
- One Form W-2 on file; box 2 = $10,000.
- Line 25a = $10,000.
- Line 25b (1099-series withholding) = $0 (no 1099 entries on the return).
- Line 25c (other withholding) = $0 (no W-2G or Form 8959 Part V excess on the return).
- Line 25d = $10,000 + $0 + $0 = $10,000.

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries)` at `TaxReturnComputeService.java:27430` reads `federalIncomeTaxWithheldAmount = $10,000` from the lone W-2 entry, returns $10,000.
- Composer at line 26617–26621 (post-G2 fix): `withholdingW2 = roundMoney($10,000) = $10,000`; `payments.setWithholdingW2($10,000)`.
- No 1099-series helper finds withholding → `withholding1099` stays null.
- No W-2G or Form 8959 Part V contribution → `withholdingOther` stays null.
- Line 25d composer at line 26654: `totalWithholding = round($10,000 + 0 + 0) = $10,000`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `payments` truthy | ✅ OK |
| 2 | `withholdingW2 === 10000` | ✅ OK — exact W-2 box 2 → line 25a wire. |
| 3 | `withholding1099 == null \|\| === 0` | ✅ OK — no 1099 entries. |
| 4 | `withholdingOther == null \|\| === 0` | ✅ OK — no W-2G / Form 8959 Part V. |
| 5 | `totalWithholding === 10000` | ✅ OK — line 25d sum. |

---

## Test 2 — `1099-R box 4 withholding flows to line 25b`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "1099-R box 4 withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
Cross-line negative-coverage test for line 25a: a single 1099-R with `federalIncomeTaxWithheldAmount = $2,500` (box 4) must flow to **line 25b**, not line 25a. Locks the canonical mapper field names that survived the 2026-06-09 grep-mapper diagnostic.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `seedPensionUploadAttestation` (`pension-annuity-income-taxpayer`) | `hadPensionOrAnnuityIncome` | `true` |
| same | `hasUploadedAtLeastOnePensionStatement` | `true` |
| same | `confirmAllReceivedPensionStatementsUploaded` | `true` |
| same | `received1099RNonIra` | `true` |
| same | `uploaded1099RNonIra` | `true` |
| `seed1099RWithWithholding` (`1099-r`) | `recipientTIN` | `111-11-1111` |
| same | `grossDistributionAmount` | `25000` |
| same | `taxableAmountAmount` | `25000` |
| same | `federalIncomeTaxWithheldAmount` | `2500` |
| same | `distributionCodes` | `'7'` (normal distribution) |
| same | `iraSepSimple` | `false` (pension, not IRA) |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `2500` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `2500` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 1040 line 25b: "Federal income tax withheld from Form(s) 1099." 1099-R box 4 is "Federal income tax withheld."
- Box 4 = $2,500 → line 25b = $2,500.
- Line 25a (W-2 box 2) = $0 (no W-2 entries).
- Line 25c = $0.
- Line 25d = $0 + $2,500 + $0 = $2,500.

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries)` returns null (empty list) → `withholdingW2 = null`.
- `sumFederalWithholdingFromMultipleLists(...)` reads `federalIncomeTaxWithheldAmount = $2,500` from the 1099-R entry → `withholding1099 = $2,500`.
- `withholdingOther` stays null.
- `totalWithholding = round(0 + $2,500 + 0) = $2,500`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK — 1099-R box 4 does NOT bleed into line 25a. |
| 2 | `withholding1099 === 2500` | ✅ OK — line 25b correctly populated. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 2500` | ✅ OK. |

---

## Test 3 — `SSA-1099 voluntary box 6 withholding flows to line 25b`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "SSA-1099 voluntary box 6 withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
Negative-coverage cross-line test for line 25a: SSA-1099 box 6 voluntary federal withholding ($1,200) must NOT populate line 25a; it belongs on line 25b under the SSA-1099 path. Locks the SSA-1099 field-name fix: the frontend uses `voluntaryFederalIncomeTaxWithheldAmount`, not the generic `federalIncomeTaxWithheldAmount`.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `seedSsa1099WithWithholding` (`ssa-1099`) | `netBenefitsAmount` | `18000` |
| same | `voluntaryFederalIncomeTaxWithheldAmount` | `1200` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `1200` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `1200` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 1040 line 25b instruction explicitly covers SSA-1099 voluntary withholding: "Include this amount on line 25b."
- SSA-1099 box 6 = $1,200 → line 25b = $1,200.
- Line 25a = $0 (no W-2 entries).

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries)` returns null → `withholdingW2 = null`.
- `sumSsa1099Withholding(...)` reads `voluntaryFederalIncomeTaxWithheldAmount = $1,200` → contributes to `withholding1099`.
- `withholding1099 = $1,200`; `withholdingOther = null`; line 25d = $1,200.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK — SSA-1099 does NOT bleed into line 25a. |
| 2 | `withholding1099 === 1200` | ✅ OK — line 25b via `voluntaryFederalIncomeTaxWithheldAmount`. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 1200` | ✅ OK. |

---

## Test 4 — `W-2G box 4 withholding flows to line 25c`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "W-2G box 4 withholding flows to line 25c" line25abcd-withholding.spec.ts
```

### Purpose
Negative-coverage cross-line test for line 25a: W-2G gambling-winnings withholding ($2,880) flows to **line 25c** ("Other forms," per the 2025 Form 1040 instructions). Distinguishes W-2G from real W-2 despite the similar form name and field naming.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `seedW2GWithWithholding` (`w-2g`) | `grossWinningsAmount` | `12000` |
| same | `federalIncomeTaxWithheldAmount` | `2880` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | null or 0 |
| `form1040.payments.withholdingOther` | `2880` |
| `form1040.payments.totalWithholding` | `2880` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 1040 line 25c "Other forms (see instructions)" covers W-2G box 4 (federal income tax withheld on gambling winnings) per the 2025 Form W-2G instructions.
- Box 4 = $2,880 → line 25c = $2,880.
- Line 25a = $0; line 25b = $0; line 25d = $2,880.

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries)` returns null → `withholdingW2 = null`.
- `sumFederalWithholdingFromEntries(w2gEntries)` returns $2,880 → contributes to `withholdingOther`.
- Line 25d = $0 + $0 + $2,880 = $2,880.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK — W-2G does NOT bleed into line 25a. |
| 2 | `withholding1099 == null \|\| === 0` | ✅ OK. |
| 3 | `withholdingOther === 2880` | ✅ OK — W-2G box 4 → line 25c. |
| 4 | `totalWithholding === 2880` | ✅ OK. |

---

## Test 5 — `Line 25d totals W-2 withholding (25a) + 1099 withholding (25b) + other withholding (25c)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Line 25d totals W-2 withholding" line25abcd-withholding.spec.ts
```

### Purpose
Integration test: with one W-2 ($8,000), one 1099-R ($1,500), and one W-2G ($600), verifies each sub-line is independently populated and that line 25d sums all three correctly to $10,100. Locks the line-25d composer.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `seedPensionUploadAttestation` | pension attestation suite | true (×5 fields) |
| `seedW2WithWithholding` (`w-2`) | `wagesTipsOtherCompAmount` | `60000` |
| same | `federalIncomeTaxWithheldAmount` | `8000` |
| same | `employeeSSN` | `111-11-1111` |
| `seed1099RWithWithholding` (`1099-r`) | `grossDistributionAmount` | `15000` |
| same | `taxableAmountAmount` | `15000` |
| same | `federalIncomeTaxWithheldAmount` | `1500` |
| same | `distributionCodes` | `'7'` |
| same | `iraSepSimple` | `false` |
| `seedW2GWithWithholding` (`w-2g`) | `grossWinningsAmount` | `8000` |
| same | `federalIncomeTaxWithheldAmount` | `600` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | `8000` |
| `form1040.payments.withholding1099` | `1500` |
| `form1040.payments.withholdingOther` | `600` |
| `form1040.payments.totalWithholding` | `10100` |

### Hand computation per Internal Revenue Service rule
- Line 25a = $8,000 (W-2 box 2).
- Line 25b = $1,500 (1099-R box 4).
- Line 25c = $600 (W-2G box 4).
- Line 25d = $8,000 + $1,500 + $600 = $10,100.

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries) = $8,000` → `withholdingW2 = $8,000`.
- 1099-series helper reads $1,500 from the 1099-R → `withholding1099 = $1,500`.
- `sumFederalWithholdingFromEntries(w2gEntries) = $600` → `withholdingOther = $600`.
- Line 25d composer: `round($8,000 + $1,500 + $600) = $10,100`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 === 8000` | ✅ OK. |
| 2 | `withholding1099 === 1500` | ✅ OK. |
| 3 | `withholdingOther === 600` | ✅ OK. |
| 4 | `totalWithholding === 10100` | ✅ OK — line 25d exact sum. |

---

## Test 6 — `1099-DA box 2a federal withholding flows to line 25b (G1 fix)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "1099-DA box 2a federal withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
Cross-line negative-coverage test for line 25a: 1099-DA (Digital Asset, new 2025 form) box 2a withholding ($1,500) flows to **line 25b**, not line 25a. Locks the G1 closure where `form1099DaEntries` was previously excluded from `sumFederalWithholdingFromMultipleLists` in `computeLine31ThroughLine38`. Required two paired fixes: (a) the mapper-output alias `federalIncomeTaxWithheldAmount` on `Form1099DaMapper.loadOne(...)` so the generic helper finds it, and (b) the `copyA_box4_federal_income_tax_withheld` prefixed seed key matching `pickString`'s `copyOther_*` / `copyA_*` convention.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `seed1099DaWithWithholding` (`1099-da`) | `copyA_box4_federal_income_tax_withheld` | `'1500'` (string-typed) |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `1500` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `1500` |

### Hand computation per Internal Revenue Service rule
- The 2025 Form 1099-DA (Proceeds from Broker Transactions — Digital Assets) box 2a is "Federal income tax withheld."
- 2025 Form 1040 line 25b covers withholding from 1099-series forms including 1099-DA.
- Box 2a = $1,500 → line 25b = $1,500.

### Java compute path
- `Form1099DaMapper.loadOne(...)` reads the prefixed key `copyA_box4_federal_income_tax_withheld = "1500"` via `pickString`, parses to BigDecimal $1,500, persists to `box4FederalIncomeTaxWithheld`, AND (post-2026-06-10 alias) exposes a duplicate read key `federalIncomeTaxWithheldAmount = $1,500`.
- `sumFederalWithholdingFromMultipleLists(form1099DaEntries, ...)` reads `federalIncomeTaxWithheldAmount = $1,500` via the same generic helper used for 1099-R, 1099-INT, etc. → contributes to `withholding1099`.
- `withholding1099 = $1,500`; line 25d = $1,500.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK — 1099-DA does NOT bleed into line 25a. |
| 2 | `withholding1099 === 1500` | ✅ OK — G1 closure verified. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 1500` | ✅ OK. |

---

## Test 7 — `Form 8959 Part V line 24 Medicare withholding flows to line 25c (G2)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Form 8959 Part V line 24 Medicare withholding flows to line 25c" line25abcd-withholding.spec.ts
```

### Purpose
Cross-line negative-coverage test for line 25a: a Single filer with $250,000 Medicare wages and $4,075 W-2 box 6 Medicare withholding triggers Form 8959 Part V. The "excess Medicare withholding" beyond 1.45% × $250,000 = $3,625 (i.e., $450) is the employer's Additional Medicare withholding, which the IRS routes to Form 1040 **line 25c**, not line 25a. Verifies that W-2 box 6 (Medicare tax withheld) does NOT bleed into line 25a (which is W-2 box 2 only).

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `setFilingStatusApi` | Filing status | `Single` |
| `seedW2ForMedicareWithholding` (`w-2`) | `medicareWagesAndTipsAmount` | `250000` |
| same | `medicareTaxWithheldAmount` | `4075` |
| same | `employeeSSN` | `111-11-1111` |
| (intentionally omitted) | `federalIncomeTaxWithheldAmount` | not seeded → null |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | null or 0 |
| `form1040.payments.withholdingOther` | `450` |
| `form1040.payments.totalWithholding` | `450` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 8959 instructions (Part V, withholding reconciliation):
  - Line 19 = W-2 box 6 (Medicare tax withheld) = $4,075.
  - Line 20 = Medicare wages = $250,000.
  - Line 21 = $250,000 × 1.45% = $3,625.
  - Line 22 = max(0, $4,075 − $3,625) = $450.
  - Line 23 = $0 (no Railroad Retirement Tax Act compensation).
  - Line 24 = $450 + $0 = $450 → Form 1040 line 25c.
- W-2 box 2 was not seeded → line 25a = $0.
- Line 25b = $0 (no 1099 entries).
- Line 25d = $0 + $0 + $450 = $450.

### Java compute path
- `buildForm8959(...)` reads `line1 = $250,000`, `line4 = $250,000`, `line5 = $200,000` (Single threshold), `line6 = $50,000`, `line7 = $50,000 × 0.009 = $450` (Part I), `line18 = $450`. Part V: `line19 = $4,075`, `line20 = $250,000`, `line21 = $3,625`, `line22 = $450`, `line24 = $450`.
- `computeLine31ThroughLine38(...)` reads `form8959.line24TotalAmtWithheld = $450` and adds it to `withholdingOther`.
- `sumFederalWithholdingFromEntries(w2Entries)` returns null because `federalIncomeTaxWithheldAmount` was not seeded.
- Line 25a = null; line 25c = $450; line 25d = $450.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK — W-2 box 6 does NOT bleed into line 25a. |
| 2 | `withholding1099 == null \|\| === 0` | ✅ OK. |
| 3 | `withholdingOther === 450` | ✅ OK — Form 8959 line 24 → line 25c. |
| 4 | `totalWithholding === 450` | ✅ OK. |

---

## Test 8 — `1099-NEC backup withholding flows to line 25b (G3)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "1099-NEC backup withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
Cross-line negative-coverage test for line 25a: 1099-NEC box 4 backup withholding ($650) flows to line 25b, not line 25a. Verifies the `form1099NecEntries` path through `sumFederalWithholdingFromMultipleLists`.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seed1099NecWithWithholding` (`1099-nec`) | `nonemployeeCompensationAmount` | `10000` |
| same | `federalIncomeTaxWithheldAmount` | `650` |
| same | `recipientTIN` | `111-11-1111` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `650` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `650` |

### Hand computation per Internal Revenue Service rule
- 1099-NEC box 4 is "Federal income tax withheld" (backup withholding under IRC §3406 when payee fails to furnish a TIN).
- 2025 Form 1040 line 25b covers all 1099-series withholding, including 1099-NEC box 4.
- Line 25b = $650; line 25a = $0; line 25d = $650.

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries)` returns null → `withholdingW2 = null`.
- `sumFederalWithholdingFromMultipleLists(...)` reads $650 from the 1099-NEC entry → `withholding1099 = $650`.
- Line 25d = $0 + $650 + $0 = $650.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK — 1099-NEC does NOT bleed into line 25a. |
| 2 | `withholding1099 === 650` | ✅ OK. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 650` | ✅ OK. |

---

## Test 9 — `MFJ: both spouses W-2 withholding combined on line 25a`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "MFJ: both spouses W-2 withholding combined" line25abcd-withholding.spec.ts
```

### Purpose
Filing-status edge case for line 25a: on a Married Filing Jointly return, the W-2 withholding of both spouses combines onto the same line 25a. Locks the no-per-SSN-filtering rule.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default taxpayer |
| `setFilingStatusApi` | Filing status | `Married filing jointly` (with `reloadAfterSave: true`) |
| `seedSpouseApi` | spouse `firstName`, `lastName`, `ssn`, `dateOfBirth` | `Jane`, `Doe`, `222-22-2222`, `1980-05-15` |
| `seedW2WithWithholding` (taxpayer `w-2`) | `wagesTipsOtherCompAmount` | `55000` |
| same | `federalIncomeTaxWithheldAmount` | `7000` |
| same | `employeeSSN` | `111-11-1111` |
| `seedW2WithWithholding` (spouse `w-2`) | `wagesTipsOtherCompAmount` | `40000` |
| same | `federalIncomeTaxWithheldAmount` | `5000` |
| same | `employeeSSN` | `222-22-2222` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | `12000` |
| `form1040.payments.totalWithholding` | `12000` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 1040 line 25a instruction for MFJ: "If you are married filing jointly, add the amounts shown as federal income tax withheld on both your Form(s) W-2 and your spouse's Form(s) W-2."
- Taxpayer box 2 + spouse box 2 = $7,000 + $5,000 = $12,000.
- Line 25d = $12,000.

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries)` iterates all W-2 entries for the return regardless of `employeeSSN` (per `XLS/Computations/25a.md` §2c note: "`w2Entries` is not filtered by Social Security Number"). Sum = $7,000 + $5,000 = $12,000.
- `withholdingW2 = $12,000`.
- Line 25d = $12,000.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `fsOk` truthy | ✅ OK — filing status save acknowledged. |
| 2 | `payments` truthy | ✅ OK. |
| 3 | `withholdingW2 === 12000` | ✅ OK — MFJ both-spouses combination. |
| 4 | `totalWithholding === 12000` | ✅ OK. |

---

## Spec file: line1a-form4852.spec.ts (5 tests)

This file primarily exercises Form 1040 line 1a (wages), but every test indirectly exercises the Form 4852 path that ALSO feeds line 25a via `line7eFederalIncomeTaxWithheld`. None of the tests in this spec seed `line7eFederalIncomeTaxWithheld` — the wage-side `line7a` is the focus — so line 25a is implicitly zero across all 5. The catalog records the line-25a relevance per assertion (default: "line 25a stays unaffected because line-7e is unseeded").

## Test 10 — `Form 4852 (taxpayer, w2 substitute) — wages flow to line 1a, no MISSING_W2 flag`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Form 4852 \(taxpayer, w2 substitute\) — wages flow to line 1a" line1a-form4852.spec.ts
```

### Purpose
Verifies that a taxpayer Form 4852 W-2 substitute with `line7aWagesTipsOtherCompensation = $12,000` flows to Form 1040 line 1a wages (without a real W-2 on the return), suppresses `MISSING_W2_EMPLOYMENT_INCOME_TAXPAYER`, and produces the Form 4852 attachment on the return with `line4IsFormW2 = true`.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single filer |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome` | `true` |
| same | `householdWork` | `false` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | `'w2'` |
| same | `employerOrPayerNameAddressZip` | `'Acme Corp, Albany NY'` |
| same | `line7aWagesTipsOtherCompensation` | `12000` |
| same | `line9HowAmountsWereDetermined` | `'Used pay stubs.'` |
| same | `line10EffortsToObtainMissingOrCorrectedForm` | `'Requested W-2 from employer.'` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `12000` |
| `flags[].code === 'MISSING_W2_EMPLOYMENT_INCOME_TAXPAYER'` | not present |
| `form4852TaxpayerList.length` | `1` |
| `form4852TaxpayerList[0].line4IsFormW2` | `true` |
| `form4852TaxpayerList[0].line7aWagesTipsOtherCompensation` | `12000` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 4852 instructions: when used as a Form W-2 substitute, line 7a (wages) feeds Form 1040 line 1a. Line 4 indicates "Form W-2."
- Line 1a = $12,000; line 25a = $0 because `line7e` was not seeded.
- The Form 4852 must be filed as an attachment to the return.

### Java compute path
- `extractForm4852WagesForPerson(...)` reads `line7a = $12,000` from the W-2-substitute entry → adds to line 1a.
- `validateEmploymentIncomeW2ForPerson(...)` sees a Form 4852 W-2 substitute exists → `MISSING_W2_EMPLOYMENT_INCOME_TAXPAYER` is suppressed.
- The `Form4852TaxpayerList` carries the attachment with `line4IsFormW2 = true`.
- Line 25a side: `extractForm4852Line25aWithholding(form4852TaxpayerData)` returns null because `line7e` was not seeded.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 12000` | ✅ OK — Form 4852 line 7a → line 1a. |
| 2 | flags do NOT include `MISSING_W2_EMPLOYMENT_INCOME_TAXPAYER` | ✅ OK. |
| 3 | `form4852TaxpayerList.length === 1` | ✅ OK — attachment generated. |
| 4 | `line4IsFormW2 === true` | ✅ OK. |
| 5 | `line7aWagesTipsOtherCompensation === 12000` | ✅ OK. |

Line-25a relevance: ✅ CLOSED via Gap A (§13b). After the 2026-06-10 closure the test now seeds `line7eFederalIncomeTaxWithheld: 1500` on the Form 4852 row and asserts `withholdingW2 === 1500` + `totalWithholding === 1500`, exercising the Form 4852 line-7e → line 25a wire end-to-end through `extractForm4852Line25aWithholding`.

---

## Test 11 — `Form 4852 + real W-2 — both flow to line 1a (no double-count, no replacement)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Form 4852 \+ real W-2 — both flow to line 1a" line1a-form4852.spec.ts
```

### Purpose
Verifies additive (not replacement) behavior on line 1a: real W-2 ($30,000) + Form 4852 W-2 substitute ($8,000) = $38,000. The Form 4852 attachment is still generated with its own $8,000 line 7a.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome` / `householdWork` | `true` / `false` |
| `seedStatementApi` (`w-2`) | `employeeSSN` | `111-11-1111` |
| same | `wagesTipsOtherCompAmount` | `30000` |
| same | `socialSecurityWagesAmount` | `30000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | `'w2'` |
| same | `employerOrPayerNameAddressZip` | `'Beta Corp'` |
| same | `line7aWagesTipsOtherCompensation` | `8000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `38000` |
| `form4852TaxpayerList.length` | `1` |
| `form4852TaxpayerList[0].line4IsFormW2` | `true` |
| `form4852TaxpayerList[0].line7aWagesTipsOtherCompensation` | `8000` |

### Hand computation per Internal Revenue Service rule
- W-2 box 1 ($30,000) AND Form 4852 line 7a ($8,000) both feed line 1a additively per the 2025 Form 1040 + Form 4852 instructions.
- Line 1a = $38,000; line 25a = $0 (neither box 2 nor line 7e seeded).

### Java compute path
- `sumW2WagesForPerson(...)` reads $30,000 from the W-2.
- `extractForm4852WagesForPerson(...)` reads $8,000 from the Form 4852.
- Line 1a = $30,000 + $8,000 = $38,000.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 38000` | ✅ OK — additive, not replacement. |
| 2 | `form4852TaxpayerList.length === 1` | ✅ OK. |
| 3 | `line4IsFormW2 === true` | ✅ OK. |
| 4 | `line7aWagesTipsOtherCompensation === 8000` | ✅ OK. |

Line-25a relevance: ✅ CLOSED via Gap G (§14b). After the 2026-06-10 second-pass closure the test now seeds `federalIncomeTaxWithheldAmount: 3000` on the W-2 and `line7eFederalIncomeTaxWithheld: 800` on the Form 4852 row, asserting `withholdingW2 === 3800` — the Single-filer same-person two-source aggregate ($3,000 + $800).

---

## Test 12 — `Form 4852 (spouse, MFJ) — substitute wages flow, no MISSING_W2_SPOUSE flag`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Form 4852 \(spouse, MFJ\) — substitute wages flow" line1a-form4852.spec.ts
```

### Purpose
Spouse-side parity for the canonical Form 4852 W-2 substitute path on MFJ: taxpayer real W-2 ($40,000) + spouse Form 4852 W-2 substitute ($15,000) = $55,000 line 1a; spouse Form 4852 attachment generated; `MISSING_W2_EMPLOYMENT_INCOME_SPOUSE` not present.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default taxpayer |
| `seedSpouseApi` | spouse `firstName`, `lastName`, `ssn`, `dateOfBirth`, `occupation` | `Pat`, `Tester`, `222-22-2222`, `1992-02-02`, `Analyst` |
| `setFilingStatusApi` | Filing status | `Married filing jointly` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome` / `householdWork` | `true` / `false` |
| `savePersonalFormApi` (`employment-income-spouse`) | `hasEmploymentIncome` / `householdWork` | `true` / `false` |
| `seedStatementApi` (`w-2`) | `employeeSSN` | `111-11-1111` |
| same | `wagesTipsOtherCompAmount` | `40000` |
| same | `socialSecurityWagesAmount` | `40000` |
| `savePersonalFormApi` (`form4852-spouse`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | `'w2'` |
| same | `line7aWagesTipsOtherCompensation` | `15000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `55000` |
| `flags[].code === 'MISSING_W2_EMPLOYMENT_INCOME_SPOUSE'` | not present |
| `form4852SpouseList.length` | `1` |
| `form4852SpouseList[0].line4IsFormW2` | `true` |
| `form4852SpouseList[0].line7aWagesTipsOtherCompensation` | `15000` |

### Hand computation per Internal Revenue Service rule
- MFJ: both spouses' wages combine on line 1a per the 2025 Form 1040 instructions.
- $40,000 + $15,000 = $55,000.
- Line 25a = $0 (no box 2 or line 7e seeded).

### Java compute path
- `sumW2WagesForPerson(taxpayer, ...) = $40,000`.
- `extractForm4852WagesForPerson(spouse, ...) = $15,000`.
- Line 1a = $55,000.
- `validateEmploymentIncomeW2ForPerson(spouse, ...)` sees a W-2 substitute → no `MISSING_W2_EMPLOYMENT_INCOME_SPOUSE`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 55000` | ✅ OK. |
| 2 | flags do NOT include `MISSING_W2_EMPLOYMENT_INCOME_SPOUSE` | ✅ OK. |
| 3 | `form4852SpouseList.length === 1` | ✅ OK. |
| 4 | `line4IsFormW2 === true` | ✅ OK. |
| 5 | `line7aWagesTipsOtherCompensation === 15000` | ✅ OK. |

Line-25a relevance: ✅ CLOSED via Gap C (§13d). After the 2026-06-10 closure the test now seeds `federalIncomeTaxWithheldAmount: 4000` on the taxpayer W-2 and `line7eFederalIncomeTaxWithheld: 2000` on the spouse Form 4852, asserting `withholdingW2 === 6000` — the MFJ cross-spouse aggregate where both sources contribute to the same line 25a.

---

## Test 13 — `Form 4852 with substituteFormType="1099-r" — does NOT flow to line 1a`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Form 4852 with substituteFormType.*1099-r.*does NOT flow to line 1a" line1a-form4852.spec.ts
```

### Purpose
Negative test for the `substituteFormType` routing: a Form 4852 with `substituteFormType = '1099-r'` and line-8 inputs ($10,000 gross + taxable) must NOT contribute to line 1a wages. The real W-2 ($40,000) is the only line 1a contributor. Confirms the 1099-R substitute attachment is still generated with `line4IsForm1099R = true` and `line4IsFormW2 = false`.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome` / `householdWork` | `true` / `false` |
| `seedStatementApi` (`w-2`) | `employeeSSN` | `111-11-1111` |
| same | `wagesTipsOtherCompAmount` | `40000` |
| same | `socialSecurityWagesAmount` | `40000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | `'1099-r'` |
| same | `line8aGrossDistribution` | `10000` |
| same | `line8bTaxableAmount` | `10000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `40000` |
| `form4852TaxpayerList.length` | `1` |
| `form4852TaxpayerList[0].line4IsForm1099R` | `true` |
| `form4852TaxpayerList[0].line4IsFormW2` | `false` |
| `form4852TaxpayerList[0].line8aGrossDistribution` | `10000` |
| `form4852TaxpayerList[0].line8bTaxableAmount` | `10000` |

### Hand computation per Internal Revenue Service rule
- Form 4852 line 4 box "Form 1099-R" indicates the substitute is for a missing 1099-R, NOT a W-2 → line 1a unchanged ($40,000); line 5a/5b pick up the substitute amounts instead.
- Line 25a = $0 (no box 2 or line 7e seeded).

### Java compute path
- `extractForm4852WagesForPerson(...)` skips the entry because `substituteFormType !== "w2"` → no line 1a contribution.
- `extractForm4852Line25aWithholding(...)` skips for the same reason → no line 25a contribution.
- Line 1a = $40,000 (real W-2 only); line 25a = $0.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 40000` | ✅ OK — 1099-R substitute correctly excluded from line 1a. |
| 2 | `form4852TaxpayerList.length === 1` | ✅ OK. |
| 3 | `line4IsForm1099R === true` | ✅ OK. |
| 4 | `line4IsFormW2 === false` | ✅ OK. |
| 5 | `line8aGrossDistribution === 10000` | ✅ OK. |
| 6 | `line8bTaxableAmount === 10000` | ✅ OK. |

Line-25a relevance: ✅ OK by construction — the test guarantees that a 1099-R substitute does NOT bleed into line 25a (because the W-2-only filter at `extractForm4852Line25aWithholding` skips it). No explicit assertion, but the line-1a-side assertion implicitly verifies the routing.

---

## Test 14 — `Form 4852 (spouse) on MFS — spouse substitute wages do NOT aggregate into taxpayer line 1a`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Form 4852 \(spouse\) on MFS — spouse substitute wages do NOT aggregate into taxpayer line 1a" line1a-form4852.spec.ts
```

### Purpose
MFS guard: on a Married Filing Separately return, the spouse's Form 4852 substitute wages ($15,000) must NOT aggregate into the taxpayer's line 1a. The taxpayer's real W-2 ($50,000) is the only contributor. The spouse Form 4852 is still PERSISTED (it'll be attached to the spouse's own separate return).

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default taxpayer |
| `seedSpouseApi` | spouse | `Pat`, `Tester`, `222-22-2222`, `1992-02-02`, `Analyst` |
| `setFilingStatusApi` | Filing status | `Married filing separately` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome` / `householdWork` | `true` / `false` |
| `seedStatementApi` (`w-2`) | `employeeSSN` | `111-11-1111` |
| same | `wagesTipsOtherCompAmount` | `50000` |
| same | `socialSecurityWagesAmount` | `50000` |
| `savePersonalFormApi` (`form4852-spouse`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | `'w2'` |
| same | `line7aWagesTipsOtherCompensation` | `15000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `50000` |
| `form4852SpouseList.length` | `1` |
| `form4852SpouseList[0].line7aWagesTipsOtherCompensation` | `15000` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 1040 line 1a on MFS: only the taxpayer's own wages count; the spouse's wages belong on the spouse's separate Form 1040.
- Taxpayer line 1a = $50,000; spouse Form 4852 still attached to the spouse's separate return.
- Taxpayer line 25a = $0 (no box 2 seeded; spouse Form 4852 `line7e` also not seeded; even if it were, the MFS guard at the composer would skip it).

### Java compute path
- `extractForm4852WagesForPerson(taxpayer, ...) = $0` (no taxpayer Form 4852).
- The orchestrator only sums spouse-side wages on MFJ paths → line 1a = $50,000.
- Line 25a side: `extractForm4852Line25aWithholding(form4852SpouseData)` would return null (no `line7e` seeded), but even if non-null the composer's `isMfsForForm4852 ? null : ...` guard at line 26619 prevents the spouse contribution.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 50000` | ✅ OK — MFS guard enforced on line 1a. |
| 2 | `form4852SpouseList.length === 1` | ✅ OK — spouse attachment persisted for spouse's own return. |
| 3 | `line7aWagesTipsOtherCompensation === 15000` | ✅ OK. |

Line-25a relevance: ✅ CLOSED via Gap B (§13c). After the 2026-06-10 closure the test now seeds `line7eFederalIncomeTaxWithheld: 3000` on the spouse Form 4852 row and asserts `withholdingW2 == null || === 0` — verifying the symmetric MFS guard on the line-25a side at `TaxReturnComputeService.java:26737` (`isMfsForForm4852 ? null : extractForm4852Line25aWithholding(form4852SpouseData)`).

---

## Spec file: employment-form4852-inmate-link.spec.ts (21 tests)

This spec covers the Employment Income ↔ Form 4852 inmate-wages linkage. The tests primarily exercise line 1a (wage source), Schedule 1 line 8u (inmate-wage reclassification), and the `MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_*` blocking flags. **Line-25a relevance is low across this spec**: none of the 21 tests seed `line7eFederalIncomeTaxWithheld` on any Form 4852 entry, and none seed `federalIncomeTaxWithheldAmount` on any W-2. The line-25a contribution is therefore $0 / null in every test. Per-test entries below still trace the line-25a wire and classify the assertion against the IRS rule, with the line-25a-relevance note appended where relevant.

## Test 15 — `Missing-W-2 advisory becomes visible when the user answers Yes`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Missing-W-2 advisory becomes visible when the user answers Yes" employment-form4852-inmate-link.spec.ts
```

### Purpose
UI test: the "Are you missing one or more W-2s from your employers?" question is rendered when `hasEmploymentIncome=Yes` is selected on the taxpayer Employment Income form, and the linked advisory ("Before continuing this form, open ...") appears when the user answers Yes, disappears when they switch to No.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `openEmploymentForm` (UI) | Navigation | sidebar → Employment income |
| `selectRadioOption` | `hasEmploymentIncome` | `Yes` |
| `selectRadioOption` | `isMissingW2` | `Yes` then `No` |

### Outputs the test asserts (UI visibility)
| What | Assertion |
|---|---|
| "Are you missing one or more W-2s from your employers?" | visible |
| "Before continuing this form, open ..." | `toHaveCount(0)` before `isMissingW2` is set |
| same | visible after `isMissingW2=Yes` |
| "Applications" link in advisory | visible |
| same | `toHaveCount(0)` after `isMissingW2=No` |

### Hand computation per Internal Revenue Service rule
- The 2025 Form 4852 instructions instruct the taxpayer to first try to obtain the missing W-2 from the employer or by calling the IRS; if all efforts fail, prepare a Form 4852 substitute. The application surfaces an advisory panel pointing the user to the Form 4852 form when they declare a missing W-2.
- This is a UX gating test only; no Form 1040 line computation involved.

### Java compute path
- N/A — this test is UI-only. Backend flag emission for `MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER` is covered by Test 19 below.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | Question visible | ✅ OK. |
| 2 | Advisory hidden initially | ✅ OK. |
| 3 | Advisory visible on Yes | ✅ OK. |
| 4 | "Applications" link visible | ✅ OK. |
| 5 | Advisory hidden on No | ✅ OK. |

Line-25a relevance: N/A — UI-only test.

---

## Test 16 — `Form 4852 W-2-substitute entry appears in the inmate-wages picker`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Form 4852 W-2-substitute entry appears in the inmate-wages picker" employment-form4852-inmate-link.spec.ts
```

### Purpose
UI test: when a Form 4852 W-2 substitute exists, the Employment Income form's inmate-wages picker renders a "Form 4852: <employer>" row alongside any real W-2 rows.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `50000`, `50000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType`, `employerOrPayerNameAddressZip`, `line7aWagesTipsOtherCompensation` | `'w2'`, `'Acme Prison Industries'`, `10000` |
| UI | `hasEmploymentIncome`, `householdWork`, `hasInmateWages` | `Yes`, `No`, `Yes` |

### Outputs the test asserts (UI)
- A label matching `/Box 1/` is visible (the real W-2 row).
- A label matching `/Form 4852:.*Acme Prison Industries.*Line 7a/i` is visible (the Form 4852 row).

### Hand computation per Internal Revenue Service rule
- N/A — UI-only test verifying the picker renders both source types.

### Java compute path
- N/A — UI-only test.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `/Box 1/` row visible | ✅ OK. |
| 2 | `/Form 4852:.*Acme Prison Industries.*Line 7a/i` row visible | ✅ OK. |

Line-25a relevance: N/A — UI-only test; no compute call.

---

## Test 17 — `Form 4852 1099-R-substitute entries are NOT shown in the inmate-wages picker`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Form 4852 1099-R-substitute entries are NOT shown in the inmate-wages picker" employment-form4852-inmate-link.spec.ts
```

### Purpose
UI test: a 1099-R-substitute Form 4852 is NOT shown in the W-2-only inmate-wages picker. Verifies the `substituteFormType === "w2"` filter at the picker level.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `50000`, `50000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType`, `line8aGrossDistribution`, `line8bTaxableAmount` | `'1099-r'`, `10000`, `10000` |
| UI | `hasEmploymentIncome`, `householdWork`, `hasInmateWages` | `Yes`, `No`, `Yes` |

### Outputs the test asserts (UI)
- A label matching `/Form 4852:/i` has count 0 (the 1099-R substitute does not appear).

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `/Form 4852:/i` count === 0 | ✅ OK — 1099-R substitute correctly filtered out of the W-2 picker. |

Line-25a relevance: N/A — UI-only test.

---

## Test 18 — `W-2 + Form 4852 mix: line 1a excludes flagged prison wages, line 8u carries them`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "W-2 \+ Form 4852 mix: line 1a excludes flagged prison wages" employment-form4852-inmate-link.spec.ts
```

### Purpose
Compute test: real W-2 ($50,000) + Form 4852 W-2 substitute ($10,000, flagged as inmate wages) → line 1a = $50,000 (Form 4852 subtracted as inmate), Schedule 1 line 8u = $10,000.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `50000`, `50000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType`, `employerOrPayerNameAddressZip`, `line7aWagesTipsOtherCompensation` | `'w2'`, `'Acme Prison Industries'`, `10000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome`, `householdWork`, `hasInmateWages`, `inmateForm4852Indices`, `inmateWagesAmount` | `true`, `false`, `true`, `[0]`, `10000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `50000` |
| `schedule1.additionalIncome.otherIncomeWagesWhileIncarcerated` | `10000` |

### Hand computation per Internal Revenue Service rule
- Per IRS Publication 525 and 2025 Schedule 1 instructions, wages earned while incarcerated must NOT be reported on Form 1040 line 1a; they are reclassified to Schedule 1 line 8u "Wages earned while incarcerated."
- Line 1a = $50,000 (real W-2) + $10,000 (Form 4852) − $10,000 (inmate) = $50,000.
- Schedule 1 line 8u = $10,000.
- Line 25a = $0 (no `federalIncomeTaxWithheldAmount` or `line7e` seeded).

### Java compute path
- `extractForm4852WagesForPerson(...)` returns $10,000; sumW2 = $50,000. Pre-clamp line 1a = $60,000.
- `applyInmateWagesReclassification(...)` subtracts `inmateWagesAmount = $10,000` → line 1a = $50,000.
- Schedule 1 line 8u = $10,000.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 50000` | ✅ OK — inmate reclassification correctly removes prison wages from line 1a. |
| 2 | `otherIncomeWagesWhileIncarcerated === 10000` | ✅ OK — Schedule 1 line 8u carries the reclassified amount. |

Line-25a relevance: line 25a is implicitly null because no withholding fields are seeded; not asserted but not at risk.

---

## Test 19 — `Form 4852-only prison wages: line 1a clamps to zero, line 8u correct`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Form 4852-only prison wages: line 1a clamps to zero" employment-form4852-inmate-link.spec.ts
```

### Purpose
Edge case: Form 4852 is the only wage source ($10,000), all of it inmate. Line 1a clamps to $0; Schedule 1 line 8u = $10,000.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852`, `forms[0].substituteFormType`, `forms[0].line7aWagesTipsOtherCompensation` | `true`, `'w2'`, `10000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome`, `householdWork`, `hasInmateWages`, `inmateForm4852Indices`, `inmateWagesAmount` | `true`, `false`, `true`, `[0]`, `10000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `0` (or null) |
| `schedule1.additionalIncome.otherIncomeWagesWhileIncarcerated` | `10000` |

### Hand computation per Internal Revenue Service rule
- Line 1a = $0 (Form 4852) + $10,000 (Form 4852) − $10,000 (inmate) = $0 — clamped at zero.
- Schedule 1 line 8u = $10,000.

### Java compute path
- `extractForm4852WagesForPerson(...) = $10,000`.
- `applyInmateWagesReclassification(...)` subtracts $10,000 → line 1a clamps to $0 via `subtractNonNegative(...)`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages ?? 0 === 0` | ✅ OK — clamp at zero verified. |
| 2 | `otherIncomeWagesWhileIncarcerated === 10000` | ✅ OK. |

Line-25a relevance: line 25a implicitly null (no `line7e` seed).

---

## Test 20 — `Compute blocks with MISSING_FORM4852 when user said Yes but no Form 4852 exists; clears after adding`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "Compute blocks with MISSING_FORM4852" employment-form4852-inmate-link.spec.ts
```

### Purpose
Verifies the canonical `MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER` blocking flag fires when `isMissingW2 = true` but no W-2-substitute Form 4852 exists, and clears after adding one.

### Inputs the test seeds (first compute)
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `30000`, `30000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome`, `householdWork`, `isMissingW2` | `true`, `false`, `true` |

### Then (second compute)
| Where | Field | Value |
|---|---|---|
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852`, `forms[0].substituteFormType`, `forms[0].employerOrPayerNameAddressZip`, `forms[0].line7aWagesTipsOtherCompensation` | `true`, `'w2'`, `'Acme Corp'`, `12000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| First compute: `response.ok` | `false` |
| First compute: `response.status` | `409` |
| First compute: `flagsBefore[].code === 'MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER'` | present |
| Second compute: `response.ok` | `true` |
| Second compute: `flagsAfter[].code === 'MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER'` | NOT present |

### Hand computation per Internal Revenue Service rule
- 2025 Form 4852 instructions: if a taxpayer cannot obtain a missing W-2 from the employer or IRS, they MUST attach Form 4852 to the return. The application enforces this by blocking compute until either (a) the user retracts `isMissingW2`, or (b) the user creates a W-2-substitute Form 4852.

### Java compute path
- `validateEmploymentIncomeW2ForPerson(...)` checks: if `isMissingW2 === true` AND no W-2-substitute Form 4852 entry exists → emit `MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER` with `blocking=true`.
- On 409, `TaxReturnResource.compute(...)` returns the flags array as the response body.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | First compute `ok === false` | ✅ OK. |
| 2 | First compute `status === 409` | ✅ OK. |
| 3 | `MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER` present in flagsBefore | ✅ OK. |
| 4 | Second compute `ok === true` | ✅ OK. |
| 5 | `MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER` not present in flagsAfter | ✅ OK. |

Line-25a relevance: line 25a not asserted; with `federalIncomeTaxWithheldAmount` unseeded on the W-2 and `line7e` unseeded on the Form 4852, line 25a is null in both compute calls. No GAP because the test is scoped to flag emission, not the line 25a wire.

---

## Test 21 — `GAP-B3 — spouse-side MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_SPOUSE fires`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-B3 — spouse-side MISSING_FORM4852" employment-form4852-inmate-link.spec.ts
```

### Purpose
Spouse-side parity for the `MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_SPOUSE` blocker on MFJ. Closes the gap that the taxpayer-side Test 20 leaves open.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default taxpayer |
| `setFilingStatusApi` | Filing status | `Married filing jointly` |
| `seedSpouseApi` | spouse | `222-22-2222`, `1988-06-15`, `Spouse`, `Tester` |
| `seedStatementApi` (`w-2`, spouse) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `222-22-2222`, `30000`, `30000` |
| `savePersonalFormApi` (`employment-income-spouse`) | `hasEmploymentIncome`, `householdWork`, `isMissingW2` | `true`, `false`, `true` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `response.ok` | `false` |
| `response.status` | `409` |
| `flags[].code === 'MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_SPOUSE'` | present |

### Hand computation per Internal Revenue Service rule
- Same as Test 20 but applied to the spouse-side gate.

### Java compute path
- `validateEmploymentIncomeW2ForPerson(spouse, ...)` emits the `_SPOUSE` suffix variant.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `ok === false` | ✅ OK. |
| 2 | `status === 409` | ✅ OK. |
| 3 | `_SPOUSE` blocker present | ✅ OK. |

Line-25a relevance: line 25a not asserted; not at risk.

---

## Test 22 — `GAP-F44-A — spouse-side missing-W-2 advisory becomes visible when the spouse answers Yes`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F44-A — spouse-side missing-W-2 advisory" employment-form4852-inmate-link.spec.ts
```

### Purpose
UI test: same advisory behavior as Test 15 but on the spouse-side Employment Income form, accessed via the Spouse person-tab.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default taxpayer |
| `setFilingStatusApi` | Filing status | `Married filing jointly` |
| `seedSpouseApi` | spouse | `222-22-2222`, `1988-06-15`, `Spouse`, `Tester` |
| UI | Person tab | `Spouse` |
| UI | sidebar → Employment income | open |
| UI | `hasEmploymentIncome` (via label click) | `Yes` |
| `selectRadioOption` | `isMissingW2` | `Yes` then `No` |

### Outputs the test asserts (UI)
- Advisory hidden before `isMissingW2` is set.
- Advisory visible after `isMissingW2=Yes`.
- Advisory hidden after `isMissingW2=No`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | Advisory hidden initially | ✅ OK. |
| 2 | Advisory visible on Yes | ✅ OK. |
| 3 | Advisory hidden on No | ✅ OK. |

Line-25a relevance: N/A — UI-only.

---

## Test 23 — `GAP-F45-A — multiple Form 4852 W-2 substitutes all appear in the inmate-wages picker`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F45-A — multiple Form 4852 W-2 substitutes all appear" employment-form4852-inmate-link.spec.ts
```

### Purpose
UI test: two Form 4852 W-2 substitutes both appear in the inmate-wages picker. Verifies the picker iterates the full `forms[]` array, not just the first entry.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `40000`, `40000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType`, `employerOrPayerNameAddressZip`, `line7aWagesTipsOtherCompensation` | `'w2'`, `'Acme Prison Industries'`, `8000` |
| same → `forms[1]` | `substituteFormType`, `employerOrPayerNameAddressZip`, `line7aWagesTipsOtherCompensation` | `'w2'`, `'Beta Reentry Workshop'`, `5000` |
| UI | `hasEmploymentIncome`, `householdWork`, `hasInmateWages` | `Yes`, `No`, `Yes` |

### Outputs the test asserts (UI)
- Acme Prison Industries Form 4852 row visible.
- Beta Reentry Workshop Form 4852 row visible.
- Exactly 2 `/Form 4852:/i` rows in the picker.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | Acme row visible | ✅ OK. |
| 2 | Beta row visible | ✅ OK. |
| 3 | Count `=== 2` | ✅ OK. |

Line-25a relevance: N/A — UI-only.

---

## Test 24 — `GAP-F46-A — mixed W-2 and 1099-R Form 4852 substitutes: only the W-2 substitute appears in the inmate picker`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F46-A — mixed W-2 and 1099-R Form 4852 substitutes" employment-form4852-inmate-link.spec.ts
```

### Purpose
UI test: in a mixed Form 4852 list (one W-2-substitute + one 1099-R-substitute), the inmate-wages picker shows only the W-2-substitute row.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `50000`, `50000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType`, `employerOrPayerNameAddressZip`, `line7aWagesTipsOtherCompensation` | `'w2'`, `'Acme Prison Industries'`, `10000` |
| same → `forms[1]` | `substituteFormType`, `employerOrPayerNameAddressZip`, `line8aGrossDistribution`, `line8bTaxableAmount` | `'1099-r'`, `'Acme Pension Plan'`, `7500`, `7500` |
| UI | `hasEmploymentIncome`, `householdWork`, `hasInmateWages` | `Yes`, `No`, `Yes` |

### Outputs the test asserts (UI)
- W-2 substitute row visible (`/Form 4852:.*Acme Prison Industries.*Line 7a/i`).
- 1099-R row NOT visible (`/Acme Pension Plan/i` count 0).
- Exactly 1 `/Form 4852:/i` row.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | W-2 row visible | ✅ OK. |
| 2 | 1099-R row not visible | ✅ OK. |
| 3 | Count `=== 1` | ✅ OK. |

Line-25a relevance: N/A — UI-only.

---

## Test 25 — `GAP-F47-A — inmate-wages mix propagates: line 1a + 1z + 8u + line 8 + line 9 + line 11a (AGI)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F47-A — inmate-wages mix propagates" employment-form4852-inmate-link.spec.ts
```

### Purpose
Full downstream propagation lock-in: W-2 ($50,000) + Form 4852 inmate ($10,000) → line 1a = $50,000, line 1z = $50,000, Schedule 1 line 8u = $10,000, line 8 = $10,000, line 9 = $60,000, line 11a AGI = $60,000.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `50000`, `50000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `forms[0].substituteFormType`, `forms[0].line7aWagesTipsOtherCompensation` | `'w2'`, `10000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasInmateWages`, `inmateForm4852Indices`, `inmateWagesAmount` | `true`, `[0]`, `10000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `50000` |
| `form1040.income.totalWages` | `50000` |
| `schedule1.additionalIncome.otherIncomeWagesWhileIncarcerated` | `10000` |
| `form1040.income.otherIncomeSchedule1` | `10000` |
| `form1040.income.totalIncome` | `60000` |
| `form1040.adjustments.line11aAdjustedGrossIncome` | `60000` |

### Hand computation per Internal Revenue Service rule
- Line 1a = $50,000 + $10,000 − $10,000 = $50,000; line 1z = $50,000.
- Schedule 1 line 8u = $10,000 → line 8 = $10,000.
- Line 9 (total income) = line 1z + line 2b + line 3b + line 4b + line 5b + line 6b + line 7a + line 8 = $50,000 + 0 + 0 + 0 + 0 + 0 + 0 + $10,000 = $60,000.
- Line 10 = $0; line 11a AGI = $60,000 − $0 = $60,000.

### Java compute path
- `applyInmateWagesReclassification(...)` clamps line 1a and routes to Schedule 1 line 8u.
- `computeLine9TotalIncome(...)` sums.
- `computeLine10ThroughLine11(...)` adjusts.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 50000` | ✅ OK. |
| 2 | `totalWages === 50000` | ✅ OK. |
| 3 | `otherIncomeWagesWhileIncarcerated === 10000` | ✅ OK. |
| 4 | `otherIncomeSchedule1 === 10000` | ✅ OK. |
| 5 | `totalIncome === 60000` | ✅ OK. |
| 6 | `line11aAdjustedGrossIncome === 60000` | ✅ OK. |

Line-25a relevance: line 25a implicitly null; not asserted.

---

## Test 26 — `GAP-F47-B — inmateForm4852Indices=[] with inmateWagesAmount>0 still subtracts from line 1a`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F47-B — inmateForm4852Indices=\[\] with inmateWagesAmount>0 still subtracts" employment-form4852-inmate-link.spec.ts
```

### Purpose
Edge case: `inmateForm4852Indices = []` (empty) with `inmateWagesAmount = 10000` still subtracts from line 1a — the subtraction is index-agnostic. Same arithmetic as Test 25.

### Inputs the test seeds (delta from Test 25)
- `inmateForm4852Indices` = `[]` (not `[0]`).

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `50000` |
| `schedule1.additionalIncome.otherIncomeWagesWhileIncarcerated` | `10000` |

### Java compute path
- `applyInmateWagesReclassification(...)` subtracts `inmateWagesAmount` regardless of which source the picker recorded — the index is recorded for audit purposes only.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 50000` | ✅ OK. |
| 2 | `otherIncomeWagesWhileIncarcerated === 10000` | ✅ OK. |

Line-25a relevance: line 25a implicitly null; not asserted.

---

## Test 27 — `GAP-F47-C — spouse-side inmate-wages mix: line 1a + line 8u parity (MFJ)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F47-C — spouse-side inmate-wages mix" employment-form4852-inmate-link.spec.ts
```

### Purpose
Spouse-side parity for the inmate-wages reclassification on MFJ.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `setFilingStatusApi` | Filing status | `Married filing jointly` |
| `seedSpouseApi` | spouse | `222-22-2222`, `1988-06-15`, `Spouse`, `Tester` |
| `seedStatementApi` (`w-2`, spouse) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `222-22-2222`, `40000`, `40000` |
| `savePersonalFormApi` (`form4852-spouse`) | `forms[0].substituteFormType`, `forms[0].line7aWagesTipsOtherCompensation` | `'w2'`, `8000` |
| `savePersonalFormApi` (`employment-income-spouse`) | `hasInmateWages`, `inmateForm4852Indices`, `inmateWagesAmount` | `true`, `[0]`, `8000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `40000` |
| `schedule1.additionalIncome.otherIncomeWagesWhileIncarcerated` | `8000` |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 40000` | ✅ OK. |
| 2 | `otherIncomeWagesWhileIncarcerated === 8000` | ✅ OK. |

Line-25a relevance: line 25a implicitly null; not asserted.

---

## Test 28 — `GAP-F47-D — combined taxpayer + spouse inmate-wages: line 1a + line 8u aggregate correctly (MFJ)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F47-D — combined taxpayer \+ spouse inmate-wages" employment-form4852-inmate-link.spec.ts
```

### Purpose
Both spouses have inmate wages on MFJ: the subtraction from line 1a and the addition to line 8u sum across both.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default taxpayer |
| `setFilingStatusApi` | Filing status | `Married filing jointly` |
| `seedSpouseApi` | spouse | `222-22-2222`, `1988-06-15`, `Spouse`, `Tester` |
| `seedStatementApi` (`w-2`, taxpayer) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `50000`, `50000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `forms[0]` | `w2`, `6000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `inmateForm4852Indices`, `inmateWagesAmount` | `[0]`, `6000` |
| `seedStatementApi` (`w-2`, spouse) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `222-22-2222`, `30000`, `30000` |
| `savePersonalFormApi` (`form4852-spouse`) | `forms[0]` | `w2`, `4000` |
| `savePersonalFormApi` (`employment-income-spouse`) | `inmateForm4852Indices`, `inmateWagesAmount` | `[0]`, `4000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `80000` |
| `schedule1.additionalIncome.otherIncomeWagesWhileIncarcerated` | `10000` |
| `form1040.income.otherIncomeSchedule1` | `10000` |

### Hand computation per Internal Revenue Service rule
- Pre-clamp wages = $50,000 + $6,000 + $30,000 + $4,000 = $90,000.
- Total inmate = $6,000 + $4,000 = $10,000.
- Line 1a = $90,000 − $10,000 = $80,000.
- Schedule 1 line 8u = $10,000.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 80000` | ✅ OK. |
| 2 | `otherIncomeWagesWhileIncarcerated === 10000` | ✅ OK. |
| 3 | `otherIncomeSchedule1 === 10000` | ✅ OK. |

Line-25a relevance: line 25a implicitly null; not asserted.

---

## Test 29 — `GAP-F47-E — INMATE_WAGES_DUPLICATE_RISK_TAXPAYER advisory fires on dual-entry`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F47-E — INMATE_WAGES_DUPLICATE_RISK_TAXPAYER advisory fires" employment-form4852-inmate-link.spec.ts
```

### Purpose
When inmate wages are entered from BOTH the Employment Income form ($10,000) AND the Other Incomes form line 8u ($5,000), the `INMATE_WAGES_DUPLICATE_RISK_TAXPAYER` advisory fires. Both sources legitimately add to line 8u (the user manually entering $5,000 via the other-incomes form is intentional).

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `50000`, `50000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `forms[0]` | `w2`, `10000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasInmateWages`, `inmateForm4852Indices`, `inmateWagesAmount` | `true`, `[0]`, `10000` |
| `savePersonalFormApi` (`other-incomes-taxpayer`) | `otherIncomeWagesWhileIncarcerated8u` | `5000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `flags[].code === 'INMATE_WAGES_DUPLICATE_RISK_TAXPAYER'` | present |
| `schedule1.additionalIncome.otherIncomeWagesWhileIncarcerated` | `15000` |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `INMATE_WAGES_DUPLICATE_RISK_TAXPAYER` present | ✅ OK. |
| 2 | `otherIncomeWagesWhileIncarcerated === 15000` | ✅ OK. |

Line-25a relevance: line 25a implicitly null; not asserted.

---

## Test 30 — `GAP-F48-A — over-clamp: inmateWagesAmount > total wages fires §17 non-overrideable INMATE_WAGES_EXCEEDS_WAGE_SOURCES blocker`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F48-A — over-clamp" employment-form4852-inmate-link.spec.ts
```

### Purpose
§17 non-overrideable blocking flag: when `inmateWagesAmount > sum(W-2 + Form 4852 line 7a)`, the compute is rejected with 409 even when `overrideFlags=true` is passed.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `savePersonalFormApi` (`form4852-taxpayer`) | `forms[0]` | `w2`, `10000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `inmateWagesAmount` | `15000` (exceeds the only $10,000 wage source) |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `blocked.ok` | `false` |
| `blocked.status` | `409` |
| `flag.code` | `'INMATE_WAGES_EXCEEDS_WAGE_SOURCES'` |
| `flag.blocking` | `true` |
| Compute with `overrideFlags=true`: `status` | `409` (non-overrideable per §17) |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `ok === false` | ✅ OK. |
| 2 | `status === 409` | ✅ OK. |
| 3 | `flag` truthy | ✅ OK. |
| 4 | `flag.blocking === true` | ✅ OK. |
| 5 | `overrideResp.status === 409` | ✅ OK — §17 non-overrideability. |

Line-25a relevance: N/A — the test verifies the §17 blocker; no successful compute occurs.

---

## Test 31 — `GAP-F48-B — Form 4852-only inmate wages downstream: line 1z=0 + line 8=10000 + line 9=10000 + AGI=10000`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F48-B — Form 4852-only inmate wages downstream" employment-form4852-inmate-link.spec.ts
```

### Purpose
Full downstream propagation when Form 4852 is the only wage source AND all of it is inmate wages: line 1a + line 1z clamp to $0, line 8 = $10,000, line 9 = $10,000, line 11a AGI = $10,000.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `savePersonalFormApi` (`form4852-taxpayer`) | `forms[0]` | `w2`, `10000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasInmateWages`, `inmateForm4852Indices`, `inmateWagesAmount` | `true`, `[0]`, `10000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages ?? 0` | `0` |
| `form1040.income.totalWages ?? 0` | `0` |
| `schedule1.additionalIncome.otherIncomeWagesWhileIncarcerated` | `10000` |
| `form1040.income.otherIncomeSchedule1` | `10000` |
| `form1040.income.totalIncome` | `10000` |
| `form1040.adjustments.line11aAdjustedGrossIncome` | `10000` |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages ?? 0 === 0` | ✅ OK. |
| 2 | `totalWages ?? 0 === 0` | ✅ OK. |
| 3 | `otherIncomeWagesWhileIncarcerated === 10000` | ✅ OK. |
| 4 | `otherIncomeSchedule1 === 10000` | ✅ OK. |
| 5 | `totalIncome === 10000` | ✅ OK. |
| 6 | `line11aAdjustedGrossIncome === 10000` | ✅ OK. |

Line-25a relevance: line 25a implicitly null; not asserted.

---

## Test 32 — `GAP-F48-C — spouse-side Form 4852-only inmate wages: line 1a clamps to 0, line 8u = full amount (MFJ)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F48-C — spouse-side Form 4852-only inmate wages" employment-form4852-inmate-link.spec.ts
```

### Purpose
Spouse-side parity for the Form-4852-only inmate-wages clamp on MFJ.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default taxpayer |
| `setFilingStatusApi` | Filing status | `Married filing jointly` |
| `seedSpouseApi` | spouse | `222-22-2222`, `1988-06-15`, `Spouse`, `Tester` |
| `savePersonalFormApi` (`form4852-spouse`) | `forms[0]` | `w2`, `7500` |
| `savePersonalFormApi` (`employment-income-spouse`) | `hasInmateWages`, `inmateForm4852Indices`, `inmateWagesAmount` | `true`, `[0]`, `7500` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages ?? 0` | `0` |
| `schedule1.additionalIncome.otherIncomeWagesWhileIncarcerated` | `7500` |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages ?? 0 === 0` | ✅ OK. |
| 2 | `otherIncomeWagesWhileIncarcerated === 7500` | ✅ OK. |

Line-25a relevance: line 25a implicitly null; not asserted.

---

## Test 33 — `GAP-F49-A — MISSING_FORM4852 fires when only a 1099-R substitute exists (no W-2 substitute)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F49-A — MISSING_FORM4852 fires when only a 1099-R substitute exists" employment-form4852-inmate-link.spec.ts
```

### Purpose
Verifies `MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER` still fires when a Form 4852 entry exists but it is a 1099-R substitute, not a W-2 substitute. The gate is satisfied only by a W-2-substitute entry.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `30000`, `30000` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `forms[0].substituteFormType`, `forms[0].line8aGrossDistribution`, `forms[0].line8bTaxableAmount` | `'1099-r'`, `5000`, `5000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome`, `householdWork`, `isMissingW2` | `true`, `false`, `true` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `response.ok` | `false` |
| `response.status` | `409` |
| `flags[].code === 'MISSING_FORM4852_AFTER_USER_REPORTED_MISSING_W2_TAXPAYER'` | present |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `ok === false` | ✅ OK. |
| 2 | `status === 409` | ✅ OK. |
| 3 | blocker present | ✅ OK — W-2-substitute filter at `hasForm4852W2SubstituteEntry()` correctly excludes the 1099-R entry. |

Line-25a relevance: line 25a not asserted; compute is blocked.

---

## Test 34 — `GAP-F49-B — post-fix compute returns line 1a = W-2 + Form 4852 = 42000`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F49-B — post-fix compute returns line 1a" employment-form4852-inmate-link.spec.ts
```

### Purpose
Once the user adds a W-2-substitute Form 4852 ($12,000) after declaring `isMissingW2=true`, the compute returns line 1a = $30,000 (real W-2) + $12,000 (Form 4852) = $42,000.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `30000`, `30000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome`, `householdWork`, `isMissingW2` | `true`, `false`, `true` |
| `savePersonalFormApi` (`form4852-taxpayer`) | `forms[0]` | `w2`, `12000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.income.wages` | `42000` |
| `form1040.income.totalWages` | `42000` |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `wages === 42000` | ✅ OK. |
| 2 | `totalWages === 42000` | ✅ OK. |

Line-25a relevance: line 25a implicitly null; not asserted.

---

## Test 35 — `GAP-F49-C — MFS taxpayer-only isMissingW2: only the TAXPAYER blocker fires (not the spouse one)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "GAP-F49-C — MFS taxpayer-only isMissingW2" employment-form4852-inmate-link.spec.ts
```

### Purpose
On an MFS return with `isMissingW2=true` for the taxpayer only (no spouse state set), only the `_TAXPAYER` blocker fires; the `_SPOUSE` blocker does NOT fire. Locks the per-person isolation of the validator.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default taxpayer |
| `setFilingStatusApi` | Filing status | `Married filing separately` |
| `seedSpouseApi` | spouse | `222-22-2222`, `1988-06-15`, `Spouse`, `Tester` |
| `seedStatementApi` (`w-2`) | `employeeSSN`, `wagesTipsOtherCompAmount`, `socialSecurityWagesAmount` | `111-11-1111`, `30000`, `30000` |
| `savePersonalFormApi` (`employment-income-taxpayer`) | `hasEmploymentIncome`, `householdWork`, `isMissingW2` | `true`, `false`, `true` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `response.ok` | `false` |
| `response.status` | `409` |
| `_TAXPAYER` blocker | present |
| `_SPOUSE` blocker | NOT present |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `ok === false` | ✅ OK. |
| 2 | `status === 409` | ✅ OK. |
| 3 | `_TAXPAYER` present | ✅ OK. |
| 4 | `_SPOUSE` not present | ✅ OK — spouse never declared `isMissingW2`. |

Line-25a relevance: line 25a not asserted; compute is blocked.

---

## Spec file: w2-document-extraction.spec.ts (8 tests, ALL `test.skip`)

**Skip directive at file top (line 26):** `test.skip(() => true, 'Disabled — requires Azure Document Intelligence service running')`. Every test in this file is unconditionally skipped in the current run; the catalog records inputs and outputs for completeness but classifies each per-assertion verdict as 🚫 **SKIPPED** until the Azure Document Intelligence service is wired in to CI.

These tests primarily exercise the W-2 OCR / extraction pipeline (`POST /api/statements/w-2/extract`), the upload-extraction UI banner, and form-field population. **Line-25a relevance is indirect**: if extraction is reliable, the `federalIncomeTaxWithheldAmount` field in the form payload is populated from W-2 box 2 of the uploaded PDF, and a subsequent compute would surface that on line 25a. None of the tests here exercise compute; the line-25a contribution is verified upstream of the compute path, at the extraction layer.

## Test 36 — `API: Extract W-2 returns mapped fields from uploaded PDF`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "API: Extract W-2 returns mapped fields from uploaded PDF" w2-document-extraction.spec.ts
```

### Purpose
Verifies the Azure Document Intelligence extraction returns a non-empty mapped-fields payload from `C:\us-tax\test_statements\w-2_sample.pdf`, with numeric fields typed as `number`.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `baseSetup` | DOB + Address + `employment-income-taxpayer` (`hasEmploymentIncome=true`, `isHouseholdEmployee=false`) | default |
| `extractViaApi` | `POST /api/statements/w-2/extract` with PDF body | `w-2_sample.pdf` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `result` | truthy |
| `Object.keys(result).length` | > 0 |
| `result.wagesTipsOtherCompAmount` | `typeof === 'number'` (if present) |
| `result.federalIncomeTaxWithheldAmount` | `typeof === 'number'` (if present) |

### Hand computation per Internal Revenue Service rule
- N/A — the extraction is read-only; it merely returns the values printed on the PDF. The IRS rule (`box 2 = federal income tax withheld`) is what the PDF preserves.

### Java compute path
- `DocumentExtractionResource.extract(...)` → Azure Document Intelligence "prebuilt-tax.us.w2" model → `W2ExtractionMapper.mapToCanonicalFields(...)`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `result` truthy | 🚫 SKIPPED. |
| 2 | `keys.length > 0` | 🚫 SKIPPED. |
| 3 | `wagesTipsOtherCompAmount` is number | 🚫 SKIPPED. |
| 4 | `federalIncomeTaxWithheldAmount` is number | 🚫 SKIPPED — DIRECTLY relevant to line 25a; ideal candidate to revive once Azure is wired in. |

Line-25a relevance: HIGH — verifies that the extraction layer produces the canonical line-25a-feeding field name. Currently skipped.

---

## Test 37 — `API: Extract rejects unsupported form type`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "API: Extract rejects unsupported form type" w2-document-extraction.spec.ts
```

### Purpose
Negative test: `POST /api/statements/1099-int/extract` with a dummy PDF returns HTTP 400.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `baseSetup` | DOB + Address + `employment-income-taxpayer` | default |
| `fetch` | `POST /api/statements/1099-int/extract` with `Blob(['dummy'], application/pdf)` | dummy |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `error.ok` | `false` |
| `error.status` | `400` |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `ok === false` | 🚫 SKIPPED. |
| 2 | `status === 400` | 🚫 SKIPPED. |

Line-25a relevance: indirect — verifies that ONLY W-2 PDFs reach the W-2 extractor.

---

## Test 38 — `API: Extract rejects unsupported file type`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "API: Extract rejects unsupported file type" w2-document-extraction.spec.ts
```

### Purpose
Negative test: a text/plain file rejected with HTTP 400 by the W-2 extraction endpoint.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `baseSetup` | default | |
| `fetch` | `POST /api/statements/w-2/extract` with `Blob(['not a real file'], text/plain)` | dummy |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `error.ok` | `false` |
| `error.status` | `400` |

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `ok === false` | 🚫 SKIPPED. |
| 2 | `status === 400` | 🚫 SKIPPED. |

Line-25a relevance: indirect.

---

## Test 39 — `UI: Upload button visible on W-2 statement form`

### Purpose
UI test: the "Upload statement" button is visible on the W-2 statement form when the user navigates to it.

### Inputs / Outputs / Verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | Upload statement button visible | 🚫 SKIPPED. |

Line-25a relevance: indirect.

---

## Test 40 — `UI: Upload populates form fields and shows extraction banner`

### Purpose
Full UI extraction flow: upload PDF → extraction completes → banner visible → button label changes to "Verify & Save."

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | Extraction button no longer shows "Extracting..." | 🚫 SKIPPED. |
| 2 | Extraction banner visible | 🚫 SKIPPED. |
| 3 | Banner contains "auto-extracted" | 🚫 SKIPPED. |
| 4 | "Verify & Save" button visible | 🚫 SKIPPED. |

Line-25a relevance: indirect.

---

## Test 41 — `UI: Verify & Save clears extraction banner and reverts button label`

### Purpose
Verify-and-save flow: clicking "Verify & Save" removes the banner and reverts the button label to plain "Save."

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | Banner visible pre-save | 🚫 SKIPPED. |
| 2 | Banner not visible post-save | 🚫 SKIPPED. |
| 3 | "Save" button visible (exact label) | 🚫 SKIPPED. |

Line-25a relevance: indirect.

---

## Test 42 — `UI: Placeholders hidden after extraction`

### Purpose
The form receives the `hide-placeholders` class after extraction completes.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `form.hide-placeholders` visible | 🚫 SKIPPED. |

Line-25a relevance: indirect.

---

## Test 43 — `E2E: Extracted values from API match populated form fields`

### Purpose
The most comprehensive extraction test: extract via API, then upload via UI, then compare model values against the API extraction response field-by-field. Numeric fields include `wagesTipsOtherCompAmount`, `federalIncomeTaxWithheldAmount`, `socialSecurityWagesAmount`, `socialSecurityTaxWithheldAmount`, `medicareWagesAndTipsAmount`, `medicareTaxWithheldAmount`. Boolean fields include `statutoryEmployee`, `retirementPlan`. Box 12 entries and state/local rows verified element-by-element. Finally, the data persists across a `page.reload()`.

### Per-assertion verdict
| # | Assertion (representative) | Verdict |
|---|---|---|
| 1 | `extracted.keys.length > 0` | 🚫 SKIPPED. |
| 2 | text fields: `input.inputValue === extracted[key]` | 🚫 SKIPPED. |
| 3 | numeric fields: `modelValues[field].toBeCloseTo(extracted[field], 2)` | 🚫 SKIPPED — INCLUDES `federalIncomeTaxWithheldAmount`. |
| 4 | boolean fields: `modelValues.statutoryEmployee === extracted.statutoryEmployee` | 🚫 SKIPPED. |
| 5 | name fields: `modelValues.employeeFirstName === extracted.employeeFirstName` | 🚫 SKIPPED. |
| 6 | box 12 entries match element-wise | 🚫 SKIPPED. |
| 7 | state/local info matches element-wise | 🚫 SKIPPED. |
| 8 | persistence across reload: saved value === extracted value | 🚫 SKIPPED. |

Line-25a relevance: HIGH — assertion #3 directly verifies that `federalIncomeTaxWithheldAmount` survives the extraction → UI → save → reload round-trip. Ideal candidate to revive once Azure is wired in.

---

## Spec file: w2-required-employment.spec.ts (2 tests)

This spec covers two upstream gating tests for the W-2 path that affect line 25a indirectly. Both tests verify a precondition for line 25a being populated — without a W-2, line 25a is null by construction.

## Test 44 — `householdWork is mandatory when hasEmploymentIncome=Yes`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "householdWork is mandatory when hasEmploymentIncome=Yes" w2-required-employment.spec.ts
```

### Purpose
UI test: when `hasEmploymentIncome=Yes` is selected on the Employment Income form, the `householdWork` question becomes mandatory. The Save button is disabled until it is answered.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `openEmploymentTaxpayer` (UI) | Navigation | sidebar → Employment income |
| `selectRadioOption` | `hasEmploymentIncome` | `Yes` |
| `selectRadioOption` | `householdWork` | `No` |

### Outputs the test asserts (UI)
- Required-question error text "Please answer this question — required to determine whether wages route through line 1a or 1b." visible after `hasEmploymentIncome=Yes`.
- Save button disabled.
- After `householdWork=No`: error count 0; Save button enabled.
- Save click succeeds.

### Hand computation per Internal Revenue Service rule
- The 2025 Form 1040 instructions route household-employee wages (Schedule H income, line 1b) separately from employer-W-2 wages (line 1a). The application enforces this routing decision at save time so downstream compute knows whether wages belong on line 1a or 1b.
- Line 25a is unaffected by the routing of the wages themselves (box 2 still applies in either case), but the gate exists so the underlying W-2 statement is captured.

### Java compute path
- N/A — UI-only test of save-button gating.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | Error text visible | ✅ OK. |
| 2 | Save disabled | ✅ OK. |
| 3 | Error gone after `householdWork=No` | ✅ OK. |
| 4 | Save enabled | ✅ OK. |
| 5 | PUT 200 succeeds | ✅ OK. |

Line-25a relevance: indirect — verifies that the save gate is enforced so that downstream W-2 statements can be entered.

---

## Test 45 — `W-2 is required when wages are reported and household work is No`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a -g "W-2 is required when wages are reported and household work is No" w2-required-employment.spec.ts
```

### Purpose
UI + flag test: with `hasEmploymentIncome=Yes`, `householdWork=No`, and no W-2 entered, the "Compute return" button triggers a flag fetch that returns `MISSING_W2_EMPLOYMENT_INCOME_TAXPAYER`. The actual `POST /api/tax-return/compute` is NOT called.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default |
| `openEmploymentTaxpayer` (UI) | Navigation | sidebar → Employment income |
| `selectRadioOption` | `hasEmploymentIncome` | `Yes` |
| `selectRadioOption` | `householdWork` | `No` |
| UI | Inline advisory "You indicated wages with no household work. Enter or upload a W-2 before computing the return." | visible |
| `saveEmploymentTaxpayer` (UI) | save | |
| UI | "Compute return" button click | triggers flag fetch |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| Inline advisory text visible | yes |
| `GET /api/tax-return/flags` response `ok` | `true` |
| `flags` is array | yes |
| `flags[].code === 'MISSING_W2_EMPLOYMENT_INCOME_TAXPAYER'` | present |
| `computeCalled` (POST `/api/tax-return/compute` issued) | `false` |

### Hand computation per Internal Revenue Service rule
- N/A — the test verifies the application's pre-compute gate that requires either a W-2 or a Form 4852 W-2 substitute when wages are reported. The 2025 Form 1040 line 1a is supported by Form(s) W-2; the absence of a W-2 (with no `isMissingW2`+Form-4852 substitute) means line 1a is unsupported and the compute is rejected upstream.

### Java compute path
- `getReturnFlags(...)` runs the same validators as `compute(...)` but without mutating state; `validateEmploymentIncomeW2ForPerson(...)` emits `MISSING_W2_EMPLOYMENT_INCOME_TAXPAYER`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | Inline advisory visible | ✅ OK. |
| 2 | Flags response `ok` | ✅ OK. |
| 3 | `Array.isArray(flags)` | ✅ OK. |
| 4 | `MISSING_W2_EMPLOYMENT_INCOME_TAXPAYER` present | ✅ OK. |
| 5 | `computeCalled === false` | ✅ OK — the missing-W-2 gate stops compute upstream. |

Line-25a relevance: HIGH — verifies that when line 25a's underlying W-2 is missing, the compute does not proceed at all. The gate is upstream of line 25a population, but is the principal blocker that prevents a $0-W-2-box-2 misreporting.

---

## §10 Per-spec summary

| Spec file | Tests | Status | Notes |
|---|---|---|---|
| `line25abcd-withholding.spec.ts` | 9 | ✅ all pass | Canonical line 25a/25b/25c/25d wires. Covers the W-2 (Test 1), 1099-R (Test 2), SSA-1099 (Test 3), W-2G (Test 4), three-source aggregate (Test 5), 1099-DA G1 closure (Test 6), Form 8959 Part V G2 closure (Test 7), 1099-NEC G3 closure (Test 8), and MFJ both-spouses Test 9. |
| `line1a-form4852.spec.ts` | 5 | ✅ all pass | Form 4852 W-2 substitute path. Tests 10/11/12/14 modified 2026-06-10 to add line-25a assertions (Gaps A, G, C, B closed — see §13 and §14). Test 13 (1099-r substitute → does not flow to line 1a) is ✅ OK by construction for line 25a (no `line7e` seeded; routing filter ensures null). |
| `employment-form4852-inmate-link.spec.ts` | 21 | ✅ all pass | Employment Income ↔ Form 4852 inmate-wages linkage. UI rendering, inmate reclassification, MISSING_FORM4852 blockers, INMATE_WAGES_EXCEEDS_WAGE_SOURCES §17 blocker. Line 25a not asserted in any test. |
| `w2-document-extraction.spec.ts` | 8 | 🚫 all skipped | Azure Document Intelligence-dependent. Tests 36 and 43 directly verify `federalIncomeTaxWithheldAmount` (line 25a's source field). Revive priority: HIGH once Azure is wired in. |
| `w2-required-employment.spec.ts` | 2 | ✅ all pass | Upstream W-2 / household-work gating. Test 45 verifies the `MISSING_W2_EMPLOYMENT_INCOME_TAXPAYER` blocker that prevents compute when line 25a's underlying W-2 is missing. |

**Totals: 45 tests; 37 OK assertions across 37 passing tests; 8 SKIPPED tests with explicit `test.skip` directive; 0 MISMATCHES.**

## §11 Outstanding gaps in test_cases/25a.md

Each gap below is a 🕳️ GAP — a missing line-25a assertion that the existing tests COULD plausibly add without changing the test seed. None of these gaps is a MISMATCH against the IRS rule; they are coverage holes.

**Closure status (2026-06-10):** 6 of 7 gaps closed in this session — Gaps A, B, C, E, F closed in §13; Gap G (surfaced in second-pass re-audit) closed in §14. Only Gap D remains (policy-blocked — Azure Document Intelligence service required). See §13 and §14 for the closure logs.

### Gap A — Form 4852 line 7e withholding wire to line 25a is e2e-uncovered

**Where:** All 5 tests in `line1a-form4852.spec.ts`, plus all 21 tests in `employment-form4852-inmate-link.spec.ts`.

**What:** None of these 26 tests seed `line7eFederalIncomeTaxWithheld` on a Form 4852 row. As a result, the line-25a wire from Form 4852 (`extractForm4852Line25aWithholding`) has zero e2e coverage. The §9 Java lock-in `form4852NegativeLine7eIsFilteredOutOfLine25a` covers the positive-filter at the unit-test level only.

**IRS rule:** 2025 Form 4852 line 7e is "Federal income tax withheld" — a substitute for box 2 of the missing W-2. The IRS expects this amount to be added to Form 1040 line 25a alongside box 2 of any real W-2s on the return.

**Fix proposal:** Add a `line7eFederalIncomeTaxWithheld: 1500` to the `forms[0]` payload of Test 10 (`Form 4852 (taxpayer, w2 substitute) — wages flow to line 1a, no MISSING_W2 flag`) and an additional assertion:
```typescript
expect(Number(computation.form1040.payments.withholdingW2)).toBe(1500);
```
Single field addition; no structural changes to the test.

### Gap B — MFS spouse-side line 25a guard is e2e-uncovered

**Where:** Test 14 (`Form 4852 (spouse) on MFS — spouse substitute wages do NOT aggregate into taxpayer line 1a`).

**What:** The test verifies the line 1a side of the MFS guard. The symmetric line-25a-side guard at `TaxReturnComputeService.java:26619` (`isMfsForForm4852 ? null : extractForm4852Line25aWithholding(form4852SpouseData)`) is not exercised by any e2e test.

**IRS rule:** On MFS, the spouse's Form 4852 substitute belongs on the SPOUSE's separate Form 1040 line 25a, not on the taxpayer's. The composer's MFS guard enforces this.

**Fix proposal:** Add a `line7eFederalIncomeTaxWithheld: 3000` to the spouse Form 4852 payload in Test 14 and an additional assertion:
```typescript
expect(computation.form1040.payments.withholdingW2 == null
  || Number(computation.form1040.payments.withholdingW2) === 0).toBeTruthy();
```

### Gap C — MFJ two-source line 25a aggregate (W-2 + Form 4852) is e2e-uncovered

**Where:** Test 12 (`Form 4852 (spouse, MFJ) — substitute wages flow, no MISSING_W2_SPOUSE flag`).

**What:** The test verifies the line 1a sum ($55,000) but does not check that on MFJ both the spouse's Form 4852 line 7e AND the taxpayer's W-2 box 2 combine onto a single line 25a.

**Fix proposal:** Add `federalIncomeTaxWithheldAmount: 4000` to the taxpayer W-2 seed, `line7eFederalIncomeTaxWithheld: 2000` to the spouse Form 4852 seed, and a paired assertion:
```typescript
expect(Number(computation.form1040.payments.withholdingW2)).toBe(6000);
```

### Gap G — Single-filer same-person two-source line 25a aggregate is e2e-uncovered

**Where:** Test 11 (`Form 4852 + real W-2 — both flow to line 1a (no double-count, no replacement)`) in `line1a-form4852.spec.ts`.

**What:** The test verifies the line 1a side ($30,000 W-2 + $8,000 Form 4852 = $38,000) but the W-2 box 2 and the Form 4852 line 7e are both unseeded, so the symmetric two-source line 25a aggregate is silently untested. This is distinct from Gap C (which covers the MFJ CROSS-SPOUSE aggregate — taxpayer W-2 + spouse Form 4852 — under a different composer guard). Gap G covers the Single-filer SAME-PERSON aggregate (taxpayer W-2 + same taxpayer's Form 4852, with no MFS guard relevant on the taxpayer-side path).

**IRS rule:** Per the 2025 Instructions for Form 1040 line 25a and the 2025 Form 4852 instructions, the taxpayer's W-2 box 2 + the taxpayer's own Form 4852 line 7e (W-2 substitute) BOTH flow ADDITIVELY to the same line 25a — neither replaces the other. The composer at `TaxReturnComputeService.java:26734-26735` enforces this with two unconditional `addNonNull(...)` calls before the MFS-gated third addition.

**Fix proposal:** Add `federalIncomeTaxWithheldAmount: 3000` to the taxpayer W-2 seed in Test 11 and `line7eFederalIncomeTaxWithheld: 800` to the Form 4852 row, plus:
```typescript
expect(Number(computation?.form1040?.payments?.withholdingW2)).toBe(3800);
```

### Gap D — w2-document-extraction tests are universally skipped

**Where:** All 8 tests in `w2-document-extraction.spec.ts`.

**What:** The file-level `test.skip(() => true, 'Disabled — requires Azure Document Intelligence service running')` at line 26 means none of these tests run today. Of the 8, Test 36 (`API: Extract W-2 returns mapped fields from uploaded PDF`) and Test 43 (`E2E: Extracted values from API match populated form fields`) directly verify `federalIncomeTaxWithheldAmount` extraction, which is the upstream source of line 25a values for users who upload a W-2 PDF rather than typing it in by hand.

**Fix proposal:** Wire Azure Document Intelligence credentials into CI (`AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT` + `AZURE_DOCUMENT_INTELLIGENCE_KEY`) and place a sample W-2 PDF at `C:\us-tax\test_statements\w-2_sample.pdf`, then remove the file-level `test.skip` directive.

### Gap E — No e2e test exercises the G4 duplicate W-2 employer EIN advisory

**Where:** `XLS/Computations/25a.md` §8c documents the G4 closure (helper `validateDuplicateW2EmployerEin` at `TaxReturnComputeService.java:4587`). Two Java lock-in tests (`duplicateW2EmployerEinFiresNonBlockingAdvisoryFlag`, `distinctW2EmployerEinsDoNotFireDuplicateAdvisory`) exist.

**What:** No e2e test seeds two W-2 entries with matching `employerEIN` and asserts the `DUPLICATE_W2_EMPLOYER_EIN` advisory fires in the compute response. The advisory is non-blocking so it would surface in `computation.flags` rather than as a 409.

**Fix proposal:** Add a new test to `line25abcd-withholding.spec.ts`:
```typescript
test('Two W-2s with same employer EIN fires DUPLICATE_W2_EMPLOYER_EIN advisory (G4)', async ({ page }) => {
  await clearUserData(page);
  await seedRequiredDob(page);
  await seedW2WithWithholding(page, { wages: 30000, withheld: 4000, employeeSSN: '111-11-1111' });
  await seedW2WithWithholding(page, { wages: 20000, withheld: 2500, employeeSSN: '111-11-1111' });
  // Both with employerEIN: '12-3456789' set explicitly.
  const result = await computeReturnApi(page, { overrideFlags: true });
  const flags = result?.flags ?? [];
  expect(flags.some(f => f.code === 'DUPLICATE_W2_EMPLOYER_EIN')).toBe(true);
  expect(Number(result.form1040.payments.withholdingW2)).toBe(6500);
});
```

### Gap F — No e2e test exercises the G5 Form 4852 ambiguous substituteFormType blocker

**Where:** `XLS/Computations/25a.md` §8d documents the G5 closure (helper `validateForm4852SubstituteTypeExplicit` at `TaxReturnComputeService.java:4638`). Two Java lock-in tests exist; no e2e tests.

**Fix proposal:** Add an e2e test that seeds a Form 4852 row with both `line7aWagesTipsOtherCompensation` and `line8aGrossDistribution` set but without `substituteFormType`, and asserts the compute returns 409 with `FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER`.

---

## §12 Bottom line for line 25a e2e coverage

The 37 passing tests across 5 spec files give comprehensive coverage of:
- **The W-2 box 2 wire** (Tests 1, 5, 9) and the MFJ both-spouses combination (Test 9).
- **The cross-line negative coverage**: 1099-R box 4 → 25b (Tests 2, 5), SSA-1099 box 6 → 25b (Test 3), W-2G box 4 → 25c (Test 4), 1099-DA box 2a → 25b (Test 6), Form 8959 Part V → 25c (Test 7), 1099-NEC box 4 → 25b (Test 8).
- **The Form 4852 W-2-substitute wage path** that satisfies the missing-W-2 gate (Tests 10–14 and the 21 inmate-link tests).
- **The §17 non-overrideable inmate-wages-exceeds-sources blocker** (Test 30).
- **The MISSING_FORM4852 blockers for both taxpayer (Tests 20, 33, 34, 35) and spouse (Test 21, 22)**.

The 8 skipped tests in `w2-document-extraction.spec.ts` represent a coverage hole HIGH on the priority list once Azure Document Intelligence is wired in: Tests 36 and 43 directly verify the `federalIncomeTaxWithheldAmount` field that feeds line 25a from the uploaded PDF.

The 6 enumerated gaps (A–F) in §11 represent additive opportunities — each is a single field + assertion addition to an existing test, NOT a structural test refactor — so closing them would be cheap.

**Verdict:** Line 25a is solidly covered by the e2e suite for the W-2 box 2 path. The Form 4852 line 7e path has Java-unit coverage (G6 lock-in) but no e2e coverage. The G4 / G5 closures have Java-unit coverage but no e2e coverage. The §17 non-overrideability is covered. No MISMATCHES against the IRS rule were identified during this audit.

---

## §13 Gap closure log — 2026-06-10

This section records the concrete tests modified or added to close Gaps A, B, C, E, and F. Gap D is policy-blocked (requires Azure Document Intelligence credentials + sample PDF + CI wiring) and remains documented but uncloseable from inside the test-catalog scope.

### §13a Closure summary

| Gap | Closure mechanism | Test location | Status |
|---|---|---|---|
| A | Add `line7eFederalIncomeTaxWithheld: 1500` + line 25a assertion to Test 10 | `line1a-form4852.spec.ts:36-46, 60-65` | ✅ CLOSED |
| B | Add spouse `line7eFederalIncomeTaxWithheld: 3000` + line 25a null assertion to Test 14 | `line1a-form4852.spec.ts:251-261, 285-290` | ✅ CLOSED |
| C | Add taxpayer W-2 `federalIncomeTaxWithheldAmount: 4000` + spouse `line7eFederalIncomeTaxWithheld: 2000` + line 25a sum assertion to Test 12 | `line1a-form4852.spec.ts:135-141, 156-161, 187-191` | ✅ CLOSED |
| D | (policy-blocked — Azure DI service required) | `w2-document-extraction.spec.ts:26` | 🚫 OUT OF SCOPE |
| E | NEW Test 10 + Test 11 (negative) in `line25abcd-withholding.spec.ts` | `line25abcd-withholding.spec.ts` (Test 10 = `DUPLICATE_W2_EMPLOYER_EIN advisory`; Test 11 = distinct-EIN negative) | ✅ CLOSED |
| F | NEW Test 12 + Test 13 (negative) in `line25abcd-withholding.spec.ts` + new `computeReturnRaw` local helper | `line25abcd-withholding.spec.ts` (Test 12 = `FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER blocker`; Test 13 = explicit-type negative) | ✅ CLOSED |

### §13b Gap A — Form 4852 line 7e wire to line 25a

**IRS rule referenced (2025 Form 4852 instructions, line 7e):** "Federal income tax withheld." The IRS expects this amount to be added to Form 1040 line 25a as if it were W-2 box 2.

**Java compute path verified (`TaxReturnComputeService.java`):**
- `extractForm4852Line25aWithholding(form4852TaxpayerData)` at line 24588 reads `entry.get("line7eFederalIncomeTaxWithheld")`, skips entries whose `substituteFormType !== "w2"`, applies the G6 positive-filter, returns the sum.
- Composer at line 26735 (`withholdingW2 = addNonNull(withholdingW2, extractForm4852Line25aWithholding(form4852TaxpayerData))`) always adds the taxpayer-side result.
- With no W-2 on the return (`sumFederalWithholdingFromEntries(w2Entries) = null`) and one Form 4852 W-2 substitute with `line7e=$1500`, the composer writes `withholdingW2 = $1,500`.

**Test modification:** Test 10 (`line1a-form4852.spec.ts:25-67`) — added `line7eFederalIncomeTaxWithheld: 1500` to the existing Form 4852 W-2-substitute row and two assertions:
```typescript
expect(Number(computation?.form1040?.payments?.withholdingW2)).toBe(1500);
expect(Number(computation?.form1040?.payments?.totalWithholding)).toBe(1500);
```

### §13c Gap B — MFS spouse-side line 25a guard

**IRS rule referenced:** 2025 Instructions for Form 1040, "Married filing separately" guidance: each spouse files their own Form 1040 with only their own W-2 and Form 4852 substitute withholding on line 25a.

**Java compute path verified:** Composer at line 26737 — `addNonNull(withholdingW2, isMfsForForm4852 ? null : extractForm4852Line25aWithholding(form4852SpouseData))`. The ternary guards the spouse-side contribution on MFS.

**Test modification:** Test 14 (`line1a-form4852.spec.ts:213-271`) — added `line7eFederalIncomeTaxWithheld: 3000` to the spouse Form 4852 W-2-substitute row and a null/zero assertion on the taxpayer's `withholdingW2`. Confirms that the spouse's $3,000 line 7e on MFS is gated out of the taxpayer's line 25a.

### §13d Gap C — MFJ two-source line 25a aggregate

**IRS rule referenced:** 2025 Form 1040 line 25a instruction for MFJ: "Add the amounts shown as federal income tax withheld on both your Form(s) W-2 and your spouse's Form(s) W-2." Form 4852 substitutes follow the same rule per the 2025 Form 4852 instructions.

**Java compute path verified:**
- `sumFederalWithholdingFromEntries(w2Entries)` sums box 2 across all W-2 entries (no per-SSN filtering on withholding, per `XLS/Computations/25a.md` §2c).
- The composer also adds `extractForm4852Line25aWithholding(form4852SpouseData)` because on MFJ `isMfsForForm4852` is false.
- With taxpayer W-2 box 2 = $4,000 and spouse Form 4852 line 7e = $2,000, `withholdingW2 = $4,000 + $2,000 = $6,000`.

**Test modification:** Test 12 (`line1a-form4852.spec.ts:107-194`) — added `federalIncomeTaxWithheldAmount: 4000` to the taxpayer W-2 seed and `line7eFederalIncomeTaxWithheld: 2000` to the spouse Form 4852 row, plus the assertion `expect(Number(computation?.form1040?.payments?.withholdingW2)).toBe(6000)`.

### §13e Gap E — G4 duplicate W-2 employer EIN advisory

**IRS rule referenced:** 2025 Form W-2c instructions: "If you receive a Form W-2c, replace the original W-2 with the corrected amounts; do not add the W-2c as a separate entry on your return." The G4 advisory surfaces this risk when 2+ W-2 entries share an employer EIN.

**Java compute path verified:**
- `validateDuplicateW2EmployerEin(w2Entries, flags, uid)` at line 4600 normalizes each W-2's `employerEIN` by stripping non-digits, counts occurrences, emits a NON-BLOCKING advisory `DUPLICATE_W2_EMPLOYER_EIN` when any count ≥ 2.
- Non-blocking because both interpretations (W-2c replacement vs legitimately-distinct W-2s from the same employer) are valid; only the user can disambiguate.
- Compute returns 200; advisory flag in `result.flags[]`; line 25a still sums both W-2 box 2 amounts.

**New tests added (`line25abcd-withholding.spec.ts`):**

| # | Test | Assertion | Verdict |
|---|---|---|---|
| 10 | Two same-EIN W-2s ($4,000 + $2,500) | `DUPLICATE_W2_EMPLOYER_EIN` present, `blocking === false`, `withholdingW2 === 6500` | ✅ OK |
| 11 | Two distinct-EIN W-2s ($4,000 + $2,500) | `DUPLICATE_W2_EMPLOYER_EIN` NOT present, `withholdingW2 === 6500` | ✅ OK (negative half) |

Helper `seedW2WithWithholding` extended to accept an optional `employerEIN` parameter at `line25abcd-withholding.spec.ts:33-47`.

### §13f Gap F — G5 Form 4852 ambiguous substituteFormType blocker

**IRS rule referenced:** 2025 Form 4852 instructions, line 4: the form REQUIRES the taxpayer to check exactly one of "Form W-2" / "Form 1099-R." A row with both kinds of inputs filled and neither box checked is malformed and cannot be filed.

**Java compute path verified:**
- `validateForm4852SubstituteTypeExplicit(form4852Data, flags, uid, label)` at line 4644 iterates entries; if any entry has no `substituteFormType` AND `hasAnyForm4852Line7Input(entry) && hasAnyForm4852Line8Input(entry)`, emits a BLOCKING flag `FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER` (or `_SPOUSE`).
- The flag is NOT in `NonOverrideableFlags.CODES` (verified by `grep` returning 0). So default compute (no `overrideFlags`) returns 409; `overrideFlags=true` would bypass it (and silently misroute the withholding to line 25b via the `hasAnyForm4852Line8Input` inference fallback in `extractForm4852Line25aWithholding`).

**New tests added (`line25abcd-withholding.spec.ts`):**

| # | Test | Assertion | Verdict |
|---|---|---|---|
| 12 | Form 4852 row with line-7a + line-7e + line-8a + line-8b, no `substituteFormType` | `status === 409`, `FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER` blocker present, `blocker.blocking === true` | ✅ OK |
| 13 | Same shape with `substituteFormType: 'w2'` set | `FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER` NOT present, `withholdingW2 === 500` | ✅ OK (negative half) |

Local `computeReturnRaw` helper added inline at `line25abcd-withholding.spec.ts` (mirrors the pattern in `employment-form4852-inmate-link.spec.ts`). The `getStoredAuthToken` + `waitForShellReady` imports were added to support it.

### §13g Files changed

```
e2e/tests/line25abcd-withholding.spec.ts
  + imports: getStoredAuthToken, waitForShellReady
  + seedW2WithWithholding: optional employerEIN parameter
  + Test 10 (Gap E positive): DUPLICATE_W2_EMPLOYER_EIN advisory
  + Test 11 (Gap E negative): distinct EINs do not fire advisory
  + Test 12 (Gap F positive): FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER blocker
  + Test 13 (Gap F negative): explicit substituteFormType does not fire blocker
  + computeReturnRaw local helper

e2e/tests/line1a-form4852.spec.ts
  ~ Test 10: + line7eFederalIncomeTaxWithheld + 2 line-25a assertions (Gap A)
  ~ Test 12: + W-2 box 2 + spouse line 7e + line-25a sum assertion (Gap C)
  ~ Test 14: + spouse line 7e + line-25a-stays-null assertion (Gap B)
```

### §13h Run verification

```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a-gap-closure `
  line25abcd-withholding.spec.ts `
  line1a-form4852.spec.ts `
  employment-form4852-inmate-link.spec.ts `
  w2-required-employment.spec.ts
```

Expected (was 45 → now 49 tests, +4 new):
- `line25abcd-withholding.spec.ts`: **13 tests** (9 existing + 4 new).
- `line1a-form4852.spec.ts`: 5 tests (Tests 10, 12, 14 each now carry an extra line-25a assertion).
- `employment-form4852-inmate-link.spec.ts`: 21 tests, unchanged.
- `w2-required-employment.spec.ts`: 2 tests, unchanged.
- `w2-document-extraction.spec.ts`: 8 skipped (Gap D — policy-blocked).

**Total: 41 expected passing + 8 skipped + 0 failures.**

### §13i What remains open after §13

After this closure session, the **only outstanding gap for line 25a is Gap D** (8 `w2-document-extraction.spec.ts` tests are universally skipped because the Azure Document Intelligence service is not running in CI). Closing Gap D requires CI infrastructure changes (Azure credentials + sample W-2 PDF + harness wiring), NOT test fixes — it is intentionally out of scope for a test-catalog audit.

### §13j Lessons captured

Per `feedback_principled_diagnosis_over_test_tweaking` ("read IRS docs → line spec → compute code FIRST; never match test to observed"), every closure followed the same sequence:

1. **Read the IRS rule** for the field being asserted (2025 Form 1040 + Form 4852 + W-2c instructions).
2. **Read the Java compute code** for the actual wire (`extractForm4852Line25aWithholding`, `validateDuplicateW2EmployerEin`, `validateForm4852SubstituteTypeExplicit`, the composer at line 26735–26737).
3. **Read the mapper** to confirm canonical field names (`Form4852Mapper.java:83` for `line7eFederalIncomeTaxWithheld`, `W2Mapper.java:123` for `employerEIN`, per `feedback_grep_mapper_for_seed_field_names`).
4. **Read the §17 registry** (`NonOverrideableFlags.java`) to confirm G4 / G5 codes are NOT registered → determine whether `overrideFlags=true` bypasses them.
5. **Then** write the test seed using canonical field names, derive the expected line-25a value by hand from the IRS rule, and write the `expect(...).toBe(N)` assertion.

The most subtle step was Gap F: the test could NOT use the default `computeReturnApi(page, { overrideFlags: true })` pattern (which is the pattern used by every other test in `line25abcd-withholding.spec.ts`) because `overrideFlags=true` would bypass the non-§17 blocker. Required adding a local `computeReturnRaw` helper. Without reading `NonOverrideableFlags.java` (step 4), this would have surfaced as a confusing 200-instead-of-409 mismatch during the test-run verification — the trial-and-error trap the principled-diagnosis rule warns against.

---

## §14 Gap closure log — 2026-06-10 second pass

The §13 closure list was "complete relative to the first pass" but a second principled re-audit (per `feedback_principled_diagnosis_over_test_tweaking` — "A complete gap closure list is only complete relative to the previous pass") surfaced one gap that §11 enumerated only implicitly via the per-test Line-25a-relevance footers: **Gap G — Single-filer same-person two-source line 25a aggregate** (Test 11's untested two-source wire).

### §14a What the second pass found

Re-running `Grep` for the `🕳️ GAP` marker across `test_cases/25a.md` returned 4 per-test gap markers (Tests 10, 11, 12, 14) plus the §10/§11 banner markers. Cross-referencing against §13 closures:

| Test | Per-test GAP | §13 closure | Match? |
|---|---|---|---|
| Test 10 | line 7e wire to line 25a uncovered | Gap A (§13b) | ✅ |
| Test 11 | W-2 box 2 + Form 4852 line 7e same-person uncovered | (none) | ❌ **MISSED** |
| Test 12 | spouse-side MFJ wire uncovered | Gap C (§13d) | ✅ |
| Test 14 | MFS spouse-side guard uncovered | Gap B (§13c) | ✅ |

The miss is Gap G — Test 11's two-source wire is structurally distinct from Gap C (Test 12) because Gap C is the MFJ cross-spouse aggregate (subject to MFS guard on the spouse-side path), while Gap G is the Single-filer same-person aggregate (no MFS guard relevant on the taxpayer-side path, which is always unconditionally added).

### §14b Gap G — Single-filer same-person two-source line 25a

**IRS rule referenced:** 2025 Instructions for Form 1040 line 25a + 2025 Form 4852 instructions: a taxpayer's own W-2 box 2 + the same taxpayer's Form 4852 line 7e (W-2 substitute) both flow ADDITIVELY to line 25a, with neither replacing the other.

**Java compute path verified (`TaxReturnComputeService.java:26734-26741`):**
- Line 26734: `withholdingW2 = sumFederalWithholdingFromEntries(w2Entries);` — picks up the taxpayer's W-2 box 2.
- Line 26735: `withholdingW2 = addNonNull(withholdingW2, extractForm4852Line25aWithholding(form4852TaxpayerData));` — UNCONDITIONALLY adds the taxpayer's own Form 4852 line 7e (no MFS guard on this side).
- Line 26736-26737: spouse-side is MFS-gated, but irrelevant for a Single filer.
- Line 26740-26741: G2 `roundMoney` + `setWithholdingW2`.
- With W-2 box 2 = $3,000 and Form 4852 line 7e = $800, `withholdingW2 = $3,000 + $800 + null = $3,800`.

**Test modification:** Test 11 (`line1a-form4852.spec.ts:64-105`) — added `federalIncomeTaxWithheldAmount: 3000` to the W-2 seed and `line7eFederalIncomeTaxWithheld: 800` to the Form 4852 row, plus assertions:
```typescript
expect(Number(computation?.form1040?.payments?.withholdingW2)).toBe(3800);
expect(Number(computation?.form1040?.payments?.totalWithholding)).toBe(3800);
```

### §14c Why §13 missed it

`§13` was driven by the §11 enumeration (Gaps A–F). The §11 enumeration was extracted by hand from §10's "Line 25a not asserted in any test" comment on `line1a-form4852.spec.ts` — but only 3 of the 4 per-test footers (Tests 10, 12, 14) were promoted to enumerated gaps. Test 11's footer was overlooked because the §11 author conflated Gap C (MFJ cross-spouse) with the broader "two-source line 25a" concept, missing that Single-filer same-person and MFJ cross-spouse exercise STRUCTURALLY DIFFERENT composer paths (different MFS guard interaction).

**Diagnostic shortcut for future passes:** before enumerating gaps in §11, run `Grep` for `🕳️ GAP` across the catalog and ensure every per-test footer gap has a corresponding §11 entry. The §10 summary line "Line 25a not asserted in any test" is an UNDERCOUNT — 4 distinct per-test gaps in `line1a-form4852.spec.ts` map to 4 distinct §11 entries (A, B, C, G), not 3.

### §14d Closure status summary after §14

| Gap | Severity | Status | Tests |
|---|---|---|---|
| A | Coverage | ✅ CLOSED (§13b) | Test 10 modified — 2 new assertions |
| B | Coverage | ✅ CLOSED (§13c) | Test 14 modified — null-guard assertion |
| C | Coverage | ✅ CLOSED (§13d) | Test 12 modified — MFJ cross-spouse sum assertion |
| D | Policy | 🚫 OUT OF SCOPE | Azure Document Intelligence required — 8 tests skipped |
| E | Coverage | ✅ CLOSED (§13e) | 2 new tests in `line25abcd-withholding.spec.ts` (positive + negative) |
| F | Coverage | ✅ CLOSED (§13f) | 2 new tests + `computeReturnRaw` local helper |
| **G** | **Coverage** | **✅ CLOSED (§14b)** | **Test 11 modified — Single-filer same-person sum assertion** |

**Total: 6 of 7 gaps closed; 1 remaining (Gap D, policy-blocked). 0 MISMATCHES against the IRS rule.**

### §14e Files changed in §14

```
e2e/tests/line1a-form4852.spec.ts
  ~ Test 11: + federalIncomeTaxWithheldAmount: 3000 on W-2 seed
              + line7eFederalIncomeTaxWithheld: 800 on Form 4852 row
              + 2 line-25a assertions ($3,800 on withholdingW2 + totalWithholding)
```

### §14f Run verification after §14

```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25a-gap-closure-v2 `
  line25abcd-withholding.spec.ts `
  line1a-form4852.spec.ts `
  employment-form4852-inmate-link.spec.ts `
  w2-required-employment.spec.ts
```

Expected: 41 passing (was 41 after §13 — Test 11 modification adds 2 assertions but no new test, so total count is unchanged) + 8 skipped Gap D + 0 failures.

### §14g Lesson captured

The principled-diagnosis re-audit pattern paid off again: a `Grep` for `🕳️ GAP` against the same catalog file took ~10 seconds and surfaced a real coverage hole that the §11 enumeration missed. **A complete gap closure list is only complete relative to the previous pass — a second principled pass against the same body of work will often uncover gaps the first pass missed, especially where the §10 summary table abbreviates per-test footers into a banner sentence.** Adding "Grep for `🕳️ GAP` after enumerating gaps in §11" to the audit-completion checklist would prevent the recurrence.

