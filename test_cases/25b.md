# Form 1040 line 25b — Federal income tax withheld from Form(s) 1099 — e2e test catalog (audit)

Source spec (per `XLS/Computations/25b.md` and the line-25b dependency map):
- `C:\us-tax\us-tax-be\e2e\tests\line25abcd-withholding.spec.ts` (18 tests)

Audit date: 2026-06-10. Result of the full run: **18 tests; 18 passed, 0 failed, 0 skipped.**

The single spec covers all four Form 1040 sub-lines 25a/25b/25c/25d because the composer at `TaxReturnComputeService.java:26747-26800` (`computeLine31ThroughLine38`) sets all four in one block. Each test in this catalog is classified by its primary line-25b relevance — direct positive flow, direct negative flow (line 25b stays null when the input belongs on a sibling line), or sibling-context (line 25b is null by construction; the test verifies a 25a / 25c / 25d wire that does not interact with 25b).

Method: for every `test(...)` — (1) read the inputs the test seeds, (2) compute by hand what the Internal Revenue Service rule requires from those inputs per the 2025 Instructions for Form 1040 (`docs/books/i1040gi_2025.txt`), Publication 17, Publication 525, Publication 575, Publication 590-A, Publication 590-B, the 2025 Form W-2 / W-2c / W-2G / 1099-R / 1099-INT / 1099-DIV / 1099-K / 1099-DA / 1099-NEC / 1099-PATR / SSA-1099 / RRB-1099 / Form 4852 instructions, the relevant Internal Revenue Code sections (§3406 backup withholding, §3402 voluntary withholding, §3405 pension default withholding, §3101(b)(2) Additional Medicare Tax), (3) read the Java compute path in `TaxReturnComputeService.java` and the related mappers, (4) compare against the `expect(...).toBe(...)` (or `.toBeTruthy()`, `.toBe(true/false)`, etc.) assertions, (5) classify each assertion **✅ OK** / **⚠️ MISMATCH** / **🕳️ GAP**.

Backend entry points relevant to line 25b:
- `sumFederalWithholdingFromEntries(...)` at `TaxReturnComputeService.java:27550` — generic helper that reads `federalIncomeTaxWithheldAmount` (canonical key) from each entry, positive-amounts only.
- `sumFederalWithholdingFromMultipleLists(...)` at `TaxReturnComputeService.java:27587` — variadic wrapper that delegates to the generic helper across all 1099-series entry lists.
- `sumSsa1099Withholding(...)` at `TaxReturnComputeService.java:27570` — dedicated reader for SSA-1099's non-canonical `voluntaryFederalIncomeTaxWithheldAmount` field (box 6).
- `extractForm4852Line25bWithholding(...)` at `TaxReturnComputeService.java:24621` — sums `line8fFederalIncomeTaxWithheld` across Form 4852 entries with `substituteFormType === "1099-r"`. Carries the §9d (Gap C) positive-filter.
- `validateDuplicateW2EmployerEin(...)` — non-blocking advisory (line-25a path; tests 10–11 reference but do not affect line 25b).
- `validateForm4852SubstituteTypeExplicit(...)` — blocking flag when a Form 4852 row has both line-7 and line-8 inputs without explicit `substituteFormType` (tests 12–13 reference; protects routing between line 25a and 25b).
- `computeLine31ThroughLine38(...)` composer at `TaxReturnComputeService.java:26747-26800` — assembles all three sub-lines and writes `Payments.withholdingW2`, `Payments.withholding1099`, `Payments.withholdingOther`, and the line 25d total.

See `C:\us-tax\XLS\Computations\25b.md` for the IRS-canonical line 25b derivation and the §9 / §10 closure logs (Gaps A–D in §9; Gaps F, G, H in §10; Gap E policy-blocked).

Shared 2025 reference data (centralized in `ReferenceData.java`):
- **None for line 25b directly** — line 25b has no rate, no threshold, no inflation-indexed constant. The withholding amounts arrive already-computed in each 1099-series box, Form 4852 line 8f, SSA-1099 box 6, or RRB-1099 box 10 / RRB-1099-R box 9.
- For Form 8959 cross-line (Test 7) context: Additional Medicare Tax rate `0.9%` (Internal Revenue Code §3101(b)(2)); regular Medicare rate `1.45%` (Internal Revenue Code §3101(b)(1)); Single threshold `$200,000`.

Recent changes affecting these tests:
- **§9b Gap A (1099-K mapper alias, 2026-06-10)** — `Form1099KMapper.loadOne(...)` now exposes `federalIncomeTaxWithheldAmount` as a canonical alias alongside the non-canonical `federalIncomeTaxWithheld4Amount`. Mirrors the 2026-06-10 1099-DA fix. **Test 14.**
- **§9c Gap B (1099-PATR compute wiring, 2026-06-10)** — `prepare()` now loads `form1099PtrEntries` via `listEntriesWithData(uid, "1099-ptr")`; the composer signature and call site gain the new parameter; the variadic call includes the list. **Test 15.**
- **§9d Gap C (Form 4852 line 8f positive filter, 2026-06-10)** — `extractForm4852Line25bWithholding` now applies the same positive-value filter the line-25a helper got in 25a.md §9 G6.
- **§9e Gap D (line 25b sub-total rounding, 2026-06-10)** — composer now wraps `roundMoney(withholding1099)` before `setWithholding1099`, mirroring the 25a.md §8b G2 closure.
- **§10b Gap H (taxpayer Form 4852 line 8f e2e, 2026-06-10 second pass)** — **Test 16.**
- **§10c Gap F (MFS spouse-side Form 4852 line 8f guard e2e, 2026-06-10 second pass)** — **Test 17.**
- **§10d Gap G (MFJ cross-spouse Form 4852 line 8f e2e, 2026-06-10 second pass)** — **Test 18.**

How to run **all 18 line-25b e2e tests** from a normal PowerShell terminal (never from inside the Claude Code sandbox — Playwright worker spawn fails with `EPERM`):

```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b line25abcd-withholding.spec.ts
```

Convention used in every test in this catalog: `seedRequiredDob(page)` writes the default taxpayer identification (`firstName="E2E"`, `lastName="Tester"`, `ssn="111-11-1111"`, `dateOfBirth="1990-01-01"`, `occupation="Analyst"`) and the default address (`1 Main St, Albany, NY 12207`). Filing status defaults to Single unless the test calls `setFilingStatusApi(...)`.

---

## Spec file: line25abcd-withholding.spec.ts (18 tests)

## Test 1 — `W-2 box 2 federal withholding flows to line 25a`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "W-2 box 2 federal withholding flows to line 25a" line25abcd-withholding.spec.ts
```

### Purpose
Sibling-line baseline: one W-2 with `federalIncomeTaxWithheldAmount = $10,000` flows to `Payments.withholdingW2` (line 25a). Verifies that W-2 box 2 does NOT bleed into line 25b. Line-25b relevance is negative: the test asserts `withholding1099` is null or 0.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single filer |
| `seedW2WithWithholding` (`w-2`) | `wagesTipsOtherCompAmount` | `80000` |
| same | `socialSecurityWagesAmount` | `80000` |
| same | `medicareWagesAndTipsAmount` | `80000` |
| same | `federalIncomeTaxWithheldAmount` | `10000` |
| same | `employeeSSN` | `111-11-1111` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | `10000` |
| `form1040.payments.withholding1099` | null or 0 |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `10000` |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 line 25a: "Federal income tax withheld from Form(s) W-2. Add the amounts shown as federal income tax withheld on your Form(s) W-2." (W-2 box 2 only.)
- 2025 Instructions for Form 1040 line 25b instruction text (`i1040gi_2025.txt` lines 4367–4375) routes 1099-series withholding to line 25b; nothing on a W-2 belongs on 25b.
- Line 25a = $10,000; line 25b = $0; line 25c = $0; line 25d = $10,000.

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries)` at line 27550 reads `federalIncomeTaxWithheldAmount = $10,000` → returns $10,000.
- Composer at line 26752 sets `withholdingW2 = $10,000`; G2-rounding wraps to $10,000.
- `sumFederalWithholdingFromMultipleLists(form1099REntries, ...)` returns null (no 1099 entries).
- `withholding1099 = null`; `withholdingOther = null`; line 25d total = $10,000.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `payments` truthy | ✅ OK. |
| 2 | `withholdingW2 === 10000` | ✅ OK — line 25a wire (sibling). |
| 3 | `withholding1099 == null \|\| === 0` | ✅ OK — **line 25b correctly stays null** (W-2 does not bleed into 25b). |
| 4 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 5 | `totalWithholding === 10000` | ✅ OK. |

Line-25b relevance: ✅ OK — negative coverage. Verifies the W-2 box 2 → line 25a wire does not accidentally populate line 25b.

---

## Test 2 — `1099-R box 4 withholding flows to line 25b`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "1099-R box 4 withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
Canonical positive flow for line 25b: one 1099-R with `federalIncomeTaxWithheldAmount = $2,500` (box 4) flows to `Payments.withholding1099`. The most common line-25b case — retirement-account withholding.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `seedPensionUploadAttestation` (`pension-annuity-income-taxpayer`) | `hadPensionOrAnnuityIncome`, `hasUploadedAtLeastOnePensionStatement`, `confirmAllReceivedPensionStatementsUploaded`, `received1099RNonIra`, `uploaded1099RNonIra` | all `true` |
| `seed1099RWithWithholding` (`1099-r`) | `recipientTIN` | `111-11-1111` |
| same | `grossDistributionAmount` | `25000` |
| same | `taxableAmountAmount` | `25000` |
| same | `federalIncomeTaxWithheldAmount` | `2500` |
| same | `distributionCodes` | `'7'` (normal distribution) |
| same | `iraSepSimple` | `false` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `2500` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `2500` |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 line 25b (`i1040gi_2025.txt` line 4367): "Form 1099-R. Attach Form 1099-R to your paper return if box 4 shows federal income tax withheld. Include the amount withheld in the total on line 25b of Form 1040 or 1040-SR."
- 1099-R box 4 = $2,500 → line 25b = $2,500.
- Line 25a = $0; line 25c = $0; line 25d = $2,500.

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries)` returns null (no W-2).
- `sumFederalWithholdingFromMultipleLists(form1099REntries, ...)` reads `federalIncomeTaxWithheldAmount = $2,500` from the one 1099-R entry → returns $2,500.
- Composer at line 26772 sets `withholding1099 = $2,500`; G2 rounding wraps to $2,500.
- Line 25d total = $2,500.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK. |
| 2 | `withholding1099 === 2500` | ✅ OK — line 25b canonical positive flow. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 2500` | ✅ OK. |

Line-25b relevance: ✅ OK — canonical positive flow.

---

## Test 3 — `SSA-1099 voluntary box 6 withholding flows to line 25b`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "SSA-1099 voluntary box 6 withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
SSA-1099 voluntary withholding ($1,200) on box 6 flows to line 25b — a different routing path than the generic 1099-series helper because the SSA-1099 mapper uses a non-canonical field name `voluntaryFederalIncomeTaxWithheldAmount` requiring the dedicated `sumSsa1099Withholding` reader.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `seedSsa1099WithWithholding` (`ssa-1099`) | `netBenefitsAmount` | `18000` |
| same | `voluntaryFederalIncomeTaxWithheldAmount` | `1200` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `1200` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `1200` |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 line 25b instruction (Publication 17 / Publication 915): SSA-1099 box 6 voluntary federal income tax withheld flows to Form 1040 line 25b.
- Internal Revenue Code §3402(p)(1)(B) — the voluntary withholding rate the SSA payer applied is one of 7% / 10% / 12% / 22% per the recipient's Form W-4V (Voluntary Withholding Request). The line-25b aggregation step does NOT recompute the rate; it accepts the dollar amount the SSA payer printed.
- Line 25b = $1,200; line 25a = $0; line 25c = $0; line 25d = $1,200.

### Java compute path
- `sumFederalWithholdingFromMultipleLists` returns null (no generic 1099 entries).
- `sumSsa1099Withholding(formSsa1099Entries)` at line 27570 reads `voluntaryFederalIncomeTaxWithheldAmount = $1,200` → returns $1,200.
- Composer at line 26775 adds the SSA contribution: `withholding1099 = $1,200`.
- Line 25d = $1,200.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK. |
| 2 | `withholding1099 === 1200` | ✅ OK — dedicated SSA-1099 reader path. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 1200` | ✅ OK. |

Line-25b relevance: ✅ OK — covers the non-canonical-field-name dedicated-reader branch.

---

## Test 4 — `W-2G box 4 withholding flows to line 25c`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "W-2G box 4 withholding flows to line 25c" line25abcd-withholding.spec.ts
```

### Purpose
Sibling-line negative coverage: W-2G box 4 gambling withholding ($2,880) flows to **line 25c** (`Payments.withholdingOther`), not line 25b. The W-2G form name resembles W-2 but routes differently — its withholding goes to the "Other forms" sub-line.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | Identification + Address | default Single |
| `seedW2GWithWithholding` (`w-2g`) | `grossWinningsAmount` | `12000` |
| same | `federalIncomeTaxWithheldAmount` | `2880` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | null or 0 |
| `form1040.payments.withholdingOther` | `2880` |
| `form1040.payments.totalWithholding` | `2880` |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 line 25c (`i1040gi_2025.txt` line 3983): "Form W-2G. If a payer withholds income tax from your gambling winnings, you should receive a Form W-2G ... Report the tax withheld on Form 1040 or 1040-SR, line 25c."
- W-2G box 4 = $2,880 → line 25c = $2,880. Line 25b = $0.

### Java compute path
- Composer at line 26790 reads `formW2gEntries` via `sumFederalWithholdingFromEntries` → returns $2,880 → `withholdingOther = $2,880`.
- `sumFederalWithholdingFromMultipleLists(...)` returns null → `withholding1099 = null`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK. |
| 2 | `withholding1099 == null \|\| === 0` | ✅ OK — **W-2G does NOT bleed into line 25b** (despite the similar form name). |
| 3 | `withholdingOther === 2880` | ✅ OK. |
| 4 | `totalWithholding === 2880` | ✅ OK. |

Line-25b relevance: ✅ OK — negative coverage (W-2G correctly excluded from 25b).

---

## Test 5 — `Line 25d totals W-2 withholding (25a) + 1099 withholding (25b) + other withholding (25c)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "Line 25d totals W-2 withholding" line25abcd-withholding.spec.ts
```

### Purpose
Aggregate integration test: simultaneously seed one W-2 ($8,000), one 1099-R ($1,500), and one W-2G ($600). Verifies each sub-line is independently populated AND line 25d sums them correctly to $10,100. Locks the composer's three-source orthogonality.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default Single |
| `seedPensionUploadAttestation` | pension attestation suite | true (×5 fields) |
| `seedW2WithWithholding` (`w-2`) | `wagesTipsOtherCompAmount` | `60000` |
| same | `federalIncomeTaxWithheldAmount` | `8000` |
| same | `employeeSSN` | `111-11-1111` |
| `seed1099RWithWithholding` (`1099-r`) | `grossDistributionAmount` | `15000` |
| same | `taxableAmountAmount` | `15000` |
| same | `federalIncomeTaxWithheldAmount` | `1500` |
| `seedW2GWithWithholding` (`w-2g`) | `grossWinningsAmount` | `8000` |
| same | `federalIncomeTaxWithheldAmount` | `600` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | `8000` |
| `form1040.payments.withholding1099` | `1500` |
| `form1040.payments.withholdingOther` | `600` |
| `form1040.payments.totalWithholding` | `10100` |

### Hand computation per Internal Revenue Service rule
- Line 25a = $8,000 (W-2 box 2); line 25b = $1,500 (1099-R box 4); line 25c = $600 (W-2G box 4).
- 2025 Instructions for Form 1040 line 25d ("Add lines 25a through 25c"): $8,000 + $1,500 + $600 = $10,100.

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries) = $8,000`.
- `sumFederalWithholdingFromMultipleLists(form1099REntries, ...) = $1,500`.
- `sumFederalWithholdingFromEntries(formW2gEntries) = $600`.
- Composer at the line 25d block: `totalWithholding = roundMoney($8,000 + $1,500 + $600) = $10,100`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 === 8000` | ✅ OK. |
| 2 | `withholding1099 === 1500` | ✅ OK. |
| 3 | `withholdingOther === 600` | ✅ OK. |
| 4 | `totalWithholding === 10100` | ✅ OK — line 25d exact sum. |

Line-25b relevance: ✅ OK — verifies 25b contribution to 25d aggregate.

---

## Test 6 — `1099-DA box 2a federal withholding flows to line 25b (G1 fix)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "1099-DA box 2a federal withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
Locks the G1 closure (knowledge file dated 2026-04-19) AND the 2026-06-10 mapper-output alias for the new-for-2025 Form 1099-DA (Digital Asset proceeds). The 1099-DA box 2a — federal income tax withheld — is the IRS's chosen box position for the digital-assets form (different from box 4 used by the rest of the 1099 series). Both a knowledge-file fix (parameter to composer) and a mapper-alias fix (canonical `federalIncomeTaxWithheldAmount` exposure) were needed.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default Single |
| `seed1099DaWithWithholding` (`1099-da`) | `copyA_box4_federal_income_tax_withheld` | `'1500'` (string-typed; pickString prefix convention) |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `1500` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `1500` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 1099-DA (Proceeds from Broker Transactions — Digital Assets) box 2a is "Federal income tax withheld."
- 2025 Instructions for Form 1040 line 25b general 1099-series rule: every Form 1099 box-4-or-equivalent federal income tax withheld → line 25b.
- Box 2a = $1,500 → line 25b = $1,500.

### Java compute path
- `Form1099DaMapper.loadOne(...)` reads `copyA_box4_federal_income_tax_withheld = "1500"` via `pickString` (the 1099-DA's prefix convention), persists to `box4FederalIncomeTaxWithheld`, and (per the 2026-06-10 alias) exposes `federalIncomeTaxWithheldAmount = $1,500`.
- `sumFederalWithholdingFromMultipleLists(..., form1099DaEntries)` (per the G1 fix) reads the canonical alias → returns $1,500.
- `withholding1099 = $1,500`; line 25d = $1,500.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK. |
| 2 | `withholding1099 === 1500` | ✅ OK — G1 closure + 2026-06-10 mapper alias verified. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 1500` | ✅ OK. |

Line-25b relevance: ✅ OK — positive flow for new-for-2025 1099-DA.

---

## Test 7 — `Form 8959 Part V line 24 Medicare withholding flows to line 25c (G2)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "Form 8959 Part V line 24 Medicare withholding flows to line 25c" line25abcd-withholding.spec.ts
```

### Purpose
Sibling-line cross-form test: a Single filer with $250,000 Medicare wages and W-2 box 6 of $4,075 triggers Form 8959 Part V. The "excess Medicare withholding" beyond 1.45% × $250,000 = $3,625 (i.e., $450) is the employer's Additional Medicare withholding. The IRS routes this excess to Form 1040 **line 25c**, not 25b. Verifies that W-2 box 6 (Medicare tax withheld) does NOT bleed into line 25b — which would be the wrong sub-line for a W-2-derived value.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default |
| `setFilingStatusApi` | Filing status | `Single` |
| `seedW2ForMedicareWithholding` (`w-2`) | `medicareWagesAndTipsAmount` | `250000` |
| same | `medicareTaxWithheldAmount` | `4075` |
| same | `employeeSSN` | `111-11-1111` |
| (omitted) | `federalIncomeTaxWithheldAmount` | not seeded → null |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | null or 0 |
| `form1040.payments.withholdingOther` | `450` |
| `form1040.payments.totalWithholding` | `450` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 8959 Part V (withholding reconciliation):
  - Line 19 = W-2 box 6 = $4,075.
  - Line 20 = Medicare wages = $250,000.
  - Line 21 = $250,000 × 1.45% = $3,625.
  - Line 22 = max(0, $4,075 − $3,625) = $450.
  - Line 24 = $450 → Form 1040 line 25c.
- Line 25b = $0 (no 1099-series entries on the return).

### Java compute path
- `buildForm8959(...)` sets `form8959.line24TotalAmtWithheld = $450`.
- Composer at line ~26790-26796 adds `form8959.getLine24TotalAmtWithheld()` to `withholdingOther`.
- `withholding1099 = null` (no 1099 entries).

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK. |
| 2 | `withholding1099 == null \|\| === 0` | ✅ OK — **W-2 box 6 / Form 8959 Part V correctly does NOT bleed into line 25b**. |
| 3 | `withholdingOther === 450` | ✅ OK. |
| 4 | `totalWithholding === 450` | ✅ OK. |

Line-25b relevance: ✅ OK — negative coverage (Form 8959 line 24 correctly excluded from 25b).

---

## Test 8 — `1099-NEC backup withholding flows to line 25b (G3)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "1099-NEC backup withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
1099-NEC box 4 backup withholding ($650) flows to line 25b. Verifies the `form1099NecEntries` path through `sumFederalWithholdingFromMultipleLists`. The 1099-NEC box 4 fills only when the payee failed to furnish a Taxpayer Identification Number, triggering Internal Revenue Code §3406 backup withholding at 24%.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default Single |
| `seed1099NecWithWithholding` (`1099-nec`) | `nonemployeeCompensationAmount` | `10000` |
| same | `federalIncomeTaxWithheldAmount` | `650` |
| same | `recipientTIN` | `111-11-1111` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `650` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `650` |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 line 25b (`i1040gi_2025.txt` lines 4369–4375): "If you were subject to backup withholding on income you received during 2025, include the amount withheld, as shown on your Form 1099, in the total on line 25b of Form 1040 or 1040-SR."
- Internal Revenue Code §3406(a)(1) — 24% backup withholding on reportable payments when the payee failed to furnish a Taxpayer Identification Number.
- 1099-NEC box 4 = $650 → line 25b = $650.

### Java compute path
- `sumFederalWithholdingFromMultipleLists(..., form1099NecEntries, ...)` reads `federalIncomeTaxWithheldAmount = $650` → returns $650.
- `withholding1099 = $650`; line 25d = $650.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK. |
| 2 | `withholding1099 === 650` | ✅ OK — backup withholding correctly routed to 25b. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 650` | ✅ OK. |

Line-25b relevance: ✅ OK — positive flow for backup withholding via 1099-NEC.

---

## Test 9 — `MFJ: both spouses W-2 withholding combined on line 25a`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "MFJ: both spouses W-2 withholding combined" line25abcd-withholding.spec.ts
```

### Purpose
Sibling-line: on Married Filing Jointly, both spouses' W-2 box 2 amounts combine on line 25a. Locks the no-SSN-filter rule for W-2 withholding (the same rule applies symmetrically to line 25b for 1099 entries — implicitly verified through `sumFederalWithholdingFromEntries`).

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default taxpayer |
| `setFilingStatusApi` | Filing status | `Married filing jointly` (with `reloadAfterSave: true`) |
| `seedSpouseApi` | spouse | `Jane`, `Doe`, `222-22-2222`, `1980-05-15` |
| `seedW2WithWithholding` (taxpayer `w-2`) | `wagesTipsOtherCompAmount`, `federalIncomeTaxWithheldAmount`, `employeeSSN` | `55000`, `7000`, `111-11-1111` |
| `seedW2WithWithholding` (spouse `w-2`) | `wagesTipsOtherCompAmount`, `federalIncomeTaxWithheldAmount`, `employeeSSN` | `40000`, `5000`, `222-22-2222` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | `12000` |
| `form1040.payments.totalWithholding` | `12000` |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 line 25a MFJ rule: combine both spouses' W-2 box 2 amounts.
- $7,000 + $5,000 = $12,000.
- Line 25b = $0 (no 1099 entries).

### Java compute path
- `sumFederalWithholdingFromEntries(w2Entries)` iterates both W-2 entries regardless of `employeeSSN` → $12,000.
- `withholding1099 = null`.
- Line 25d = $12,000.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 === 12000` | ✅ OK. |
| 2 | `totalWithholding === 12000` | ✅ OK. |

Line-25b relevance: ✅ OK by transitivity — the no-SSN-filter rule that aggregates W-2 box 2 on 25a applies symmetrically to 1099 box 4 on 25b via the same helper.

---

## Test 10 — `Two W-2s with same employer EIN fires DUPLICATE_W2_EMPLOYER_EIN non-blocking advisory (G4)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "DUPLICATE_W2_EMPLOYER_EIN non-blocking advisory" line25abcd-withholding.spec.ts
```

### Purpose
Sibling: tests the G4 closure from 25a.md — non-blocking advisory when two W-2 entries share an employer EIN. The flag is informational because both interpretations are legitimate (W-2c replacement vs same-employer interim+annual W-2). Line 25a sums both W-2s; the advisory just surfaces the disambiguation question.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default Single |
| `seedW2WithWithholding` (W-2 #1) | wages, withheld, employeeSSN, **employerEIN** | `30000`, `4000`, `111-11-1111`, `'12-3456789'` |
| `seedW2WithWithholding` (W-2 #2) | wages, withheld, employeeSSN, **employerEIN** | `20000`, `2500`, `111-11-1111`, `'12-3456789'` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `result.flags[].code === 'DUPLICATE_W2_EMPLOYER_EIN'` | present |
| flag.blocking | `false` |
| `payments.withholdingW2` | `6500` |
| `payments.totalWithholding` | `6500` |

### Hand computation per Internal Revenue Service rule
- 2025 Form W-2c instructions: a corrected W-2c REPLACES the original; line 25a includes only the corrected amount, not both.
- The advisory is the project's defensive surface for the most common data-entry mistake. Sum-of-both is the correct line-25a value when both entries are legitimately separate.
- Line 25a = $4,000 + $2,500 = $6,500.

### Java compute path
- `validateDuplicateW2EmployerEin(...)` normalizes both EINs to "123456789", counts 2 → emits non-blocking advisory.
- `sumFederalWithholdingFromEntries(w2Entries) = $6,500`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | advisory present | ✅ OK. |
| 2 | `blocking === false` | ✅ OK. |
| 3 | `withholdingW2 === 6500` | ✅ OK. |
| 4 | `totalWithholding === 6500` | ✅ OK. |

Line-25b relevance: ⚪ NEUTRAL — sibling test (line 25b not exercised; would be null by construction).

---

## Test 11 — `Two W-2s with distinct employer EINs do NOT fire DUPLICATE_W2_EMPLOYER_EIN (G4 negative)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "distinct employer EINs do NOT fire" line25abcd-withholding.spec.ts
```

### Purpose
Negative half of G4: two distinct EINs do NOT fire the advisory. Locks the EIN-normalization-and-grouping logic.

### Inputs the test seeds
Same as Test 10 except `employerEIN` differs (`'12-3456789'` vs `'98-7654321'`).

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| flags does NOT include `DUPLICATE_W2_EMPLOYER_EIN` | true |
| `payments.withholdingW2` | `6500` |

### Hand computation per Internal Revenue Service rule
- Same as Test 10 but with distinct employers — sum-of-both is correct; no advisory needed.

### Java compute path
- `validateDuplicateW2EmployerEin(...)` normalizes to two distinct EINs → no advisory emitted.
- Line 25a = $6,500.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | advisory NOT present | ✅ OK. |
| 2 | `withholdingW2 === 6500` | ✅ OK. |

Line-25b relevance: ⚪ NEUTRAL — sibling test.

---

## Test 12 — `Form 4852 entry with both line-7 and line-8 inputs without substituteFormType blocks with FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER (G5)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER" line25abcd-withholding.spec.ts
```

### Purpose
**Cross-cutting (25a AND 25b protection):** when a Form 4852 row has both line-7 (W-2-substitute) AND line-8 (1099-R-substitute) inputs without an explicit `substituteFormType`, the inference fallback would silently route the whole entry to either line 25a or 25b (depending on which field set is detected first). This blocker stops the compute and forces the user to disambiguate. Critical for line-25b correctness: without this blocker, a malformed Form 4852 could silently dump W-2 substitute withholding into line 25b OR 1099-R substitute withholding into line 25a, in either case corrupting the affected sub-line.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default Single |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | **omitted** |
| same | `line7aWagesTipsOtherCompensation` | `5000` |
| same | `line7eFederalIncomeTaxWithheld` | `500` |
| same | `line8aGrossDistribution` | `3000` |
| same | `line8bTaxableAmount` | `3000` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `response.ok` | `false` |
| `response.status` | `409` |
| flags includes `FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER` | present |
| `blocker.blocking` | `true` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 4852 instructions, line 4: the form REQUIRES the taxpayer to check exactly one of "Form W-2" / "Form 1099-R" — a row with both kinds of inputs and neither box checked is malformed and cannot be filed.
- 2025 Instructions for Form 1040: line 25a vs line 25b routing depends on which substitute type the Form 4852 represents.

### Java compute path
- `validateForm4852SubstituteTypeExplicit(...)` at line 4644 iterates entries. For row 1: no `substituteFormType`, hasLine7=true, hasLine8=true → ambiguous → emits blocker.
- `TaxReturnResource.compute(...)` returns 409 with the flag in the response body.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `ok === false` | ✅ OK. |
| 2 | `status === 409` | ✅ OK. |
| 3 | blocker present | ✅ OK. |
| 4 | `blocker.blocking === true` | ✅ OK. |

Line-25b relevance: ✅ OK — critical defensive routing safeguard. Without this blocker, a malformed Form 4852 could silently misroute withholding into 25b.

---

## Test 13 — `Form 4852 entry with explicit substituteFormType does NOT fire FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER (G5 negative)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "explicit substituteFormType does NOT fire" line25abcd-withholding.spec.ts
```

### Purpose
Negative half of G5: with `substituteFormType: 'w2'` explicitly set, the routing is unambiguous; compute succeeds and the line-7e $500 flows to line 25a.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default Single |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | `'w2'` |
| same | `line7aWagesTipsOtherCompensation` | `5000` |
| same | `line7eFederalIncomeTaxWithheld` | `500` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `result` | truthy |
| flags does NOT include `FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE_TAXPAYER` | true |
| `withholdingW2` | `500` |

### Hand computation per Internal Revenue Service rule
- `substituteFormType = "w2"` → Form 4852 W-2 substitute path → line 7e $500 flows to line 25a.
- Line 25b = $0 (no 1099-r substitute on this row).

### Java compute path
- `validateForm4852SubstituteTypeExplicit(...)` skips the entry because `substituteFormType` is set.
- `extractForm4852Line25aWithholding(...)` returns $500.
- `withholdingW2 = $500`; `withholding1099 = null`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `result` truthy | ✅ OK. |
| 2 | blocker NOT present | ✅ OK. |
| 3 | `withholdingW2 === 500` | ✅ OK. |

Line-25b relevance: ✅ OK by construction — line 25b stays null because the substitute is W-2-typed.

---

## Test 14 — `1099-K box 4 backup withholding flows to line 25b (25b Gap A)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "1099-K box 4 backup withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
**§9b Gap A closure.** 1099-K box 4 backup withholding ($900) flows to line 25b after the 2026-06-10 mapper-output alias closure. Before the fix, the 1099-K mapper exposed the value only under the non-canonical key `federalIncomeTaxWithheld4Amount`; the generic line-25b summer reads the canonical `federalIncomeTaxWithheldAmount` and so silently returned 0.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default Single |
| `seed1099KWithWithholding` (`1099-k`) | `grossAmountTransactions1aAmount` | `15000` |
| same | `federalIncomeTaxWithheld4Amount` | `900` (mapper INPUT key — non-canonical) |
| same | `recipientTIN` | `111-11-1111` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `900` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `900` |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 line 25b general rule + Internal Revenue Code §3406: 1099-K box 4 backup withholding (24% of reportable payments when payee failed to furnish a Taxpayer Identification Number) flows to line 25b.
- Box 4 = $900 → line 25b = $900.

### Java compute path
- `Form1099KMapper.loadOne(...)`: reads `federalIncomeTaxWithheld4Amount = $900`; emits both `federalIncomeTaxWithheld4Amount` AND (post-§9b alias) `federalIncomeTaxWithheldAmount = $900`.
- `sumFederalWithholdingFromMultipleLists(..., form1099KEntries, ...)` reads the canonical alias → returns $900.
- `withholding1099 = $900`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK. |
| 2 | `withholding1099 === 900` | ✅ OK — §9b Gap A closure verified end-to-end. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 900` | ✅ OK. |

Line-25b relevance: ✅ OK — bug-level fix verified.

---

## Test 15 — `1099-PATR box 4 backup withholding flows to line 25b (25b Gap B)`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "1099-PATR box 4 backup withholding flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
**§9c Gap B closure.** 1099-PATR (Taxable Distributions Received From Cooperatives) box 4 backup withholding ($720) flows to line 25b. Before the 2026-06-10 closure, `form1099PtrEntries` was never loaded in `prepare()` and never passed into the composer — the mapper + StatementFormCatalog + UserDataBulkDelete entries existed but the compute wiring was missing entirely.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default Single |
| `seed1099PtrWithWithholding` (`1099-ptr`) | `patronageDividendsAmount` | `5000` |
| same | `federalIncomeTaxWithheldAmount` | `720` (canonical key — 1099-PATR uses it natively) |
| same | `recipientTIN` | `111-11-1111` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `720` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `720` |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 line 25b general rule + Internal Revenue Code §3406. 1099-PATR box 4 backup withholding → line 25b.
- Box 4 = $720 → line 25b = $720.

### Java compute path
- `prepare()` at line 288 loads `form1099PtrEntries` via `listEntriesWithData(uid, "1099-ptr")` (§9c addition).
- `Form1099PatrMapper.loadOne(...)` reads + emits `federalIncomeTaxWithheldAmount = $720`.
- Composer at line 26772 includes `form1099PtrEntries` in the variadic call (§9c addition).
- `withholding1099 = $720`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK. |
| 2 | `withholding1099 === 720` | ✅ OK — §9c Gap B closure verified end-to-end. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 720` | ✅ OK. |

Line-25b relevance: ✅ OK — bug-level fix verified.

---

## Test 16 — `Form 4852 (taxpayer, 1099-R substitute) line 8f flows to line 25b`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "Form 4852 \(taxpayer, 1099-R substitute\) line 8f flows to line 25b" line25abcd-withholding.spec.ts
```

### Purpose
**§10b Gap H closure.** Taxpayer Form 4852 with `substituteFormType: '1099-r'` and `line8fFederalIncomeTaxWithheld: 2500` flows to line 25b — the canonical positive flow for the Form-4852-substitute-of-a-missing-1099-R path. Until the §10 second pass, only Java unit `form4852NegativeLine8fIsFilteredOutOfLine25b` covered the negative branch.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default Single |
| `savePersonalFormApi` (`form4852-taxpayer`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | `'1099-r'` |
| same | `line8aGrossDistribution` | `20000` |
| same | `line8bTaxableAmount` | `20000` |
| same | `line8fFederalIncomeTaxWithheld` | `2500` |
| same | `line9HowAmountsWereDetermined` | `'Used final statement.'` |
| same | `line10EffortsToObtainMissingOrCorrectedForm` | `'Requested replacement 1099-R.'` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholding1099` | `2500` |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `2500` |

### Hand computation per Internal Revenue Service rule
- 2025 Form 4852 instructions: line 8f federal income tax withheld is the substitute for box 4 of the missing 1099-R. Flows to Form 1040 line 25b.
- Line 8f = $2,500 → line 25b = $2,500.
- Line 25a = $0 (substituteFormType is 1099-R, not W-2 — no line 7e contribution).

### Java compute path
- `extractForm4852Line25bWithholding(form4852TaxpayerData)`: substituteFormType "1099-r" → keep entry; G6-style positive-filter passes $2,500 → returns $2,500.
- `extractForm4852Line25aWithholding(form4852TaxpayerData)`: substituteFormType "1099-r" → skip entry → returns null.
- Composer: `withholding1099 = null + $2,500 = $2,500`; `withholdingW2 = null`.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholdingW2 == null \|\| === 0` | ✅ OK — Form 4852 1099-R-typed substitute correctly excluded from line 25a. |
| 2 | `withholding1099 === 2500` | ✅ OK — §10b Gap H closure verified end-to-end. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 2500` | ✅ OK. |

Line-25b relevance: ✅ OK — Form 4852 line 8f positive flow.

---

## Test 17 — `Form 4852 (spouse, 1099-R substitute) line 8f on MFS does NOT contribute to taxpayer line 25b`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "Form 4852 \(spouse, 1099-R substitute\) line 8f on MFS does NOT contribute" line25abcd-withholding.spec.ts
```

### Purpose
**§10c Gap F closure.** On Married Filing Separately, the spouse's Form 4852 1099-R-substitute line 8f ($1,800) belongs on the spouse's own separate Form 1040 line 25b, NOT on the taxpayer's return. Locks the symmetric line-25b MFS guard at `TaxReturnComputeService.java:26781-26782`.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default taxpayer |
| `seedSpouseApi` | spouse | `Pat`, `Tester`, `222-22-2222`, `1985-05-15` |
| `setFilingStatusApi` | Filing status | `Married filing separately` |
| `savePersonalFormApi` (`form4852-spouse`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | `'1099-r'` |
| same | `line8aGrossDistribution` | `15000` |
| same | `line8bTaxableAmount` | `15000` |
| same | `line8fFederalIncomeTaxWithheld` | `1800` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholding1099` | null or 0 |
| `form1040.payments.totalWithholding` | null or 0 |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 Married Filing Separately rules: each spouse files their own Form 1040 with only their own information returns.
- 2025 Form 4852 instructions: a Form 4852 the spouse prepared belongs on the spouse's own return.
- Taxpayer's line 25b = $0 (the spouse's $1,800 is excluded).

### Java compute path
- `extractForm4852Line25bWithholding(form4852TaxpayerData)` returns null (no taxpayer Form 4852).
- Composer ternary `isMfsForForm4852 ? null : extractForm4852Line25bWithholding(form4852SpouseData)`: `isMfsForForm4852 = true` → result = null.
- `withholding1099 = null + null + null = null`; G2-rounding preserves null.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholding1099 == null \|\| === 0` | ✅ OK — symmetric MFS guard on line 25b verified. |
| 2 | `totalWithholding == null \|\| === 0` | ✅ OK. |

Line-25b relevance: ✅ OK — MFS guard verified end-to-end.

---

## Test 18 — `Form 4852 (spouse, 1099-R substitute) line 8f on MFJ contributes to line 25b alongside taxpayer 1099-R`

Command (run individually):
```powershell
cd C:\us-tax\us-tax-be\e2e
npx playwright test --workers=1 --output=test-results-25b -g "Form 4852 \(spouse, 1099-R substitute\) line 8f on MFJ contributes" line25abcd-withholding.spec.ts
```

### Purpose
**§10d Gap G closure.** On Married Filing Jointly, both spouses' line-25b contributions combine: taxpayer's real 1099-R box 4 ($3,000) + spouse's Form 4852 line 8f ($1,200) = $4,200. Locks the MFJ else-branch of the composer's MFS guard ternary.

### Inputs the test seeds
| Where | Field | Value |
|---|---|---|
| `seedRequiredDob` | default taxpayer |
| `seedPensionUploadAttestation` | pension attestation suite | true (×5 fields) |
| `seedSpouseApi` | spouse | `Jane`, `Doe`, `222-22-2222`, `1980-05-15` |
| `setFilingStatusApi` | Filing status | `Married filing jointly` (with `reloadAfterSave: true`) |
| `seed1099RWithWithholding` (`1099-r`, taxpayer side) | `grossDistributionAmount` | `25000` |
| same | `federalIncomeTaxWithheldAmount` | `3000` |
| `savePersonalFormApi` (`form4852-spouse`) | `needsForm4852` | `true` |
| same → `forms[0]` | `substituteFormType` | `'1099-r'` |
| same | `line8aGrossDistribution` | `10000` |
| same | `line8bTaxableAmount` | `10000` |
| same | `line8fFederalIncomeTaxWithheld` | `1200` |

### Outputs the test asserts
| Path | Asserted value |
|---|---|
| `form1040.payments.withholding1099` | `4200` |
| `form1040.payments.withholdingW2` | null or 0 |
| `form1040.payments.withholdingOther` | null or 0 |
| `form1040.payments.totalWithholding` | `4200` |

### Hand computation per Internal Revenue Service rule
- 2025 Instructions for Form 1040 line 25b Married Filing Jointly rule: both spouses' Form 1099 (and Form 4852 substitute) amounts combine onto the same line 25b.
- 1099-R box 4 ($3,000) + Form 4852 line 8f spouse ($1,200) = $4,200.

### Java compute path
- `sumFederalWithholdingFromMultipleLists(form1099REntries, ...) = $3,000`.
- `extractForm4852Line25bWithholding(form4852TaxpayerData) = null`.
- Composer ternary: `isMfsForForm4852 = false` (MFJ) → else-branch → `extractForm4852Line25bWithholding(form4852SpouseData) = $1,200`.
- `withholding1099 = $3,000 + null + $1,200 = $4,200`; G2-rounding wraps to $4,200.

### Per-assertion verdict
| # | Assertion | Verdict |
|---|---|---|
| 1 | `withholding1099 === 4200` | ✅ OK — §10d Gap G closure verified end-to-end. |
| 2 | `withholdingW2 == null \|\| === 0` | ✅ OK. |
| 3 | `withholdingOther == null \|\| === 0` | ✅ OK. |
| 4 | `totalWithholding === 4200` | ✅ OK. |

Line-25b relevance: ✅ OK — MFJ cross-spouse aggregation verified end-to-end.

---

## §10 Per-spec summary

| Spec file | Tests | Status | Notes |
|---|---|---|---|
| `line25abcd-withholding.spec.ts` | 18 | ✅ all pass | The complete line 25a/25b/25c/25d wire. Tests 2, 3, 6, 8, 14, 15, 16, 18 directly exercise line 25b positive flow (8 of 18). Tests 1, 4, 7, 9 verify sibling lines do not bleed into 25b (4 of 18 negative coverage). Tests 5 verifies the 25b → 25d sum. Tests 10–11 (DUPLICATE_W2_EMPLOYER_EIN) are 25a-only sibling tests. Tests 12–13 (FORM_4852_AMBIGUOUS_SUBSTITUTE_TYPE) protect routing between 25a and 25b. Test 17 verifies the MFS guard on the 25b path. |

**Totals: 18 tests; 18 OK assertion verdicts across 18 passing tests; 0 SKIPPED; 0 MISMATCHES; 0 GAPs.**

---

## §11 Outstanding gaps in test_cases/25b.md

**None.** After the §9 first-pass closure (Gaps A, B, C, D in `XLS/Computations/25b.md`) and the §10 second-pass closure (Gaps F, G, H), the entire enumerable line-25b coverage matrix is closed except for the one policy-blocked item:

- 🚫 **Gap E (Schedule K-1 / Form 1042-S / Form 8805 / Form 8288-A)** — these forms route to **line 25c**, not line 25b, per the 2025 Instructions for Form 1040. They are deferred (out of scope) per `lines\25abcd.md §10`. Not a 25b gap.

### Composer-path coverage matrix (all branches exercised)

| Composer branch (location) | Test covering it |
|---|---|
| `sumFederalWithholdingFromMultipleLists(form1099REntries, ...)` (1099-series variadic) | Test 2 (1099-R), Test 6 (1099-DA), Test 8 (1099-NEC), Test 14 (1099-K), Test 15 (1099-PATR), Test 18 (1099-R MFJ) |
| `sumSsa1099Withholding(formSsa1099Entries)` (SSA-1099 dedicated reader) | Test 3 |
| `extractForm4852Line25bWithholding(form4852TaxpayerData)` (taxpayer-side, unconditional add) | Test 16 |
| `isMfsForForm4852 ? null : extractForm4852Line25bWithholding(form4852SpouseData)` (MFS branch — returns null) | Test 17 |
| Same ternary (MFJ else-branch — returns spouse contribution) | Test 18 |
| `roundMoney(withholding1099)` (G2 closure) | Implicitly by Tests 2, 14, 15, 16, 18 |

### Defensive-testing observation (not classified as a gap)

8 of 13 entry-lists in the variadic call have no direct end-to-end test seeding them in this spec file (1099-INT box 4, 1099-DIV box 4, 1099-B box 4, 1099-OID box 4, 1099-G box 4, 1099-MISC box 4, RRB-1099 box 10, RRB-1099-R box 9). The corresponding mappers all use the canonical `federalIncomeTaxWithheldAmount` output key (verified during the §9 first-pass mapper grep), and the helper `sumFederalWithholdingFromEntries` reads the canonical key — so the structural integrity holds today. A future regression breaking any of those 8 mapper outputs (e.g., a future automated extractor that writes a box-number-embedded field name like 1099-K originally did) would not be caught by the existing tests. This is the same regression pattern that produced §9b Gap A (1099-K) and the earlier 1099-DA G1 — it requires a future code change to manifest, so it is a defensive-testing extension consideration rather than an outstanding gap in the IRS-correctness sense the catalog tracks.

If the defensive extension is desired later, the fix is mechanical: add 8 one-shot end-to-end tests mirroring Tests 14 / 15 (positive sum + null on other sub-lines). Skipped here pending user direction.

---

## §12 Bottom line for line 25b e2e coverage

The 18 passing tests across the single spec file give comprehensive coverage of:

- **Every primary positive-flow path**: 1099-R, 1099-DA, 1099-NEC, 1099-K, 1099-PATR (all five via 1099-series variadic helper); SSA-1099 (via dedicated reader); Form 4852 line 8f (taxpayer and spouse on MFJ).
- **Every defensive negative path**: W-2 → 25a (Test 1), W-2G → 25c (Test 4), Form 8959 Part V → 25c (Test 7) — all three sibling-line tests verify their input does NOT bleed into 25b.
- **Filing-status edge cases**: Married Filing Jointly cross-spouse aggregation (Tests 9, 18); Married Filing Separately spouse-side guard on line 25b (Test 17).
- **Defensive validators**: Form 4852 ambiguous-type blocker (Test 12) protects the line-25a / line-25b routing decision; its negative half (Test 13) confirms explicit types are accepted.
- **Routing aggregate**: Line 25d = 25a + 25b + 25c (Test 5).

**Verdict:** Line 25b is solidly covered by the end-to-end suite for every IRS-rule-enumerated path. The §9 + §10 closures of `XLS/Computations/25b.md` (Gaps A, B, C, D, F, G, H) all have matching end-to-end verification in this spec file. Only Gap E remains, and it is policy-blocked (routes to line 25c, not 25b). No MISMATCHES against the IRS rule were identified during this audit.
