# Test catalog — `line8863-education-credits.spec.ts` (Form 1040 line 29 American Opportunity Credit + Schedule 3 line 3 Education Credits)

> Spec file: `C:\us-tax\us-tax-be\e2e\tests\line8863-education-credits.spec.ts` (14 tests, 472 lines).
> Audited 2026-06-11 against IRS sources: `docs/IRS-Forms/f8863.pdf` (2025 Form 8863 verbatim), `docs/books/i1040gi_2025.pdf` (2025 Instructions for Form 1040), `docs/books/p17.pdf` (Publication 17 — Your Federal Income Tax, chapter 33 — Education Credits), Publication 970 (Tax Benefits for Education), J.K. Lasser's Your Income Tax 2025 chapter 33 (sections 33.7–33.9), Internal Revenue Code section 25A (Hope and Lifetime Learning Credits), Internal Revenue Code section 25A(d)(2) (phaseout thresholds), Internal Revenue Code section 25A(g)(1) (taxpayer and student TIN-by-due-date rule), Internal Revenue Code section 25A(g)(3) (dependent-of-another disqualifier), Internal Revenue Code section 25A(g)(6) (Married Filing Separately disqualifier), Internal Revenue Code section 25A(g)(7) (non-resident alien disqualifier), Internal Revenue Code section 25A(i)(1) (under-age-24 refundable-share restriction). Code paths: `TaxReturnComputeService.computeForm8863` (line 26127), `applyForm8863ToSchedule3` (helper called at line 1750), `prepare()` orchestrator (lines 1749-1772). Spec docs: `XLS/Computations/29.md`, `lines/29.md`, `dependencies/29.md`, `knowledge/line-29-aotc-form-8863.md`.
> Tax year: **2025**.

## How to run

```powershell
cd C:\us-tax\us-tax-be\e2e

# All 14 line 29 / Form 8863 tests
npx playwright test --workers=1 --output=test-results-line29 line8863-education-credits.spec.ts

# Single-test runs (use the -g flag with the title substring)
npx playwright test --workers=1 --output=test-results-line29-1 -g "single AOTC student \$4k expenses" line8863-education-credits.spec.ts
```

Per `e2e/CLAUDE.md`: `--workers=1` always; never run in the Claude Code sandbox (EPERM); fresh `--output` directory each run.

---

## 2025 Form 8863 reference data

All constants below from Internal Revenue Code section 25A (statutory; not adjusted for inflation). Centralized in `ReferenceData.java` lines 606-617 after the XLS/Computations/29.md G-NEW-2 closure (2026-06-11).

| Constant | 2025 value | Source |
|---|---:|---|
| American Opportunity Credit — Married Filing Jointly upper phaseout threshold | $180,000 | IRC §25A(d)(2)(A)(ii) |
| American Opportunity Credit — Single/HoH/QSS/MFS upper phaseout threshold | $90,000 | IRC §25A(d)(2)(A)(i) |
| American Opportunity Credit — Married Filing Jointly phaseout range width | $20,000 | IRC §25A(d)(2)(A) |
| American Opportunity Credit — Single/HoH/QSS/MFS phaseout range width | $10,000 | IRC §25A(d)(2)(A) |
| Per-student first bracket (100 percent) — top of bracket | $2,000 | IRC §25A(b)(1) |
| Per-student second bracket (25 percent) — top of bracket | $4,000 | IRC §25A(b)(1) |
| Maximum American Opportunity Credit per student | $2,500 | IRC §25A(b)(1) |
| Refundable share of American Opportunity Credit | 40 percent | IRC §25A(i)(1) |
| Lifetime Learning Credit — total expense cap (per return) | $10,000 | IRC §25A(c)(1) |
| Lifetime Learning Credit rate | 20 percent | IRC §25A(c)(1) |
| Phaseout fraction rounding rule | at least 3 decimal places, half-up | 2025 Form 8863 lines 6 and 18 |
| 2025 Single standard deduction | $15,750 | Rev. Proc. 2024-40 |
| 2025 Married Filing Jointly standard deduction | $31,500 | Rev. Proc. 2024-40 |
| 2025 Single tax brackets | 10% to $11,925 / 12% to $48,475 / 22% to $103,350 / 24% to $197,300 | Rev. Proc. 2024-40 |
| 2025 MFJ tax brackets | 10% to $23,850 / 12% to $96,950 / 22% to $206,700 / 24% to $394,600 | Rev. Proc. 2024-40 |

---

## Summary

| # | Line | Test | Net result |
|---:|---:|---|---|
| 1 | 50 | Education-credits taxpayer UI workflow round-trip | ✅ OK on 5 assertions |
| 2 | 100 | Education-credits spouse UI workflow round-trip | ✅ OK on 4 assertions |
| 3 | 148 | Single AOTC $4k expenses, MAGI $70k → line 29 = $1,000 | ✅ OK on 8 assertions |
| 4 | 171 | LLC student only, MAGI $70k → line 29 = $0, line 19 = $1,000 | ✅ OK on 4 assertions |
| 5 | 197 | MAGI $95k Single > $90k ceiling → both credit lines null | ✅ OK on 4 assertions |
| 6 | 212 | Married Filing Separately → form8863 null + blocking flag | ✅ OK on 3 assertions |
| 7 | 231 | Under-24 refundable restriction (override boolean) → line 29 = $0; line 19 > $0 | ✅ OK on 4 assertions; ⚠️ 2 loose assertions (GAP A) |
| 8 | 249 | MFJ taxpayer AOTC + spouse LLC merged → studentCount = 2; line 29 = $1,000 | ✅ OK on 4 assertions; ⚠️ 1 loose assertion (GAP A) |
| 9 | 298 | Form 8862 gate: aotcPreviouslyDenied + no Form 8862 → AOTC blocked, LLC credited | ✅ OK on 4 assertions; ⚠️ 2 loose assertions (GAP A) |
| 10 | 338 | Two AOTC students each $4k → line 29 = $2,000 aggregate | ✅ OK on 4 assertions |
| 11 | 378 | LLC student $10k expenses, MAGI $85k Single → 50% phaseout → line 19 = $1,000 | ✅ OK on 4 assertions |
| 12 | 413 | AOTC student disqualified by 4-year limit → form8863 present but no credit | ✅ OK on 3 assertions |
| 13 | 442 | MAGI exactly $80k Single → full credit (phaseoutFraction 1.000) | ✅ OK on 4 assertions |
| 14 | 459 | MAGI exactly $90k Single → fully phased out (phaseoutFraction 0) | ✅ OK on 4 assertions |

**0 MISMATCHes against the IRS rule.** **0 per-assertion correctness GAPs.** **3 tests have loose-assertion GAPs (GAP A)** where `> 0` or `> 1500` is used instead of an exact dollar amount that hand-calculation can pin. **13 coverage GAPs (B–N)** — paths not exercised by ANY test in this spec — listed at the end.

---

## Test 1 (line 50) — `education credits taxpayer workflow saves return-level data and a student entry`

```powershell
npx playwright test --workers=1 --output=test-results-line29-1 `
  -g "education credits taxpayer workflow" line8863-education-credits.spec.ts
```

**Purpose.** End-to-end persistence round-trip: fills the `education-credits-taxpayer` form through the user interface (return-level fields, statement-upload screening, the `aotcPreviouslyDenied` and `hasMagiAddBacks` answers, plus one Add-student row with name, Taxpayer Identification Number, institution data, all four Part III disqualifier answers) and then reads the saved record via the REST endpoint to confirm it persisted. Verifies the Angular form ↔ Quarkus REST ↔ Firestore round-trip, not the compute.

**Inputs (seeded via UI).**
- `seedRequiredDob` — taxpayer identification + address (background).
- Return-level (`education-credits-taxpayer`):
  - `claimsEducationCreditsOnReturn` = `Yes`
  - `receivedAny1098TForClaimedStudents` = `Yes`
  - `uploadedAll1098TStatementsForClaimedStudents` = `Yes`
  - `usingAlternativeEnrollmentDocumentationForAnyStudent` = `No`
  - `confirmAllEducationSupportUploaded` = `Yes`
  - `aotcPreviouslyDenied` = `No`
  - `hasMagiAddBacks` = `No`
- One Add-student row:
  - `studentFirstNameLine20` = `Jordan`, `studentLastNameLine20` = `Miles`
  - `studentTinLine21` = `333-44-5555`
  - `studentRelationshipToTaxReturn` = `Dependent`
  - `creditTypeRequested` = `aotc`
  - `adjustedQualifiedEducationExpenses` = `$4,000`
  - `taxFreeEducationAssistanceReductionAmount` = `$750`
  - `institution1NameLine22` = `North Lake University`
  - `institution1AddressLine22` = `100 College Ave, Springfield, IL 62701`
  - `institution1EinLine22` = `12-3456789`
  - `institution1Received1098T` = `Yes`
  - `institution1PriorYear1098TBox7Checked` = `No`
  - `studentHasSecondInstitutionLine22` = `No`
  - `aotcClaimedFourPriorYearsLine23` = `No`
  - `studentWasAtLeastHalfTimeLine24` = `Yes`
  - `studentCompletedFirstFourYearsBefore2025Line25` = `No`
  - `studentHadFelonyDrugConvictionLine26` = `No`

**IRS rule.** This is a persistence test, not a tax computation. The Internal Revenue Service does not impose a structural requirement on a software product's data round-trip; this test simply asserts that the application's form save mechanism does not silently drop fields.

**Code path.** Angular `form-education-credits.component.ts` → `PersonalDataService.saveForm('education-credits-taxpayer', payload)` → REST `PUT /api/personal/education-credits-taxpayer` → `PersonalDataServiceImpl` → Firestore document write. Read-back: `GET /api/personal/education-credits-taxpayer` → JSON envelope `{ data: { ... } }`. The fields under test are governed by `PersonalResource.PERSONAL_FORMS` allow-list per the memory note `feedback_personal_resource_allowlist`.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `payload.data.claimsEducationCreditsOnReturn === true` | (persistence) | persisted true | ✅ OK |
| 2 | `payload.data.confirmAllEducationSupportUploaded === true` | (persistence) | persisted true | ✅ OK |
| 3 | `payload.data.taxpayerEducationCreditStudents.length === 1` | (persistence) | 1 | ✅ OK |
| 4 | `payload.data.taxpayerEducationCreditStudents[0].creditTypeRequested === 'aotc'` | (persistence) | `'aotc'` | ✅ OK |
| 5 | `payload.data.taxpayerEducationCreditStudents[0].institution1NameLine22 === 'North Lake University'` | (persistence) | `'North Lake University'` | ✅ OK |

---

## Test 2 (line 100) — `education credits spouse workflow saves additional student entries separately`

```powershell
npx playwright test --workers=1 --output=test-results-line29-2 `
  -g "education credits spouse workflow" line8863-education-credits.spec.ts
```

**Purpose.** Mirror of Test 1 for the spouse-tab form. Verifies that the spouse-only form (`education-credits-spouse`) holds its own `spouseHasAdditionalEducationCreditStudents` gate plus a separate `spouseEducationCreditStudents[]` array, and that the spouse form does NOT also carry the taxpayer's return-level `confirmAllEducationSupportUploaded` (only the taxpayer form owns return-level fields per the YAML person-split convention).

**Inputs (seeded via UI).**
- Spouse profile seeded via `savePersonalFormApi('spouse', ...)`.
- Spouse tab selected; `spouseHasAdditionalEducationCreditStudents` = `Yes`.
- One Add-student row on the spouse form:
  - `studentFirstNameLine20` = `Taylor`, `studentLastNameLine20` = `Miles`
  - `studentTinLine21` = `777-88-9999`
  - `studentRelationshipToTaxReturn` = `Dependent`
  - `creditTypeRequested` = `llc`
  - `adjustedQualifiedEducationExpenses` = `$2,500`
  - `institution1NameLine22` = `Metro Career Institute`
  - `institution1AddressLine22` = `50 Learning Way, Madison, WI 53703`
  - `institution1Received1098T` = `No`
  - `studentHasSecondInstitutionLine22` = `No`

**IRS rule.** Persistence test — no IRS rule applies. The architectural rule under test is the project's YAML person-split convention.

**Code path.** Spouse-tab navigation → `form-education-credits.component.ts` (spouse mode) → `PUT /api/personal/education-credits-spouse` → Firestore. `confirmAllEducationSupportUploaded` is NOT in the spouse form's YAML and must therefore be `undefined` on the spouse read-back.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `payload.data.spouseHasAdditionalEducationCreditStudents === true` | (persistence) | true | ✅ OK |
| 2 | `payload.data.spouseEducationCreditStudents.length === 1` | (persistence) | 1 | ✅ OK |
| 3 | `payload.data.spouseEducationCreditStudents[0].creditTypeRequested === 'llc'` | (persistence) | `'llc'` | ✅ OK |
| 4 | `payload.data.confirmAllEducationSupportUploaded === undefined` | spouse form does not own this field | undefined | ✅ OK |

---

## Test 3 (line 148) — `Line 29 — single AOTC student $4k expenses, no phaseout → line 29 = $1,000`

```powershell
npx playwright test --workers=1 --output=test-results-line29-3 `
  -g "single AOTC student" line8863-education-credits.spec.ts
```

**Purpose.** The "happy-path" AOTC: one qualifying student with the AOTC-maxing $4,000 of adjusted qualified education expenses, MAGI well under the $80,000 lower phaseout threshold. Pins the canonical 40% refundable / 60% nonrefundable split.

**Inputs (seeded via API).**
- Single filer, $70,000 W-2 wages → AGI = $70,000.
- One AOTC student, all four Part III disqualifier answers compliant, $4,000 adjusted qualified education expenses.

**IRS hand calculation.**

Per Form 8863 Part III for the single student:
- Line 27 = `min($4,000, $4,000)` = $4,000
- Line 28 = `max(0, $4,000 - $2,000)` = $2,000
- Line 29 (of Form 8863) = $2,000 × 25% = $500
- Line 30 = `$500 + $2,000` (line 28 > 0 path) = **$2,500** (maximum AOTC per student)

Form 8863 Part I:
- Line 1 = sum of student line 30 = $2,500
- Line 2 = $90,000 (Single threshold)
- Line 3 = MAGI = $70,000 (no add-backs)
- Line 4 = $90,000 − $70,000 = $20,000 (greater than range)
- Line 5 = $10,000
- Line 6 = `min(1.000, $20,000 / $10,000)` = **1.000** (no phaseout)
- Line 7 = $2,500 × 1.000 = $2,500
- Line 8 = $2,500 × 40% = **$1,000** → Form 1040 line 29

Form 8863 Part II:
- Line 9 = $2,500 − $1,000 = $1,500 (nonrefundable AOTC remainder)
- Line 10-12 = $0 (no Lifetime Learning Credit students)
- Line 18 = $1,500 (just the nonrefundable AOTC; no Lifetime Learning Credit phased amount to add)

Credit Limit Worksheet:
- Form 1040 line 18 (tax before credits) at AGI $70,000 − standard deduction $15,750 = taxable $54,250
  - 10% × $11,925 = $1,193 (rounded)
  - 12% × ($48,475 − $11,925) = 12% × $36,550 = $4,386
  - 22% × ($54,250 − $48,475) = 22% × $5,775 = $1,271 (rounded)
  - Total = $6,850 (hand-calc); code uses unrounded brackets → ≈ $6,849
- Prior Schedule 3 credits = $0
- Credit limit = $6,849 − $0 = $6,849
- Line 19 = `min($1,500, $6,849)` = **$1,500** → Schedule 3 line 3 → Form 1040 line 20

Schedule 3 wiring:
- Schedule 3 line 3 = $1,500

**Code path.** `computeForm8863` at `TaxReturnComputeService.java:26127`. Single → `isMfj = false`. `phaseoutUpper = $90,000`, `phaseoutRange = $10,000`. MAGI computation reads four add-back fields (all null → zero) plus the new G-NEW-12 auto-fill (no `foreign-earned-income-taxpayer` form saved → zero). Per-student loop: G-NEW-1 line 30 formula picks the "line 28 > 0" branch → $2,500. Part I yields line8 = $1,000. Part II yields line9 = $1,500, line18 = $1,500. CLW caps at $1,500. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | not null | non-null | ✅ OK |
| 2 | `line8RefundableAotc === 1000` | $1,000 | $1,000 | ✅ OK |
| 3 | `americanOpportunityCredit === 1000` | $1,000 | $1,000 | ✅ OK |
| 4 | `line19NonrefundableEducationCredits === 1500` | $1,500 | $1,500 | ✅ OK |
| 5 | `schedule3.nonrefundableCredits.educationCredits === 1500` | $1,500 | $1,500 | ✅ OK |
| 6 | `header.name === 'E2E Tester'` | seeded name | `'E2E Tester'` | ✅ OK |
| 7 | `header.ssn === '111-11-1111'` | seeded SSN | `'111-11-1111'` | ✅ OK |
| 8 | `students.length === 1` and `students[0].creditType === 'aotc'` | 1, `'aotc'` | matches | ✅ OK |

---

## Test 4 (line 171) — `Line 29 — LLC student only → line 29 = 0, Schedule 3 line 3 > 0`

```powershell
npx playwright test --workers=1 --output=test-results-line29-4 `
  -g "LLC student only" line8863-education-credits.spec.ts
```

**Purpose.** Lifetime Learning Credit smoke test. Confirms that no Lifetime Learning Credit dollars leak into the refundable line 29 (Lifetime Learning Credit is 100% nonrefundable per Internal Revenue Code section 25A(c)).

**Inputs.**
- Single, $70,000 W-2 wages.
- One LLC student, $5,000 adjusted qualified education expenses.

**IRS hand calculation.**
- Form 8863 Part III line 31 = $5,000 (LLC student row, just expenses; no per-student credit math).
- Part I: no AOTC students → line 1 = $0, line 7 = $0, line 8 = $0.
- Part II:
  - Line 10 = $5,000 (sum of LLC expenses)
  - Line 11 = `min($5,000, $10,000)` = $5,000
  - Line 12 = $5,000 × 20% = $1,000
  - phaseoutFraction at MAGI $70,000 Single = 1.000 (well below $80,000 lower threshold)
  - Line 18 = $1,000 × 1.000 = $1,000 (Lifetime Learning Credit after phaseout)
- Credit Limit Worksheet:
  - line 9 (Form 8863) = $0 (no nonrefundable AOTC); CLW input = $0 + $1,000 = $1,000
  - Tax before credits at $70k AGI Single = $6,849; prior credits = $0; credit limit = $6,849
  - Line 19 = `min($1,000, $6,849)` = **$1,000**
- Schedule 3 line 3 = $1,000.
- Form 1040 line 29 = $0 / null.

**Code path.** Same `computeForm8863` flow. The per-student loop hits the `else if ("llc".equals(creditType))` branch (line 26365-26367 after the recent edits): `totalLlcExpenses += $5,000`. Part I yields zero. Part II computes line 12 = $1,000, llcPhased = $1,000, line 18 = $1,000, line 19 = $1,000. Wiring: `payments.americanOpportunityCredit` is left null because `line8RefundableAotc` is zero. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | non-null | non-null | ✅ OK |
| 2 | `line8RefundableAotc` is null | null/$0 | null | ✅ OK |
| 3 | `americanOpportunityCredit` is null | null/$0 | null | ✅ OK |
| 4 | `Number(line19NonrefundableEducationCredits) === 1000` | $1,000 | $1,000 | ✅ OK |
| 5 | `Number(schedule3.educationCredits) === 1000` | $1,000 | $1,000 | ✅ OK |

---

## Test 5 (line 197) — `Line 29 — MAGI above $90k ceiling → form8863 present, line 8 and line 19 both null`

```powershell
npx playwright test --workers=1 --output=test-results-line29-5 `
  -g "MAGI above \$90k ceiling" line8863-education-credits.spec.ts
```

**Purpose.** Hard ceiling. Verifies that crossing the $90,000 Single upper phaseout cap zeros both the refundable line 29 and the nonrefundable Schedule 3 line 3, while leaving the Form 8863 envelope present (so a header is still populated for an explanatory printed return).

**Inputs.**
- Single, $95,000 W-2 wages → AGI = MAGI = $95,000.
- One AOTC student, $4,000 expenses.

**IRS hand calculation.**
- MAGI ($95,000) ≥ upper ceiling ($90,000) → phaseoutFraction = 0.
- Form 8863 line 6 = 0; line 7 = $0; line 8 = **$0** (null on the wire); line 9-18 all zero; line 19 = **$0** (null on the wire).
- Form 8863 envelope is still produced (header populated, magiExceedsPhaseoutCeiling = true).

**Code path.** `computeForm8863` sets `phaseoutFraction = BigDecimal.ZERO` at line 26233-26234, sets `magiExceedsPhaseoutCeiling = true` at line 26249, populates the header, and `return result` early at line 26266-26268. The Form 8863 still goes back to the caller; `line8RefundableAotc` / `line19NonrefundableEducationCredits` are never set (default null). Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | envelope present | non-null | ✅ OK |
| 2 | `magiExceedsPhaseoutCeiling === true` | true | true | ✅ OK |
| 3 | `line8RefundableAotc` is null | null/$0 | null | ✅ OK |
| 4 | `line19NonrefundableEducationCredits` is null | null/$0 | null | ✅ OK |
| 5 | `americanOpportunityCredit` is null | null/$0 | null | ✅ OK |

---

## Test 6 (line 212) — `Line 29 — MFS filing status → form8863 null, EDUCATION_CREDITS_MFS_INELIGIBLE flag emitted`

```powershell
npx playwright test --workers=1 --output=test-results-line29-6 `
  -g "MFS filing status" line8863-education-credits.spec.ts
```

**Purpose.** Verifies the Married Filing Separately disqualifier per Internal Revenue Code section 25A(g)(6). Education credits are completely barred to MFS filers; the entire Form 8863 envelope must be suppressed.

**Inputs.**
- Filing status = Married Filing Separately.
- Taxpayer DOB seeded; $60,000 W-2 wages.
- One AOTC student, $4,000 expenses.
- Compute called with `overrideFlags: true` because non-blocking advisory flags from the MFS path would otherwise be present.

**IRS hand calculation.** Section 25A(g)(6) bars BOTH the American Opportunity Tax Credit and the Lifetime Learning Credit on a Married Filing Separately return. Form 8863 is not filed and Form 1040 line 29 is $0 / null.

**Code path.** `computeForm8863` at line 26196-26200: when `isMfs` is true, the non-blocking flag is emitted and the helper returns `null`. Downstream `applyForm8863ToSchedule3` is a no-op on null; the `payments.americanOpportunityCredit` setter is never called. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8863` is null | null | null | ✅ OK |
| 2 | `americanOpportunityCredit` is null | null | null | ✅ OK |
| 3 | `flags.some(f => f.code === 'EDUCATION_CREDITS_MFS_INELIGIBLE')` | true | true | ✅ OK |

---

## Test 7 (line 231) — `Line 29 — under-24 refundable restriction → line 29 = 0, Schedule 3 line 3 > 0`

```powershell
npx playwright test --workers=1 --output=test-results-line29-7 `
  -g "under-24 refundable restriction" line8863-education-credits.spec.ts
```

**Purpose.** Verifies the Internal Revenue Code section 25A(i)(1) under-age-24 restriction. The refundable share (Form 1040 line 29) is forced to zero; the entire credit shifts to the nonrefundable column (Schedule 3 line 3 → Form 1040 line 20).

**Inputs.**
- Single, $60,000 W-2 wages.
- One AOTC student, $4,000 expenses.
- `refundableAotcRestrictionApplies = true` (the legacy manual-override boolean; recent G-NEW-6 closure kept this as the override path).

**IRS hand calculation.**
- Per-student line 30 = $2,500 (same as Test 3 math).
- Line 1 = $2,500; line 6 = 1.000 (MAGI $60,000 < $80,000); line 7 = $2,500.
- Under-24 path: line 8 = $0; line 9 = $2,500 (full credit shifts).
- Part II line 18 (CLW input) = $2,500 + $0 (no LLC) = $2,500.
- Tax at AGI $60,000 Single − standard deduction $15,750 = taxable $44,250.
  - 10% × $11,925 = $1,193 (rounded)
  - 12% × ($44,250 − $11,925) = 12% × $32,325 = $3,879
  - Total ≈ $5,072
- Credit limit = $5,072 − $0 = $5,072.
- Line 19 = `min($2,500, $5,072)` = **$2,500** → Schedule 3 line 3.

**Code path.** `computeForm8863` at lines 26376-26397 after the G-NEW-6 closure: the legacy `refundableAotcRestrictionApplies` boolean is checked first as an override; when present and `true`, `restrictRefundable = true`, `line8 = 0`, `line9 = line7`. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | non-null | non-null | ✅ OK |
| 2 | `line8RefundableAotc` is null | null | null | ✅ OK |
| 3 | `americanOpportunityCredit` is null | null | null | ✅ OK |
| 4 | `line19NonrefundableEducationCredits > 0` | $2,500 | $2,500 | ⚠️ OK but loose (could pin to `=== 2500`) — **GAP A** |
| 5 | `schedule3.educationCredits > 0` | $2,500 | $2,500 | ⚠️ OK but loose (could pin to `=== 2500`) — **GAP A** |

---

## Test 8 (line 249) — `Line 29 — MFJ spouse student: taxpayer AOTC + spouse LLC → studentCount 2`

```powershell
npx playwright test --workers=1 --output=test-results-line29-8 `
  -g "MFJ spouse student" line8863-education-credits.spec.ts
```

**Purpose.** Verifies the MFJ taxpayer-and-spouse student merge: one Form 8863 covers BOTH spouses' students, with the spouse-form `spouseEducationCreditStudents[]` array merged into the taxpayer-form `taxpayerEducationCreditStudents[]` array before the per-student loop runs. Both credit types (AOTC + LLC) compute on the same Form 8863.

**Inputs.**
- MFJ, taxpayer $70,000 W-2 wages, spouse profile seeded.
- Taxpayer: 1 AOTC student, $4,000 expenses.
- Spouse: 1 LLC student, $5,000 expenses, gated by `spouseHasAdditionalEducationCreditStudents = true`.

**IRS hand calculation.**
- AOTC student: same math as Test 3 → line 30 = $2,500 → line 1 = $2,500.
- MFJ phaseout: phaseoutUpper = $180,000, phaseoutRange = $20,000. MAGI = $70,000 (well below lower threshold $160,000) → phaseoutFraction = 1.000.
- Line 7 = $2,500; line 8 = 40% × $2,500 = **$1,000**; line 9 = $1,500.
- LLC student: line 10 = $5,000; line 11 = $5,000; line 12 = $1,000; llcPhased = $1,000.
- Line 18 (CLW input) = $1,500 + $1,000 = $2,500.
- MFJ tax at AGI $70,000 − standard deduction $31,500 = taxable $38,500.
  - 10% × $23,850 = $2,385
  - 12% × ($38,500 − $23,850) = 12% × $14,650 = $1,758
  - Total tax = $4,143
- Credit limit = $4,143; line 19 = `min($2,500, $4,143)` = **$2,500**.

**Code path.** `computeForm8863` at lines 26303-26310: when `isMfj` and `spouseHasAdditionalEducationCreditStudents === true`, the spouse student list is appended to the taxpayer list and the studentCount reflects the combined size. Per-student loop then runs once with both. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | non-null | non-null | ✅ OK |
| 2 | `studentCount === 2` | 2 | 2 | ✅ OK |
| 3 | `Number(line8RefundableAotc) === 1000` | $1,000 | $1,000 | ✅ OK |
| 4 | `Number(americanOpportunityCredit) === 1000` | $1,000 | $1,000 | ✅ OK |
| 5 | `Number(line19NonrefundableEducationCredits) > 1500` | $2,500 | $2,500 | ⚠️ OK but loose (could pin to `=== 2500`) — **GAP A** |

---

## Test 9 (line 298) — `Line 29 — Form 8862 gate: aotcPreviouslyDenied + no Form8862 → AOTC blocked, LLC credited`

```powershell
npx playwright test --workers=1 --output=test-results-line29-9 `
  -g "Form 8862 gate" line8863-education-credits.spec.ts
```

**Purpose.** Verifies the Internal Revenue Code section 6213(g)(2)(K) Form 8862 recertification requirement: when the American Opportunity Tax Credit was previously denied or reduced (for any reason other than math or clerical error) and Form 8862 Part IV has not been completed, the AOTC for ALL students is blocked. The Lifetime Learning Credit is unaffected (it has its own Form 8862 Part V gate, not exercised here).

**Inputs.**
- Single, $70,000 W-2 wages.
- Form `education-credits-taxpayer` with `aotcPreviouslyDenied = true`.
- Two students: 1 AOTC ($4,000), 1 LLC ($5,000).
- No `form8862` personal form saved → gate fires.
- Compute called with `overrideFlags: true` because the gate's advisory flag would otherwise pass through.

**IRS hand calculation.**
- AOTC student: per-student loop's Form 8862 gate at lines 26261-26269 sees `aotcStudentEligible = empty list` → blocked → `continue`. Student not added to AOTC aggregate. line 1 = $0; line 7 = $0; line 8 = **null/$0**.
- LLC student: same math as Test 4 → line 12 = $1,000; line 18 = $1,000.
- Tax = $6,849; credit limit = $6,849; line 19 = `min($1,000, $6,849)` = **$1,000**.

**Code path.** `computeForm8863` at lines 26194-26201 emits `FORM_8862_AOTC_REQUIRED` flag and sets `aotcStudentEligible = List.of()`. Per-student loop blocks the AOTC student; the LLC student computes normally. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | non-null | non-null | ✅ OK |
| 2 | `line8RefundableAotc` is null | null | null | ✅ OK |
| 3 | `americanOpportunityCredit` is null | null | null | ✅ OK |
| 4 | `line19NonrefundableEducationCredits > 0` | $1,000 | $1,000 | ⚠️ OK but loose (could pin to `=== 1000`) — **GAP A** |
| 5 | `schedule3.educationCredits > 0` | $1,000 | $1,000 | ⚠️ OK but loose (could pin to `=== 1000`) — **GAP A** |
| 6 | `flags.some(f => f.code === 'FORM_8862_AOTC_REQUIRED')` | true | true | ✅ OK |

---

## Test 10 (line 338) — `Line 29 — two AOTC students both qualify → line 29 = $2,000 aggregate`

```powershell
npx playwright test --workers=1 --output=test-results-line29-10 `
  -g "two AOTC students" line8863-education-credits.spec.ts
```

**Purpose.** Per-student aggregation: two AOTC students each at the $4,000 expense cap should produce $2,500 + $2,500 = $5,000 on Form 8863 line 1 and $2,000 on line 8 (40%).

**Inputs.**
- Single, $70,000 W-2 wages.
- Two AOTC students, each $4,000 expenses, all disqualifier answers compliant.

**IRS hand calculation.**
- Each student: line 30 = $2,500 (same as Test 3).
- Line 1 = $5,000; line 6 = 1.000; line 7 = $5,000.
- Line 8 = $5,000 × 40% = **$2,000**.

**Code path.** `computeForm8863` per-student loop accumulates `totalAotcBeforePhaseout` across both students. Both students pass all four disqualifier gates. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | non-null | non-null | ✅ OK |
| 2 | `studentCount === 2` | 2 | 2 | ✅ OK |
| 3 | `Number(line1TotalAotcBeforePhaseout) === 5000` | $5,000 | $5,000 | ✅ OK |
| 4 | `Number(line8RefundableAotc) === 2000` | $2,000 | $2,000 | ✅ OK |
| 5 | `Number(americanOpportunityCredit) === 2000` | $2,000 | $2,000 | ✅ OK |

---

## Test 11 (line 378) — `Line 29 — LLC student, MAGI $85k Single → 50% phaseout → nonrefundable ≈ $1,000`

```powershell
npx playwright test --workers=1 --output=test-results-line29-11 `
  -g "MAGI \$85k Single" line8863-education-credits.spec.ts
```

**Purpose.** Verifies the Lifetime Learning Credit through the midpoint of the phaseout band ($85,000 Single = halfway between $80,000 and $90,000). Confirms that LLC respects the same fraction as AOTC and that the $10,000 cap (per return, not per student) is applied before phaseout.

**Inputs.**
- Single, $85,000 W-2 wages → AGI = MAGI = $85,000.
- One LLC student, $10,000 expenses (exactly at the LLC return-level cap).

**IRS hand calculation.**
- phaseoutFraction = `(90,000 − 85,000) / 10,000` = **0.500**.
- Line 10 = $10,000; line 11 = `min($10,000, $10,000)` = $10,000.
- Line 12 = $10,000 × 20% = $2,000 (tentative LLC).
- llcPhased = $2,000 × 0.500 = **$1,000** (form line 18).
- Line 9 = $0 (no AOTC).
- CLW input = $0 + $1,000 = $1,000.
- Tax at AGI $85,000 Single − standard deduction $15,750 = taxable $69,250.
  - 10% × $11,925 = $1,193
  - 12% × $36,550 = $4,386
  - 22% × ($69,250 − $48,475) = 22% × $20,775 = $4,571 (rounded)
  - Total ≈ $10,149
- Credit limit = $10,149; line 19 = `min($1,000, $10,149)` = **$1,000**.
- Form 1040 line 29 = $0 / null (no AOTC).

**Code path.** `computeForm8863` phaseout fraction logic at line 26230-26243 produces 0.500 (after the G-NEW-3 closure that changed `ROUND_DOWN` → `HALF_UP`; at exactly 0.5 both rounding modes produce the same value). LLC pipeline at lines 26416-26420 yields llcPhased = $1,000. CLW caps at $1,000. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | non-null | non-null | ✅ OK |
| 2 | `line8RefundableAotc` is null | null | null | ✅ OK |
| 3 | `Number(line19NonrefundableEducationCredits) === 1000` | $1,000 | $1,000 | ✅ OK |
| 4 | `Number(schedule3.educationCredits) === 1000` | $1,000 | $1,000 | ✅ OK |

---

## Test 12 (line 413) — `Line 29 — AOTC student disqualified by 4-year limit → form8863 present but no credit`

```powershell
npx playwright test --workers=1 --output=test-results-line29-12 `
  -g "disqualified by 4-year limit" line8863-education-credits.spec.ts
```

**Purpose.** Verifies the Internal Revenue Code section 25A(b)(2)(C) four-year limit (Form 8863 Part III line 23 "Stop!"): a student who has already had the American Opportunity Tax Credit claimed for them in any 4 prior tax years is permanently barred from AOTC. The user's `creditTypeRequested = 'aotc'` election is respected (no auto-fallback to LLC per G-NEW-9, IRS Form 8863 Part III Caution).

**Inputs.**
- Single, $70,000 W-2 wages.
- One AOTC student with `aotcClaimedFourPriorYearsLine23 = true`.

**IRS hand calculation.**
- Per-student loop: line 23 = "Yes" → "Stop! Go to line 31 for this student" — but the user did NOT request LLC, so no line 31 is computed either.
- Line 1 = $0 (no qualifying AOTC students); line 8 = **null/$0**.
- No LLC student → line 19 = **null/$0**.

**Code path.** `computeForm8863` per-student loop at line 26336-26345 (after the G-NEW-1 closure): `alreadyClaimed4Years = true` → `continue` short-circuits the student. No accumulation to `totalAotcBeforePhaseout`. Form 8863 envelope still constructed with header + magi populated. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | envelope present | non-null | ✅ OK |
| 2 | `line8RefundableAotc` is null | null | null | ✅ OK |
| 3 | `line19NonrefundableEducationCredits` is null | null | null | ✅ OK |
| 4 | `americanOpportunityCredit` is null | null | null | ✅ OK |

---

## Test 13 (line 442) — `Line 29 — MAGI exactly $80k Single → full AOTC credit (phaseoutFraction = 1.0)`

```powershell
npx playwright test --workers=1 --output=test-results-line29-13 `
  -g "MAGI exactly \$80k" line8863-education-credits.spec.ts
```

**Purpose.** Boundary at the lower phaseout threshold. At MAGI = $80,000 exactly, the IRS rule produces fraction 1.000 (= `(90,000 − 80,000) / 10,000`), not the truncated 0.999 a naive division might give. Locks in the inclusive-boundary semantics.

**Inputs.**
- Single, $80,000 W-2 wages.
- One AOTC student, $4,000 expenses.

**IRS hand calculation.**
- MAGI = $80,000; numerator = $90,000 − $80,000 = $10,000; range = $10,000.
- Numerator ≥ range → phaseoutFraction = 1.000 (no phaseout).
- Same math as Test 3 after that: line 1 = $2,500; line 8 = $1,000.

**Code path.** `computeForm8863` at lines 26236-26238: `numerator.compareTo(phaseoutRange) >= 0` → `phaseoutFraction = BigDecimal.ONE`. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | non-null | non-null | ✅ OK |
| 2 | `magiExceedsPhaseoutCeiling === false` | false | false | ✅ OK |
| 3 | `Number(line1TotalAotcBeforePhaseout) === 2500` | $2,500 | $2,500 | ✅ OK |
| 4 | `Number(line8RefundableAotc) === 1000` | $1,000 | $1,000 | ✅ OK |
| 5 | `Number(americanOpportunityCredit) === 1000` | $1,000 | $1,000 | ✅ OK |

---

## Test 14 (line 459) — `Line 29 — MAGI exactly $90k Single → AOTC fully phased out (phaseoutFraction = 0)`

```powershell
npx playwright test --workers=1 --output=test-results-line29-14 `
  -g "MAGI exactly \$90k" line8863-education-credits.spec.ts
```

**Purpose.** Boundary at the upper phaseout threshold. At MAGI = $90,000 the IRS rule "if MAGI ≥ upper threshold → no credit" must apply strictly (≥, not >).

**Inputs.**
- Single, $90,000 W-2 wages.
- One AOTC student, $4,000 expenses.

**IRS hand calculation.**
- MAGI ($90,000) ≥ upper ceiling ($90,000) → phaseoutFraction = **0**.
- Form 8863 line 7 = $0; line 8 = **null/$0**; line 19 = **null/$0**.
- `magiExceedsPhaseoutCeiling = true`.

**Code path.** `computeForm8863` at line 26232-26233: `magi.compareTo(phaseoutUpper) >= 0` → `phaseoutFraction = BigDecimal.ZERO`. Early return at line 26266 without setting line 7/8/19. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `f8863` is truthy | envelope present | non-null | ✅ OK |
| 2 | `magiExceedsPhaseoutCeiling === true` | true | true | ✅ OK |
| 3 | `line8RefundableAotc` is null | null | null | ✅ OK |
| 4 | `line19NonrefundableEducationCredits` is null | null | null | ✅ OK |
| 5 | `americanOpportunityCredit` is null | null | null | ✅ OK |

---

## Coverage GAPs — paths not exercised by ANY test in this spec

> **Round-2 closure status (2026-06-11)** — 1 assertion-tightening gap (A) + 12 coverage gaps (B, D-N) closed. **Round-3 closure status (2026-06-11)** — remaining GAP C closed via new MFJ ReferenceData wiring test. Spec file now has **14 original tests + 13 new tests = 27 tests total**. All listed gaps fixed; **0 policy-blocked outstanding**.
>
> | Gap | Status | Closed by |
> |---|---|---|
> | A | **FIXED (round 2)** | Tests 7, 8, 9 — 5 loose assertions tightened to exact IRS hand-calc values |
> | B | **FIXED (round 2)** | New test: `G-NEW-1 boundary: AOTC student with $1,000 expenses → line 30 = $1,000 (NOT $2,000)` |
> | C | **FIXED (round 3)** | New test: `G-NEW-2 MFJ ReferenceData wiring: $170k MAGI → threshold $180k, range $20k, fraction 0.500` — Single side was already covered by GAP K's test ($90k/$10k); the new test exercises the MFJ branch ($180k/$20k) so both literals in `ReferenceData.AOTC_PHASEOUT_UPPER_*` are now directly load-bearing on at least one e2e assertion. |
> | D | **FIXED (round 2)** | New test: `G-NEW-3 non-round MAGI: $85,432 Single → fraction 0.457 (half-up rounded)` |
> | E | **FIXED (round 2)** | New test: `G-NEW-4 taxpayer TIN-by-due-date: blocking flag, form8863 null` |
> | F | **FIXED (round 2)** | New test: `G-NEW-4 student TIN-by-due-date: advisory flag, AOTC dropped` |
> | G | **FIXED (round 2)** | New test: `G-NEW-5 claimed-as-dependent: blocking flag, form8863 null` |
> | H | **FIXED (round 2)** | New test: `G-NEW-6 under-24 compound: under-18 + parent alive + Single → line 8 = 0 (sub-fields, no override)` |
> | I | **FIXED (round 2)** | New test: `G-NEW-7 1098-T advisory flag` |
> | J | **FIXED (round 2)** | New test: `G-NEW-10 NRA blocker: blocking flag, form8863 null` |
> | K | **FIXED (round 2)** | New test: `G-NEW-11 PDF intermediates round-trip: 8 form-line fields exposed` |
> | L | **FIXED (round 2)** | New test: `G-NEW-12 Form 2555 MAGI auto-fill: $85k W-2 + $20k FEIE → MAGI $105k > ceiling → no credit` — highest-value gap (cross-form) |
> | M | **FIXED (round 2)** | New test: `G-NEW-13 Part III disqualifier booleans: round-trip onto students[0]` |
> | N | **FIXED (round 2)** | New test: `CLW capping: $25k W-2 + LLC $10k → tentative $2,000 capped to tax $925` |
>
> Detailed gap descriptions retained below for historical reference.

Each row below is a real-world Form 8863 scenario or a known divergence already tracked in `XLS/Computations/29.md`. None of these paths fails today (most have Java unit-test coverage), but per the memory note `feedback_java_unit_passing_doesnt_mean_e2e_passing` — Java unit tests bypass REST/persistence/mapper/JSON-serialization, so the e2e variant can fail even when the unit passes. Each is a candidate test to add.

### GAP A — Loose dollar-amount assertions in Tests 7, 8, 9

Per `feedback_e2e_exact_value_pins`: tax assertions must use exact hand-computed pins (`.toBe(N)`), never `> 0` or other loose comparisons. Tests 7, 8, 9 currently use `> 0` or `> 1500` for hand-computable values ($2,500 / $2,500 / $1,000). Tightening prevents future regressions from being masked by an over-permissive bound. Fix is one-line per assertion (change to `.toBe(2500)` / `.toBe(2500)` / `.toBe(1000)`).

### GAP B — G-NEW-1 line 30 boundary case: expenses ≤ $2,000

The single biggest fix in `XLS/Computations/29.md` (Round 1, 2026-06-11) was the line 30 formula. Java unit tests `computeForm8863_aotcExpensesBelow2000_creditEqualsExpenses` and `computeForm8863_aotcExpensesExactly2000_creditEquals2000` cover the new branch. NO e2e covers it. Add an e2e: Single, $70k W-2, one AOTC student with $1,000 expenses → expect line 30 = $1,000; line 1 = $1,000; line 8 = $400; line 19 = $600.

### GAP C — G-NEW-2 reference data wiring

No e2e verifies that the centralized `ReferenceData.AOTC_*` constants are actually read by the helper. Indirectly covered by Tests 3, 5, 13, 14 (which would break if the constants moved), but no test explicitly asserts the threshold value via `f8863.phaseoutThresholdLine2`.

### GAP D — G-NEW-3 phaseout fraction rounding (non-round MAGI)

`RoundingMode.DOWN` → `RoundingMode.HALF_UP` change is invisible on all 14 existing tests because every MAGI ($70k, $80k, $85k, $90k, $95k, $60k) produces a round fraction (0.000, 0.500, or 1.000). Add an e2e: Single, $85,432 W-2 → MAGI $85,432 → fraction = `(90,000 − 85,432) / 10,000` = 0.4568 → rounds to 0.457; AOTC line 7 = $2,500 × 0.457 = $1,143; line 8 = $1,143 × 40% rounded = $457.

### GAP E — G-NEW-4 taxpayer TIN-by-due-date blocker

`taxpayerTinIssuedByDueDate = false` should set blocking flag `EDUCATION_CREDITS_TAXPAYER_TIN_NOT_BY_DUE_DATE` and return null form8863. Covered by Java unit `computeForm8863_taxpayerTinNotByDueDate_blocksCredit`. No e2e.

### GAP F — G-NEW-4 student TIN-by-due-date advisory

`studentTinIssuedByDueDate = false` on a per-student basis should skip the student for AOTC and emit non-blocking `AOTC_STUDENT_TIN_NOT_BY_DUE_DATE`. Covered by Java unit `computeForm8863_studentTinNotByDueDate_skipsAotcForStudent`. No e2e.

### GAP G — G-NEW-5 claimed-as-dependent blocker

`claimedAsDependentOnAnotherReturn = true` should set blocking flag `EDUCATION_CREDITS_TAXPAYER_CLAIMED_AS_DEPENDENT` and return null. Covered by Java unit `computeForm8863_claimedAsDependent_blocksCredit`. No e2e.

### GAP H — G-NEW-6 under-24 compound test legs

Test 7 only exercises the legacy override boolean. The new sub-condition fields (`under24WasUnder18AtYearEnd`, `under24WasAge18AndEarnedIncomeLtHalfSupport`, `under24WasAge19To23FullTimeStudentEarnedIncomeLtHalfSupport`, `under24AtLeastOneParentAlive`) are covered by 6 Java unit tests. No e2e covers any of the three age legs or the parent-alive gating.

### GAP I — G-NEW-7 advisory 1098-T flag

When `receivedAny1098TForClaimedStudents = false` and `usingAlternativeEnrollmentDocumentationForAnyStudent = false`, the advisory `AOTC_1098T_NOT_RECEIVED_NO_ALTERNATIVE` flag should fire. Java unit `computeForm8863_no1098TAndNoAlternative_advisoryFlag` covers it. No e2e.

### GAP J — G-NEW-10 non-resident alien blocker

`taxpayerOrSpouseDisqualifiedAsNonresidentAlien = true` should set blocking flag `EDUCATION_CREDITS_NONRESIDENT_ALIEN_INELIGIBLE`. Covered by Java unit `computeForm8863_nonresidentAlien_blocksCredit`. No e2e.

### GAP K — G-NEW-11 PDF intermediates

Form 8863 fields `phaseoutThresholdLine2`, `phaseoutRangeLine5`, `phaseoutFractionLine6`, `line9NonrefundableAotcRemainder`, `line10TotalLlcExpenses`, `line11LlcExpensesAfterCap`, `line12LlcTentativeCredit`, `line18LlcAfterPhaseout` are exposed for PDF rendering. Java unit `computeForm8863_intermediatesExposed_forPdfRendering` covers it. No e2e asserts these fields are present and correct in the JSON payload.

### GAP L — G-NEW-12 Form 2555 MAGI auto-fill

The most significant correctness fix in the last round: when manual MAGI add-back fields are blank, the helper reads Form 2555 output. Java unit `computeForm8863_form2555AddBack_autoFilledFromForeignEarnedIncomeWhenManualBlank` covers the auto-fill; `computeForm8863_form2555AddBack_manualOverrideWins` covers the override. NO e2e covers either. This is the most worthwhile coverage gap to close because it crosses a form boundary (`foreign-earned-income-taxpayer` → Form 2555 compute → Form 8863 MAGI add-back) that pure Java unit tests do not exercise end-to-end.

Suggested e2e: Single, $85k W-2 + `foreign-earned-income-taxpayer` with $20k foreign earned income exclusion → expect `f8863.magiForPhaseout === 105000` (= $85k AGI + $20k Form 2555 exclusion); `f8863.magiExceedsPhaseoutCeiling === true`; `f8863.line8RefundableAotc` null.

### GAP M — G-NEW-13 Part III "Stop!" disqualifier booleans on student view

The four per-student booleans (`aotcClaimedFourPriorYearsLine23`, `studentWasAtLeastHalfTimeLine24`, `studentCompletedFirstFourYearsBefore2025Line25`, `studentHadFelonyDrugConvictionLine26`) are now copied onto `Form8863Student` for PDF checkbox rendering. Java unit `computeForm8863_disqualifierBooleansCopiedToStudentView_forPdfCheckboxes` covers the round-trip. No e2e asserts these are present on `payload.form8863.students[0]`.

### GAP N — Credit Limit Worksheet capping path

All existing tests have tax liability much greater than the tentative nonrefundable credit, so the CLW always returns `min(line18, creditLimit) === line18`. No test exercises the case where `creditLimit` is the binding constraint (taxable income very low so tax < tentative credit). Add an e2e: Single, $25k W-2, one LLC student with $10k expenses → tentative LLC = $2,000; standard deduction = $15,750; taxable = $9,250; tax = $925; line 19 = `min($2,000, $925)` = **$925** (NOT $2,000). Also exercises the Schedule 3 prior-credit subtraction path when paired with a Form 2441 child-and-dependent-care credit.

---

## Audit signal

- **0 MISMATCHes**: 14/14 tests assert values that match the IRS-canonical computation per Form 8863, Internal Revenue Code section 25A, and the 2025 Form 1040 Instructions.
- **0 per-assertion correctness GAPs**: every existing assertion lands on the IRS expected value.
- **GAP A (loose assertions in Tests 7, 8, 9)**: 5 assertions across 3 tests should be tightened from `> 0` / `> 1500` to exact `=== 2500` / `=== 2500` / `=== 1000`. The hand-calculated value is available; the loose form risks masking a future regression.
- **GAPs B–N (13 coverage GAPs)**: paths not exercised by ANY test in this spec — most have Java unit coverage but no e2e variant. Per project memory `feedback_java_unit_passing_doesnt_mean_e2e_passing`, the e2e variant verifies REST + persistence + JSON serialization on the same code paths and can fail when Java unit passes.

If you want me to harden Tests 7/8/9 (GAP A) and/or add new spec tests for the highest-value coverage gaps (GAP B line-30 boundary, GAP L Form 2555 auto-fill, GAP N CLW capping), say the word and I'll do them in a follow-up.
