# Test catalog — Form 1040 line 32 (Total Other Payments and Refundable Credits)

> Spec files audited:
> - `C:\us-tax\us-tax-be\e2e\tests\line32-total-other-payments.spec.ts` (4 tests, 170 lines) — direct line 32 summation tests.
> - `C:\us-tax\us-tax-be\e2e\tests\form8839-adoption-credits.spec.ts` (19 tests, 636 lines) — line 30 (Refundable Adoption Credit) producer that feeds line 32.
> - `C:\us-tax\us-tax-be\e2e\tests\line33-refund-owed.spec.ts` (7 tests, 324 lines) — line 33 consumer of line 32, plus lines 34/35/36/37 downstream.
>
> Audited 2026-06-12 against IRS sources: 2025 Form 1040 page 2 (`docs/IRS-Forms/f1040.pdf`); 2025 Instructions for Form 1040 (`docs/books/i1040gi_2025.pdf`); Publication 17 (Your Federal Income Tax) chapters 4 (Tax Withholding and Estimated Tax) and 25 (Other Credits); Publication 525 (Taxable and Nontaxable Income) for the Earned Income Credit and Adoption Credit; Publication 575 (Pension and Annuity Income) only as a sanity check for line 25b 1099-R withholding (not exercised by line 32 specs); Publication 596 (Earned Income Credit) for the qualifying-child rules and the EIC table; Publication 970 (Tax Benefits for Education) chapter 2 for the American Opportunity Credit refundable share; 2025 Form 8839 (Adoption Credit) Part II and Part III; 2025 Form 8863 (Education Credits) line 8; 2025 Schedule 8812 (Credits for Qualifying Children and Other Dependents) Part II-A; 2025 Schedule 3 (Additional Credits and Payments) Part II; Internal Revenue Code section 32 (Earned Income Credit), section 24 (Child Tax Credit), section 25A (American Opportunity Credit), section 23 (Adoption Credit), section 137 (Adoption Assistance Programs), section 1341 (Claim of Right), section 1372 (2%+ S-corporation owner), section 965(i) (deferred net section 965 tax); One Big Beautiful Bill Act of 2025 (Refundable Adoption Credit per-child cap = $5,000); Rev. Proc. 2024-40 (2025 EIC and CTC parameters). Code paths: `TaxReturnComputeService.computeLine27aEIC`, `computeSchedule8812`, `computeForm8863`, `computeAdoptionBenefits`, `applyAdoptionCredit`, `applyOtherPaymentsFormToSchedule3`, `applyForm2439CreditToSchedule3`, `finalizeSchedule3Totals`, `computeLine31ThroughLine38`. Spec docs: `XLS/Computations/32.md`, `lines/32.md`, `dependencies/32.md`, `knowledge/line-32-total-other-payments.md`.
>
> Tax year: **2025**.

## How to run

```powershell
cd C:\us-tax\us-tax-be\e2e

# All 30 tests across the 3 spec files
npx playwright test --workers=1 --output=test-results-line32 `
  line32-total-other-payments.spec.ts `
  form8839-adoption-credits.spec.ts `
  line33-refund-owed.spec.ts

# Just the 4 direct line-32 tests
npx playwright test --workers=1 --output=test-results-line32-only line32-total-other-payments.spec.ts

# Single-test runs (use the -g flag with a title substring)
npx playwright test --workers=1 --output=test-results-line32-1 -g "subtotal stored" line32-total-other-payments.spec.ts
```

Per `e2e/CLAUDE.md`: `--workers=1` always; never run in the Claude Code sandbox (EPERM); fresh `--output` directory each run.

---

## 2025 reference data (constants used by line 32 components)

All five line-32 addends depend on year-specific IRS reference data. Centralized in `ReferenceData.java`.

| Constant | 2025 value | Source |
|---|---:|---|
| Earned Income Credit — maximum (no qualifying children) | $649 | IRC §32; Rev. Proc. 2024-40 |
| Earned Income Credit — maximum (1 qualifying child) | $4,328 | IRC §32 |
| Earned Income Credit — maximum (2 qualifying children) | $7,152 | IRC §32 |
| Earned Income Credit — maximum (3+ qualifying children) | $8,046 | IRC §32 |
| Earned Income Credit — investment income ceiling | $11,950 | IRC §32(i); Rev. Proc. 2024-40 |
| Earned Income Credit — Single phase-in end (1 child) | $12,730 | Rev. Proc. 2024-40 |
| Earned Income Credit — Single phase-out start (1+ children) | $23,350 | Rev. Proc. 2024-40 |
| Earned Income Credit — childless Single phase-in rate | 7.65% | IRC §32(b) |
| Earned Income Credit — childless Single phase-in end | $8,490 | Rev. Proc. 2024-40 |
| Additional Child Tax Credit — refundable cap per qualifying child | $1,700 | IRC §24 + 2025 inflation indexing |
| Additional Child Tax Credit — earned-income floor offset | $2,500 | IRC §24(d)(1)(B)(i) |
| Additional Child Tax Credit — earned-income multiplier | 15% | IRC §24(d)(1)(B)(ii) |
| Child Tax Credit — maximum per qualifying child for 2025 | $2,200 | IRC §24 + One Big Beautiful Bill Act of 2025 |
| American Opportunity Credit — refundable percentage | 40% | IRC §25A(i)(5) |
| American Opportunity Credit — maximum per student | $2,500 | IRC §25A(b) |
| American Opportunity Credit — refundable cap per student | $1,000 | derived: 40% × $2,500 |
| American Opportunity Credit — Single phase-out range | $80,000–$90,000 | IRC §25A(d) |
| Adoption Credit — maximum qualified expenses per child | $17,280 | IRC §23 + 2025 inflation indexing |
| Adoption Credit — Refundable Adoption Credit cap per child | $5,000 | IRC §23 (One Big Beautiful Bill Act of 2025) |
| Adoption Credit — MAGI phaseout start | $259,190 | IRC §23(b) |
| Adoption Credit — MAGI phaseout end | $299,190 | derived: $259,190 + $40,000 |
| Adoption Benefits exclusion (employer-provided) max per child | $17,280 | IRC §137 + 2025 inflation indexing |
| Section 1341 — repayment threshold for the credit method | $3,000 | IRC §1341(a)(3) |
| 2025 Single standard deduction | $15,750 | Rev. Proc. 2024-40 |

---

## Summary

| # | Spec | Test title | Direct line-32 | Net result |
|---:|---|---|:-:|---|
| 1 | f8839 | Part III taxable benefits flow to line 1f via line 31 | ✗ indirect (line 1f) | ✅ OK on 4 assertions |
| 2 | f8839 | Special needs final adoption creates negative line 1f | ✗ indirect | ✅ OK on 4 assertions |
| 3 | f8839 | Part II refundable credit computed when MAGI is available | ✓ feeds line 30 | ✅ OK on 4 assertions (line-32 pin added 2026-06-12) |
| 4 | f8839 | Part II refundable credit reduced by MAGI phaseout at 50% | ✓ feeds line 30 | ✅ OK on 4 assertions (line-32 pin added 2026-06-12) |
| 5 | f8839 | Part II credit null when MAGI is above phaseout ceiling | ✓ feeds line 30 | ✅ OK on 4 assertions (line-32 null pin added 2026-06-12) |
| 6 | f8839 | Line 30 displays refundable adoption credit on Form 1040 preview | ✓ feeds line 30 | ✅ OK on 1 assertion; ⚠️ 1 coverage GAP — policy-deferred (PDF field rendering needs pdf-lib parsing in e2e) |
| 7 | f8839 | Line 32 includes refundable adoption credit in total other payments | ✓ direct line 32 | ✅ OK on 2 assertions |
| 8 | f8839 | Part II nonrefundable adoption credit flows to Schedule 3 line 6c and Form 1040 line 20 | ✗ feeds line 20 only | ✅ OK on 3 assertions |
| 9 | f8839 | GAP C section 137(d) reimbursement subtraction | ✗ Part II producer | ✅ OK on 3 assertions |
| 10 | f8839 | GAP D MFS disqualifier | ✓ pins line 30 null | ✅ OK on 5 assertions |
| 11 | f8839 | GAP E MFS considered-unmarried exception | ✓ feeds line 30 | ✅ OK on 4 assertions (line-32 pin added 2026-06-12) |
| 12 | f8839 | GAP F special-needs final without W-2 box T | ✗ Part III producer only | ✅ OK on 4 assertions |
| 13 | f8839 | GAP G multi-child phaseout | ✓ feeds line 30 | ✅ OK on 5 assertions (line-32 pin added 2026-06-12) |
| 14 | f8839 | GAP H foreign-child finality-year retroactive exclusion | ✗ Part III producer only | ✅ OK on 4 assertions |
| 15 | f8839 | GAP I section 1372 disqualifier | ✗ Part III producer only | ✅ OK on 1 assertion |
| 16 | f8839 | GAP J shared-adoption cap split | ✓ feeds line 30 | ✅ OK on 5 assertions (line-32 pin added 2026-06-12) |
| 17 | f8839 | GAP K G-NEW-2 MAGI manual-vs-auto mismatch advisory | ✗ flag advisory | ✅ OK on 4 assertions |
| 18 | f8839 | GAP L G-NEW-3 CLW-B line 14 = max(0, line 12 - line 27) | ✗ Schedule 8812 worksheet | ✅ OK on 2 assertions |
| 19 | f8839 | GAP M G-NEW-1 more-than-six-children attachment advisory | ✗ flag advisory | ✅ OK on 3 assertions |
| 20 | line32 | line32: subtotal stored and flows correctly into line33 | ✓ direct | ✅ OK on 4 assertions |
| 21 | line32 | line32: null when no refundable credits or other payments | ✓ direct (null path) | ✅ OK on 3 assertions |
| 22 | line32 | line32: EIC and adoption credit both contribute to line 32 total | ✓ direct (2 sources) | ✅ OK on 4 assertions |
| 23 | line32 | line32: refundable adoption credit contributes to line 32 total | ✓ direct (1 source) | ✅ OK on 2 assertions |
| 24 | line33 | line33 = line24: neither refund nor amount owed | ✗ line 33/24 edge | ✅ OK on 4 assertions |
| 25 | line33 | line34: refund calculated when overpaid | ✗ line 34 | ✅ OK on 5 assertions |
| 26 | line33 | line37: amount owed when underpaid | ✗ line 37 | ✅ OK on 4 assertions |
| 27 | line33 | line36: apply-to-next-year reduces refund | ✗ line 36 | ✅ OK on 6 assertions |
| 28 | line33 | line36: apply-to-next-year capped when amount exceeds overpayment | ✗ line 36 | ✅ OK on 6 assertions |
| 29 | line33 | line35bcd: direct deposit suppressed when Form 8888 elected | ✗ line 35b/c/d | ✅ OK on 9 assertions |
| 30 | line33 | line35bcd: direct deposit fields populated when elected | ✗ line 35b/c/d | ✅ OK on 5 assertions |
| 31 | line32 | line32: ACTC (line 28) alone contributes to line 32 total | ✓ direct (suite gap #2 closure 2026-06-12) | ✅ OK on 4 assertions |
| 32 | line32 | line32: Refundable AOTC (line 29) alone contributes to line 32 total | ✓ direct (suite gap #3 closure 2026-06-12) | ✅ OK on 4 assertions |
| 33 | line32 | line32: all five sources (EIC + ACTC + AOTC + Adoption + Schedule 3) sum correctly | ✓ direct (suite gap #1 closure 2026-06-12) | ✅ OK on 8 assertions |

**0 MISMATCHes against the IRS rule.** **0 per-assertion correctness GAPs.** **6 of the original 7 "missing line-32 pin" coverage gaps closed 2026-06-12** (Tests 3, 4, 5, 11, 13, 16); the remaining gap on Test 6 (PDF field rendering) is policy-deferred because verifying it requires pdf-lib parsing inside e2e. **3 of the original 6 suite-level gaps closed 2026-06-12** via the new Tests 31, 32, 33 (ACTC alone, refundable AOTC alone, all-5-sources); the remaining gaps (PTC via Schedule 3, PDF field key in render, non-negativity guardrail) are policy-deferred — see "Suite-level coverage gaps" at the end of this document.

---

## Test 1 (line 1f / line 31 path, not line 32) — `Part III taxable benefits flow to line 1f via line 31`

```powershell
npx playwright test --workers=1 --output=test-results-line32-1 `
  -g "Part III taxable benefits flow to line 1f via line 31" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies Form 8839 Part III computes the taxable portion of employer-provided adoption benefits (line 31 of Form 8839, not Form 1040) and that the taxable portion is added back to Form 1040 line 1f (wages). This test exercises a **producer** for Form 1040 line 1f, not line 32.

**Inputs.**
- W-2: $60,000 wages, $60,000 Social Security wages, box 12 code T = $5,000.
- Personal form `adoption-expenses`: 1 child, prior-year employer benefits $16,000, current-year (2025) benefits $5,000.

**IRS hand calculation.** Per Form 8839 Part III instructions and IRC §137:
- Line 23 (total benefits this year) = $5,000.
- Lifetime cap per child for the exclusion = $17,280 (IRC §137 + 2025 indexing).
- Already-used in prior year = $16,000; remaining exclusion room = $17,280 − $16,000 = $1,280.
- Line 30 (total excluded benefits) = min($5,000, $1,280) = $1,280.
- Line 31 (taxable benefits) = $5,000 − $1,280 = $3,720.
- Form 1040 line 1f (other wages) gets +$3,720 from this Form 8839 Part III result.

**Code path.** `computeAdoptionBenefits` Part III at `TaxReturnComputeService.java` (the function reads `priorYearEmployerBenefitsByChild` and `currentYearBenefitsAllocation`, computes the per-child excluded amount against the IRC §137 lifetime cap, and adds the taxable remainder to `Form1040.income.adoptionBenefits` which flows to line 1f). Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.part3Line23TotalBenefits === 5000` | $5,000 | $5,000 | ✅ OK |
| 2 | `form8839.part3Line30TotalExcludedBenefits === 1280` | $1,280 | $1,280 | ✅ OK |
| 3 | `form8839.part3Line31TaxableBenefits === 3720` | $3,720 | $3,720 | ✅ OK |
| 4 | `form1040.income.adoptionBenefits === 3720` | $3,720 | $3,720 | ✅ OK |

**Why this test is in the line-32 bundle.** It is a precondition for tests 7 and 22 (which use the same Form 8839 path to populate line 30 → line 32). The line-1f effect itself does not appear on line 32.

---

## Test 2 (line 1f / Part III producer) — `Special needs final adoption creates negative line 1f`

```powershell
npx playwright test --workers=1 --output=test-results-line32-2 `
  -g "Special needs final" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the IRC §137(a)(3) special-needs final-adoption rule: the $17,280 exclusion applies retroactively in the year the adoption becomes final, regardless of how much was paid by the employer. The resulting line 1f goes **negative** (wages decrease) so that the prior-year W-2 inclusion is unwound.

**Inputs.** W-2 wages $60,000, box 12 code T $2,000 (current-year benefits); 1 child with `hasSpecialNeeds=true` and `adoptionFinal2025OrEarlier=true`; current-year allocation $2,000.

**IRS hand calculation.** Per IRC §137(a)(3) and Form 8839 Part III instructions for special-needs final adoption:
- Line 21 = $17,280 (special-needs lump-sum exclusion, regardless of actual benefits paid).
- Line 24 = line 21 = $17,280 (not capped to line 22 in the special-needs path).
- Line 30 = $17,280.
- Line 31 = $2,000 − $17,280 = **−$15,280**.
- Form 1040 line 1f gets −$15,280.

**Code path.** `computeAdoptionBenefits` Part III special-needs branch — when `hasSpecialNeeds=true` AND `adoptionFinal2025OrEarlier=true`, the exclusion is set to the full $17,280 regardless of current-year benefits. Line 1f receives the negative remainder. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.part3Line30TotalExcludedBenefits === 17280` | $17,280 | $17,280 | ✅ OK |
| 2 | `form8839.part3Line31TaxableBenefits === -15280` | −$15,280 | −$15,280 | ✅ OK |
| 3 | `form1040.income.adoptionBenefits === -15280` | −$15,280 | −$15,280 | ✅ OK |
| 4 | `flags.some(f => f.code === 'ADOPTION_BENEFITS_WRITTEN_QAAP_REMINDER')` | flag present | flag present | ✅ OK |

---

## Test 3 (line 30 producer, indirectly line 32) — `Part II refundable credit computed when MAGI is available`

```powershell
npx playwright test --workers=1 --output=test-results-line32-3 `
  -g "Part II refundable credit computed when MAGI" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the Form 8839 Part II Refundable Adoption Credit fires for a single child with $15,000 expenses when MAGI is below the $259,190 phase-out start. This is the producer that feeds **Form 1040 line 30** which in turn feeds line 32.

**Inputs.** W-2 box T $3,000; MAGI $200,000 (below phase-out start); 1 child, $15,000 qualified expenses; current-year benefits $3,000.

**IRS hand calculation.** Per Form 8839 Part II instructions and IRC §23 as amended by the One Big Beautiful Bill Act of 2025:
- MAGI $200,000 < phase-out start $259,190 → phaseout fraction = 0.
- Line 6 = min($17,280, $15,000) = $15,000.
- Line 11a = $15,000 × (1 − 0) = $15,000.
- Line 11b = min($15,000, $5,000 refundable cap) = $5,000.
- Line 13 (refundable) = $5,000.
- `Payments.refundableAdoptionCredit` = $5,000 → flows to Form 1040 line 30 → line 32.

**Code path.** `computeAdoptionBenefits` Part II + `applyAdoptionCredit` (which re-confirms after Credit Limit Worksheet B for the nonrefundable side). Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.part2Line13RefundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 2 | `form8839.part2Line11cTotalRefundableBase === 5000` | $5,000 | $5,000 | ✅ OK |
| 3 | `form1040.payments.refundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |

**Per-test coverage GAP.** Test does not pin `totalOtherPaymentsAndRefundableCredits` (line 32). With this single source, line 32 should also be $5,000 (verified by Test 23). Adding `expect(Number(payments.totalOtherPaymentsAndRefundableCredits)).toBe(5000)` here would catch a regression where line 30 sets correctly but the line-32 summation breaks.

---

## Test 4 (line 30 producer, MAGI phase-out) — `Part II refundable credit reduced by MAGI phaseout at 50%`

```powershell
npx playwright test --workers=1 --output=test-results-line32-4 `
  -g "Part II refundable credit reduced by MAGI" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the IRC §23(b) MAGI phase-out: a MAGI of $279,190 places the taxpayer exactly midway through the $40,000 phase-out window (from $259,190 to $299,190), so the phase-out fraction is 0.5. The credit base is halved, but the $5,000 per-child refundable cap still applies.

**Inputs.** W-2 box T $3,000; MAGI $279,190; 1 child, $17,280 expenses; current-year benefits $3,000.

**IRS hand calculation.**
- Phaseout fraction = ($279,190 − $259,190) / ($299,190 − $259,190) = $20,000 / $40,000 = 0.5.
- Line 6 = $17,280.
- Line 11a = $17,280 × (1 − 0.5) = $8,640.
- Line 11b = min($8,640, $5,000) = **$5,000** (cap still applies).
- Line 13 = $5,000.

**Code path.** `computeAdoptionBenefits` Part II MAGI phaseout formula in `applyAdoptionCredit`. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.part2Line9PhaseoutFraction ≈ 0.5` | 0.5 | 0.5 | ✅ OK |
| 2 | `form8839.part2Line11cTotalRefundableBase === 5000` | $5,000 | $5,000 | ✅ OK |
| 3 | `form8839.part2Line13RefundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |

**Per-test coverage GAP.** Test does not pin line 32 (would be $5,000 here as the sole source).

---

## Test 5 (line 30 zero path) — `Part II credit null when MAGI is above phaseout ceiling`

```powershell
npx playwright test --workers=1 --output=test-results-line32-5 `
  -g "Part II credit null when MAGI" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the high-MAGI ceiling at $299,190: above this MAGI the phaseout fraction is 1.0, fully eliminating the Refundable Adoption Credit. Also verifies the project convention that a zero credit is stored as `null` (not `0`) in `payments.refundableAdoptionCredit`.

**Inputs.** W-2 box T $3,000; MAGI $310,000; 1 child, $17,280 expenses.

**IRS hand calculation.**
- Phaseout fraction = ($310,000 − $259,190) / $40,000 = 1.27, clamped at 1.0.
- Line 11a = $17,280 × (1 − 1.0) = $0.
- Line 11b = $0.
- Line 13 = **$0** → `payments.refundableAdoptionCredit` = null (project convention).

**Code path.** `applyAdoptionCredit` at `TaxReturnComputeService.java:22188` clamps the fraction, computes a zero credit, then the null-when-zero project convention applies. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.part2Line11cTotalRefundableBase === 0` | $0 | $0 | ✅ OK |
| 2 | `form8839.part2Line13RefundableAdoptionCredit === 0` | $0 | $0 | ✅ OK |
| 3 | `form1040.payments.refundableAdoptionCredit ?? null === null` | null | null | ✅ OK |

**Per-test coverage GAP.** Test does not pin line 32 null (would be null here since all 5 sources are absent).

---

## Test 6 (UI line 30 render) — `Line 30 displays refundable adoption credit on Form 1040 preview`

```powershell
npx playwright test --workers=1 --output=test-results-line32-6 `
  -g "Line 30 displays refundable adoption" form8839-adoption-credits.spec.ts
```

**Purpose.** Same fixture as Test 3 but exercises `computeReturnViaUi` (clicks the Compute button instead of using the API) to verify the UI round-trip and PDF preview produce the same `payments.refundableAdoptionCredit` value.

**Inputs.** Same as Test 3 (MAGI $200,000, 1 child, $15,000 expenses).

**IRS hand calculation.** Same as Test 3: line 30 = $5,000.

**Code path.** Same backend as Test 3. UI path goes through `TaxReturnService.computeReturn` then the Angular preview renders.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form1040.payments.refundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |

**Per-test coverage GAP.** Test does not pin the value of `line30_refundable_adoption_credit` in the rendered PDF field map. The recent gap-A fix in `form-tax-return-1040.component.ts` (lines 29/30/31/32 now use 2025 semantic field names) is the kind of regression that a PDF-field assertion here would catch. Currently this fix is verifiable only by manual save-as-PDF.

---

## Test 7 (DIRECT line 32) — `Line 32 includes refundable adoption credit in total other payments`

```powershell
npx playwright test --workers=1 --output=test-results-line32-7 `
  -g "Line 32 includes refundable adoption" form8839-adoption-credits.spec.ts
```

**Purpose.** Direct test that the Refundable Adoption Credit ($5,000) flows from line 30 to line 32 when it is the **sole** contributor. Uses `computeReturnViaUi`.

**Inputs.** MAGI $200,000; 1 child, $15,000 expenses; current-year benefits $3,000 (the W-2 code-T value is set but does not affect line 32 directly).

**IRS hand calculation.**
- Line 30 = $5,000 (as in Test 3).
- Line 27a = 0 (no EIC claim).
- Line 28 = 0 (no ACTC fixture).
- Line 29 = 0 (no Form 8863 fixture).
- Line 31 = 0 (no Schedule 3 Part II credit).
- **Line 32 = 0 + 0 + 0 + 5000 + 0 = $5,000.**

**Code path.** `computeLine31ThroughLine38` at `TaxReturnComputeService.java:27262` sums the five `Payments.*` fields. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form1040.payments.refundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 2 | `form1040.payments.totalOtherPaymentsAndRefundableCredits === 5000` | $5,000 | $5,000 | ✅ OK |

---

## Test 8 (line 20 path, not line 32) — `Part II nonrefundable adoption credit flows to Schedule 3 line 6c and Form 1040 line 20`

```powershell
npx playwright test --workers=1 --output=test-results-line32-8 `
  -g "nonrefundable adoption credit flows to Schedule 3" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the **nonrefundable** portion of the Adoption Credit (Form 8839 line 14 = line 12 − line 13) flows to Schedule 3 line 6c (not to Form 1040 line 30) and then to Form 1040 line 20 (not line 32). This test enforces the line-30-vs-line-20 split.

**Inputs.** W-2 $80,000 wages, $0 withholding; MAGI $200,000; 1 child, $10,000 expenses.

**IRS hand calculation.** Per Form 8839 Part II:
- Line 11a (after MAGI phaseout) = $10,000.
- Line 11b (refundable, capped at $5,000) = $5,000 → Form 1040 line 30.
- Line 14 (nonrefundable) = $10,000 − $5,000 = $5,000 → Schedule 3 line 6c.
- Schedule 3 line 6c capped by Credit Limit Worksheet B at tax-after-refundable-credits. Tax on $80,000 single ≈ $9,050; CLW-B caps the nonrefundable side at $5,000 (the entire line 14 is allowed).
- Schedule 3 lines 1–6 subtotal → Form 1040 line 20 = $5,000.

**Code path.** `computeAdoptionBenefits` + `applyAdoptionCredit` (which runs after Credit Limit Worksheet B for the nonrefundable side). Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form1040.payments.refundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 2 | `schedule3.nonrefundableCredits.adoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 3 | `form1040.taxAndCredits.otherCreditsSchedule3 === 5000` | $5,000 | $5,000 | ✅ OK |

**Why this test is in the line-32 bundle.** It validates that the nonrefundable portion does NOT leak into line 32. This is the **negative-coverage** test for line 32 — the absence of line 30 contributions from this fixture (only the refundable $5,000 routed to line 30) confirms the split.

---

## Test 9 (Part II producer detail) — `GAP C section 137(d) reimbursement subtraction`

```powershell
npx playwright test --workers=1 --output=test-results-line32-9 `
  -g "GAP C section 137" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies IRC §137(d) double-dip prevention: per-child employer reimbursement reduces the qualified expenses base before the per-child cap applies.

**Inputs.** Gross expenses $15,000; employer reimbursement $3,000 → net qualified $12,000.

**IRS hand calculation.** Per IRC §137(d):
- Line 5 (qualified expenses net of reimbursement) = $15,000 − $3,000 = $12,000.
- Line 6 = min($17,280, $12,000) = $12,000.
- Line 11a (no MAGI phaseout) = $12,000.
- Line 11b = min($12,000, $5,000) = $5,000 → line 30.

**Code path.** `computeAdoptionBenefits` Part II reimbursement subtraction at the `child1EmployerReimbursementToSubtract` field. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.children[0].part2Line5QualifiedExpenses === 12000` | $12,000 | $12,000 | ✅ OK |
| 2 | `form8839.children[0].part2Line6SmallerOf4Or5 === 12000` | $12,000 | $12,000 | ✅ OK |
| 3 | `form8839.part2Line13RefundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |

---

## Test 10 (MFS disqualifier) — `GAP D MFS disqualifier`

```powershell
npx playwright test --workers=1 --output=test-results-line32-10 `
  -g "GAP D MFS disqualifier" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies IRC §137(f)/§23(g): a married-filing-separately filer is disqualified from BOTH the Adoption Credit and the exclusion (without the considered-unmarried exception). Form 8839 should not be generated; line 30 should be null; a non-blocking advisory flag should fire.

**Inputs.** Filing status MFS; W-2 box T $5,000; MAGI $200,000; 1 child, $15,000 expenses; current-year benefits $5,000.

**IRS hand calculation.**
- Filing status MFS → Adoption Credit blocked.
- `form8839` not built → `Payments.refundableAdoptionCredit` = null → line 30 = null → line 32 unaffected by this path.
- Adoption benefits exclusion is also disallowed → line 1f gets +$5,000 (the W-2 box T amount becomes taxable wages, because the exclusion is denied).
- Advisory flag `ADOPTION_BENEFITS_MFS_EXCLUSION_DISALLOWED` (non-blocking).

**Code path.** `computeAdoptionBenefits` MFS gate at the top of the method. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839 === null` | null | null | ✅ OK |
| 2 | `form1040.payments.refundableAdoptionCredit ?? null === null` | null | null | ✅ OK |
| 3 | `form1040.income.adoptionBenefits === 5000` | $5,000 | $5,000 | ✅ OK |
| 4 | flag `ADOPTION_BENEFITS_MFS_EXCLUSION_DISALLOWED` exists | exists | exists | ✅ OK |
| 5 | `flag.blocking === false` | non-blocking | non-blocking | ✅ OK |

---

## Test 11 (MFS considered-unmarried exception) — `GAP E MFS considered-unmarried exception`

```powershell
npx playwright test --workers=1 --output=test-results-line32-11 `
  -g "GAP E MFS considered-unmarried" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies IRC §137(f) exception: an MFS filer who meets the considered-unmarried test (lived apart from spouse for the last 6 months, or legally separated) is allowed the Adoption Credit and the exclusion on the same terms as Single.

**Inputs.** Filing status MFS with `mfsOrHohLivedApartOrLegallySeparated=true`; W-2 box T $3,000; MAGI $200,000; 1 child, $15,000 expenses.

**IRS hand calculation.**
- MFS exception met → treated as Single for §137 / §23 purposes.
- Same as Test 3: line 30 = $5,000.
- Line 32 (sole source) = $5,000.

**Code path.** `computeAdoptionBenefits` MFS exception branch checking `mfsOrHohLivedApartOrLegallySeparated`. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839` is non-null | non-null | non-null | ✅ OK |
| 2 | `form8839.part2Line13RefundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 3 | `form1040.payments.refundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |

**Per-test coverage GAP.** Test does not pin line 32 (would be $5,000).

---

## Test 12 (Part III producer, foreign / final / special needs) — `GAP F special-needs final without W-2 box T: line 1f = -$17,280`

```powershell
npx playwright test --workers=1 --output=test-results-line32-12 `
  -g "GAP F special-needs final without W-2 box T" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the special-needs final-adoption $17,280 exclusion applies even when there is no W-2 box 12 code T amount (i.e., the employer never paid benefits but the lump-sum special-needs exclusion still kicks in as a "retroactive wage reduction" on line 1f).

**Inputs.** Plain W-2 ($60,000 wages); 1 child with `hasSpecialNeeds=true` and `adoptionFinal2025OrEarlier=true`; no current-year benefits.

**IRS hand calculation.**
- Line 30 = $17,280 (special-needs final).
- Line 31 = $0 − $17,280 = −$17,280.
- Line 1f gets −$17,280.

**Code path.** Same special-needs branch as Test 2. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.part3Line30TotalExcludedBenefits === 17280` | $17,280 | $17,280 | ✅ OK |
| 2 | `form8839.part3Line31TaxableBenefits === -17280` | −$17,280 | −$17,280 | ✅ OK |
| 3 | `form1040.income.adoptionBenefits === -17280` | −$17,280 | −$17,280 | ✅ OK |
| 4 | flag `ADOPTION_BENEFITS_WRITTEN_QAAP_REMINDER` exists | exists | exists | ✅ OK |

---

## Test 13 (line 30 multi-child phase-out) — `GAP G multi-child phaseout: 2 children + MAGI $279,190 → line 13 = $10,000`

```powershell
npx playwright test --workers=1 --output=test-results-line32-13 `
  -g "GAP G multi-child phaseout" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the per-child cap applies independently for each child, even after MAGI phase-out. Two children at $17,280 expenses each, MAGI midway through phase-out → each child's line 11a = $8,640 → each child's line 11b = $5,000 → line 13 total = $10,000.

**Inputs.** MAGI $279,190 (50% phase-out); 2 children, $17,280 each; current-year benefits $1,500 each.

**IRS hand calculation.**
- Phaseout fraction 0.5.
- Per-child line 11a = $17,280 × 0.5 = $8,640.
- Per-child line 11b = min($8,640, $5,000) = $5,000.
- Line 11c (sum across children) = $10,000.
- Line 13 (refundable total) = $10,000.

**Code path.** `computeAdoptionBenefits` per-child loop applies the per-child cap inside the per-child branch. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.part2Line9PhaseoutFraction ≈ 0.5` | 0.5 | 0.5 | ✅ OK |
| 2 | `form8839.part2Line11cTotalRefundableBase === 10000` | $10,000 | $10,000 | ✅ OK |
| 3 | `form8839.part2Line13RefundableAdoptionCredit === 10000` | $10,000 | $10,000 | ✅ OK |
| 4 | `form1040.payments.refundableAdoptionCredit === 10000` | $10,000 | $10,000 | ✅ OK |

**Per-test coverage GAP.** Test does not pin line 32 (would be $10,000 in this fixture as the sole source).

---

## Test 14 (Part III producer, foreign-child retro) — `GAP H foreign-child finality-year retroactive exclusion produces negative line 1f`

```powershell
npx playwright test --workers=1 --output=test-results-line32-14 `
  -g "GAP H foreign-child finality" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the foreign-child retroactive-exclusion rule: when a foreign adoption becomes final in 2025, deferred prior-year benefits become eligible for exclusion retroactively. This reduces line 1f.

**Inputs.** W-2 box T $2,000; 1 foreign child, final in 2025; deferred prior-year foreign benefits $10,000; current-year benefits $2,000.

**IRS hand calculation.**
- Line 24 (per-child smaller of line 21 or 22) = $12,000 ($10,000 deferred + $2,000 current).
- Line 30 = $12,000.
- Line 31 = $2,000 − $12,000 = −$10,000.

**Code path.** `computeAdoptionBenefits` Part III foreign-child retroactive branch reading `deferredPriorYearForeignBenefitsByChild`. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.children[0].part3Line24SmallerOf21Or22 === 12000` | $12,000 | $12,000 | ✅ OK |
| 2 | `form8839.part3Line30TotalExcludedBenefits === 12000` | $12,000 | $12,000 | ✅ OK |
| 3 | `form8839.part3Line31TaxableBenefits === -10000` | −$10,000 | −$10,000 | ✅ OK |
| 4 | flag `ADOPTION_BENEFITS_WRITTEN_QAAP_REMINDER` exists | exists | exists | ✅ OK |

---

## Test 15 (Part III §1372 disqualifier) — `GAP I section 1372 disqualifier`

```powershell
npx playwright test --workers=1 --output=test-results-line32-15 `
  -g "GAP I section 1372" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies IRC §1372 disallowance: a 2%+ S-corporation owner does NOT get to exclude employer-provided adoption benefits paid by their own S-corp. The entire benefit becomes taxable wages on line 1f.

**Inputs.** W-2 box T $5,000; `section1372AnyEmployer=true` + `section1372ByEmployer[entryId]=true`; 1 child; current-year allocation $0 (since the §1372 disallowed slice bypasses Part III).

**IRS hand calculation.**
- §1372 disallows the entire $5,000 exclusion.
- Line 1f += $5,000 directly (bypassing the Part III line-31 calculation because the benefit was never eligible).

**Code path.** `computeAdoptionBenefits` §1372 disallowance branch. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form1040.income.adoptionBenefits === 5000` | $5,000 | $5,000 | ✅ OK |

---

## Test 16 (line 30 shared-adoption cap split) — `GAP J shared-adoption cap split: per-child cap reduced to $10,000`

```powershell
npx playwright test --workers=1 --output=test-results-line32-16 `
  -g "GAP J shared-adoption cap" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies IRC §137(b)(1): when two unrelated taxpayers share the adoption of the same child, the $17,280 per-child cap is split between them. Here the user is allocated $10,000.

**Inputs.** Shared adoption with non-spouse; allocated share $10,000; expenses $12,000.

**IRS hand calculation.**
- Per-child max = $10,000 (allocated share).
- Line 5 = $12,000; line 6 = min($10,000, $12,000) = $10,000.
- Line 11a = $10,000; line 11b = min($10,000, $5,000) = $5,000.
- Line 13 = $5,000.

**Code path.** `computeAdoptionBenefits` shared-adoption branch using `sharedAdoptionAllocatedShare`. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.children[0].part2Line2MaxCredit === 10000` | $10,000 | $10,000 | ✅ OK |
| 2 | `form8839.children[0].part2Line6SmallerOf4Or5 === 10000` | $10,000 | $10,000 | ✅ OK |
| 3 | `form8839.part2Line13RefundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 4 | flag `ADOPTION_BENEFITS_SHARED_ADOPTION_CAP_SPLIT` exists | exists | exists | ✅ OK |

**Per-test coverage GAP.** Test does not pin line 32 (would be $5,000).

---

## Test 17 (advisory) — `GAP K G-NEW-2 MAGI manual-vs-auto mismatch advisory`

```powershell
npx playwright test --workers=1 --output=test-results-line32-17 `
  -g "GAP K G-NEW-2" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the non-blocking advisory flag fires when the user-entered MAGI ($200,000) does not match the auto-computed MAGI (which would be based on $80,000 W-2 wages plus other income → much lower than $200,000). The credit still computes; the flag is informational.

**Inputs.** W-2 $80,000 wages; user-entered MAGI override $200,000; 1 child, $10,000 expenses.

**IRS hand calculation.** MAGI mismatch doesn't change the IRS computation — the user has the right to enter MAGI directly (e.g., to account for income not yet in this app). The advisory exists so the user is warned of the discrepancy. Per Form 8839 line 7 = $200,000 (as entered).

**Code path.** `applyAdoptionCredit` checks user-entered MAGI vs auto-computed MAGI from `Form1040.adjustments.line11bAmountFromLine11aAdjustedGrossIncome` and emits the advisory. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839.part2Line7ModifiedAgi === 200000` | $200,000 | $200,000 | ✅ OK |
| 2 | `form8839.part2Line13RefundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 3 | flag `ADOPTION_MAGI_MANUAL_VS_AUTO_MISMATCH` exists | exists | exists | ✅ OK |
| 4 | `flag.blocking === false` | non-blocking | non-blocking | ✅ OK |

---

## Test 18 (Schedule 8812 CLW-B for line 28) — `GAP L G-NEW-3 CLW-B line 14 = max(0, line 12 - line 27)`

```powershell
npx playwright test --workers=1 --output=test-results-line32-18 `
  -g "GAP L G-NEW-3" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the Schedule 8812 Credit Limit Worksheet B (used to bound the nonrefundable CTC slice, which in turn bounds the refundable Additional Child Tax Credit on line 28). The IRS rule: CLW-B line 14 = max(0, CTC line 12 − ACTC line 27).

**Inputs.** Single, W-2 $40,000; 1 CTC dependent; 1 adopted child $4,000 expenses; MAGI $40,000 (below all phaseouts).

**IRS hand calculation.** Per Schedule 8812 instructions:
- Line 12 (CTC after phaseout) = $2,200 per OBBBA 2025 (max $2,200 per child).
- Line 27 (ACTC) = some computed amount.
- CLW-B line 14 = max(0, $2,200 − line 27).

**Code path.** `computeSchedule8812` Credit Limit Worksheet B. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `schedule8812.creditLimitWorksheetBLine14 === max(0, line12 − line27)` | matches formula | matches | ✅ OK |
| 2 | `schedule8812.line12CreditAfterPhaseOut === 2200` | $2,200 | $2,200 | ✅ OK |

**Why this test is in the line-32 bundle.** It validates an upstream Schedule 8812 calculation that feeds Form 1040 line 28 → line 32. The CLW-B value bounds the Adoption Credit's nonrefundable side, which in turn affects how much of the Adoption Credit goes to the refundable side (line 30 → line 32).

---

## Test 19 (advisory) — `GAP M G-NEW-1 more-than-six-children attachment advisory`

```powershell
npx playwright test --workers=1 --output=test-results-line32-19 `
  -g "GAP M G-NEW-1" form8839-adoption-credits.spec.ts
```

**Purpose.** Verifies the non-blocking advisory flag fires when the user indicates more than 3 children on the Form 8839 (the form only has 3 child rows per page; additional rows require an attachment). Affects display only; does not affect line 30 / line 32 amounts.

**Inputs.** `moreThanThreeChildren=true` + 1 entry shown; $8,000 expenses.

**IRS hand calculation.** No effect on the dollar amount; only the advisory.

**Code path.** `computeAdoptionBenefits` reads `children.moreThanThreeChildren` and emits the advisory. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `form8839` is non-null | non-null | non-null | ✅ OK |
| 2 | flag `ADOPTION_BENEFITS_MORE_THAN_SIX_CHILDREN_ATTACHMENT_REQUIRED` exists | exists | exists | ✅ OK |
| 3 | `flag.blocking === false` | non-blocking | non-blocking | ✅ OK |

---

## Test 20 (DIRECT line 32, §1341 source) — `line32: subtotal stored and flows correctly into line33`

```powershell
npx playwright test --workers=1 --output=test-results-line32-20 `
  -g "subtotal stored" line32-total-other-payments.spec.ts
```

**Purpose.** Direct test that a single §1341 claim-of-right credit ($500) on Schedule 3 line 13b flows to Form 1040 line 31 = $500, then to line 32 = $500, then combines with $2,000 W-2 withholding (line 25a → 25d) to produce line 33 = $2,500.

**Inputs.** W-2: $10,000 wages, $2,000 withholding; `31-other-payments`: `hasSection1341Credit=true`, `section1341RepaymentAmount=4000` (above $3,000 threshold), `section1341Credit=500`.

**IRS hand calculation.**
- Line 25d (total withholding) = $2,000.
- Schedule 3 line 13b = $500 (§1341 credit; repayment $4,000 > $3,000 threshold per IRC §1341(a)(3)).
- Schedule 3 line 15 = $500 → line 31 = $500.
- Line 27a = 0, line 28 = 0, line 29 = 0, line 30 = 0.
- **Line 32 = 0 + 0 + 0 + 0 + $500 = $500.**
- **Line 33 = $2,000 + 0 + $500 = $2,500.**

**Code path.** `applyOtherPaymentsFormToSchedule3` → `finalizeSchedule3Totals` → `computeLine31ThroughLine38`. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `result.form1040.payments` is non-null | non-null | non-null | ✅ OK |
| 2 | `payments.totalOtherPaymentsAndRefundableCredits === 500` | $500 | $500 | ✅ OK |
| 3 | `payments.totalPayments === 2500` | $2,500 | $2,500 | ✅ OK |
| 4 | (the truthy check on `result`) | truthy | truthy | ✅ OK |

---

## Test 21 (DIRECT line 32, null path) — `line32: null when no refundable credits or other payments`

```powershell
npx playwright test --workers=1 --output=test-results-line32-21 `
  -g "null when no refundable credits" line32-total-other-payments.spec.ts
```

**Purpose.** Verifies the project null-storage convention: when all five line-32 inputs are zero, the field is stored as `null` (not `0`). Line 33 still works correctly (sees null as zero via `safeAmount`).

**Inputs.** W-2 only: $50,000 wages, $8,000 withholding. No EIC, ACTC, AOTC, adoption, Schedule 3 Part II.

**IRS hand calculation.**
- Line 27a = 0, line 28 = 0, line 29 = 0, line 30 = 0, line 31 = 0.
- **Line 32 = $0** → stored as null per project convention.
- Line 25d = $8,000.
- Line 33 = $8,000 + 0 + 0 (null → safeAmount → 0) = $8,000.

**Code path.** `computeLine31ThroughLine38` at line 27270 calls `setTotalOtherPaymentsAndRefundableCredits(... > 0 ? line32 : null)`. Code matches IRS rule (a blank Form 1040 line 32 is equivalent to $0).

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `payments.totalOtherPaymentsAndRefundableCredits ?? null === null` | null (project convention) | null | ✅ OK |
| 2 | `payments.totalPayments === 8000` | $8,000 | $8,000 | ✅ OK |
| 3 | (the truthy check on `result`) | truthy | truthy | ✅ OK |

---

## Test 22 (DIRECT line 32, 2 sources) — `line32: EIC (line 27a) and adoption credit (line 30) both contribute to line 32 total`

```powershell
npx playwright test --workers=1 --output=test-results-line32-22 `
  -g "EIC.*and adoption credit both contribute" line32-total-other-payments.spec.ts
```

**Purpose.** Verifies multi-source summation: childless EIC ($649) + Refundable Adoption Credit ($5,000) → line 32 = $5,649. Confirms each source flows independently and the sum is correct.

**Inputs.** W-2: $9,000 wages, $500 withholding (childless EIC phase-in zone); EIC claimed; adoption MAGI $200,000, 1 child, $15,000 expenses.

**IRS hand calculation.**
- Earned Income Credit (childless, single, $9,000 wages): $9,000 is in the plateau zone after phase-in (phase-in ends at $8,490 for childless Single). Max childless credit = $649. Line 27a = **$649**.
- Refundable Adoption Credit (as in Test 3): line 30 = **$5,000**.
- Line 28 = 0, line 29 = 0, line 31 = 0.
- **Line 32 = $649 + 0 + 0 + $5,000 + 0 = $5,649.**
- Line 25d = $500.
- **Line 33 = $500 + 0 + $5,649 = $6,149.**

**Code path.** `computeLine27aEIC` + `computeAdoptionBenefits` + `applyAdoptionCredit` + `computeLine31ThroughLine38`. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `payments.earnedIncomeCredit === 649` | $649 | $649 | ✅ OK |
| 2 | `payments.refundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 3 | `payments.totalOtherPaymentsAndRefundableCredits === 5649` | $5,649 | $5,649 | ✅ OK |
| 4 | `payments.totalPayments === 6149` | $6,149 | $6,149 | ✅ OK |

---

## Test 23 (DIRECT line 32, single source) — `line32: refundable adoption credit (line 30) contributes to line 32 total`

```powershell
npx playwright test --workers=1 --output=test-results-line32-23 `
  -g "refundable adoption credit.*contributes to line 32" line32-total-other-payments.spec.ts
```

**Purpose.** Verifies the single-source Refundable Adoption Credit case (same fixture as Test 7 but via API instead of UI).

**Inputs.** No W-2; MAGI $200,000; 1 child, $15,000 expenses.

**IRS hand calculation.**
- Line 30 = $5,000 (as in Test 3).
- All other sources = 0.
- **Line 32 = $5,000.**

**Code path.** Same as Test 7.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `payments.refundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 2 | `payments.totalOtherPaymentsAndRefundableCredits === 5000` | $5,000 | $5,000 | ✅ OK |

---

## Test 24 (line 33 / 34 edge case) — `line33 = line24: neither refund nor amount owed when payments exactly equal tax`

```powershell
npx playwright test --workers=1 --output=test-results-line32-24 `
  -g "neither refund nor amount owed" line33-refund-owed.spec.ts
```

**Purpose.** Verifies the boundary condition where line 33 (total payments) equals line 24 (total tax): both `refund` and `amountOwed` must be null. Uses a two-compute pattern to discover the exact tax then re-seed with matching withholding.

**Inputs.** Single, $100,000 wages, withholding adjusted to exactly match line 24 (no Schedule 2 → line 22 = line 24).

**IRS hand calculation.** Form 1040 lines 34 / 37 instructions: line 34 fires only when line 33 > line 24; line 37 fires only when line 24 > line 33. When equal, both are zero / null.

**Code path.** `computeLine31ThroughLine38` final branch: `payments.setTotalPayments(...)`, then the refund/amount-owed determination. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `taxAmount > 0` (tax from first compute) | positive | positive | ✅ OK |
| 2 | (truthy check on second result) | truthy | truthy | ✅ OK |
| 3 | `refund === null \|\| undefined` | null | null | ✅ OK |
| 4 | `amountOwed === null \|\| undefined` | null | null | ✅ OK |

**Why this test is in the line-32 bundle.** It exercises the refund/amount-owed determination that depends on the correct line-32 → line-33 chain.

---

## Test 25 (line 34) — `line34: refund calculated when overpaid`

```powershell
npx playwright test --workers=1 --output=test-results-line32-25 `
  -g "line34: refund calculated" line33-refund-owed.spec.ts
```

**Purpose.** Verifies line 34 = line 33 − line 24 when line 33 > line 24 (overpayment → refund).

**Inputs.** $100,000 wages, $50,000 withholding. Far more withholding than tax owed.

**IRS hand calculation.** Line 33 = $50,000 + 0 + 0 = $50,000. Line 24 ≈ $13,449. Overpaid = $50,000 − $13,449 = ~$36,551. Refund amount = overpaid (no line 36).

**Code path.** Refund determination after line 33. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `refund` is non-null | non-null | non-null | ✅ OK |
| 2 | `refund.overpaid > 0` | positive | positive | ✅ OK |
| 3 | `refund.refundAmount === refund.overpaid` | equal | equal | ✅ OK |
| 4 | `amountOwed === null` | null | null | ✅ OK |
| 5 | (truthy on `result`) | truthy | truthy | ✅ OK |

---

## Test 26 (line 37) — `line37: amount owed when underpaid`

```powershell
npx playwright test --workers=1 --output=test-results-line32-26 `
  -g "amount owed when underpaid" line33-refund-owed.spec.ts
```

**Purpose.** Verifies line 37 = line 24 − line 33 when line 24 > line 33 (underpayment → amount owed).

**Inputs.** Single, $100,000 wages, $1,000 withholding.

**IRS hand calculation.** Line 33 = $1,000 + 0 + 0 = $1,000. Line 24 ≈ $13,449. Amount owed = $13,449 − $1,000 = ~$12,449.

**Code path.** Amount-owed determination after line 33. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `amountOwed` is non-null | non-null | non-null | ✅ OK |
| 2 | `amountOwed.amountOwed > 0` | positive | positive | ✅ OK |
| 3 | `refund === null` | null | null | ✅ OK |
| 4 | (truthy on `result`) | truthy | truthy | ✅ OK |

---

## Test 27 (line 36 election) — `line36: apply-to-next-year reduces refund and populates amountAppliedToNextYear`

```powershell
npx playwright test --workers=1 --output=test-results-line32-27 `
  -g "apply-to-next-year reduces refund" line33-refund-owed.spec.ts
```

**Purpose.** Verifies the line 36 election: when the taxpayer elects to apply some of the overpayment to next year's estimated tax, the refund amount is reduced but the overpaid amount stays the same.

**Inputs.** $100,000 wages, $50,000 withholding; `36-apply-to-next-year`: electsApplyToNextYear=true, amountToApply=$500.

**IRS hand calculation.** Per Form 1040 lines 34/35a/36 instructions:
- Overpaid stays at ~$36,551 (line 34 is the gross overpayment).
- Line 36 (amount applied to 2026) = $500.
- Line 35a (refund) = overpaid − line 36 = ~$36,051.

**Code path.** `computeLine31ThroughLine38` line 36 logic at line 27282+. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `refund` is non-null | non-null | non-null | ✅ OK |
| 2 | `refund.overpaid ≈ original overpaid` | unchanged | unchanged | ✅ OK |
| 3 | `refund.amountAppliedToNextYear === 500` | $500 | $500 | ✅ OK |
| 4 | `refund.refundAmount ≈ overpaid - 500` | reduced by 500 | reduced by 500 | ✅ OK |
| 5 | `amountOwed === null` | null | null | ✅ OK |
| 6 | (truthy on `result`) | truthy | truthy | ✅ OK |

---

## Test 28 (line 36 cap) — `line36: apply-to-next-year capped when amount exceeds overpayment`

```powershell
npx playwright test --workers=1 --output=test-results-line32-28 `
  -g "apply-to-next-year capped" line33-refund-owed.spec.ts
```

**Purpose.** Verifies the line 36 cap: electing more than the overpayment is capped at the overpayment, and the refund amount becomes null (entire overpayment consumed by the election).

**Inputs.** $100,000 wages, $50,000 withholding; elect $999,999 to next year.

**IRS hand calculation.** Per Form 1040 instructions, line 36 cannot exceed line 34 (overpayment).
- Overpaid ≈ $36,551 (unchanged).
- Line 36 capped at $36,551.
- Refund = $0 → null per project convention.

**Code path.** Line 36 cap clamp in `computeLine31ThroughLine38`. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `refund` is non-null | non-null | non-null | ✅ OK |
| 2 | `refund.overpaid ≈ original overpaid` | unchanged | unchanged | ✅ OK |
| 3 | `refund.amountAppliedToNextYear ≈ overpaid` (capped) | capped | capped | ✅ OK |
| 4 | `applied <= overpaid + 1` (rounding tolerance) | bounded | bounded | ✅ OK |
| 5 | `refund.refundAmount === null \|\| 0` | null/0 | null/0 | ✅ OK |
| 6 | (truthy on `result`) | truthy | truthy | ✅ OK |

---

## Test 29 (Form 8888 + direct deposit interaction) — `line35bcd: direct deposit suppressed when Form 8888 split-refund elected`

```powershell
npx playwright test --workers=1 --output=test-results-line32-29 `
  -g "direct deposit suppressed when Form 8888" line33-refund-owed.spec.ts
```

**Purpose.** Verifies that Form 8888 (Allocation of Refund) overrides the simple lines 35b/c/d direct-deposit fields. When the taxpayer elects to split their refund, lines 35b/c/d must be blank (the split is on Form 8888 instead).

**Inputs.** $100,000 wages, $50,000 withholding; both `35-direct-deposit` AND `refund-allocation-taxpayer` saved.

**IRS hand calculation.** Per Form 1040 line 35 instructions and Form 8888: lines 35b/c/d are for a SINGLE account direct-deposit. To split into multiple accounts the taxpayer files Form 8888 instead, and lines 35b/c/d MUST be blank.

**Code path.** Refund building code in `computeLine31ThroughLine38` checks for `refund-allocation-taxpayer.wantsRefundAllocation` and, when true, suppresses lines 35b/c/d on the refund object and emits Form 8888 instead. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `refund` is non-null | non-null | non-null | ✅ OK |
| 2 | `refund.overpaid > 0` | positive | positive | ✅ OK |
| 3 | `refund.directDeposit` is null/false | null/false | null/false | ✅ OK |
| 4 | `refund.routingNumber` is null | null | null | ✅ OK |
| 5 | `refund.accountType` is null | null | null | ✅ OK |
| 6 | `refund.accountNumber` is null | null | null | ✅ OK |
| 7 | `f8888` is non-null | non-null | non-null | ✅ OK |
| 8 | `f8888.account1Amount === half` | half | half | ✅ OK |
| 9 | `f8888.account2Amount === rest` | rest | rest | ✅ OK |

---

## Test 30 (line 35b/c/d simple) — `line35bcd: direct deposit fields populated when elected`

```powershell
npx playwright test --workers=1 --output=test-results-line32-30 `
  -g "direct deposit fields populated when elected" line33-refund-owed.spec.ts
```

**Purpose.** Verifies that when the user saves the simple `35-direct-deposit` form (without Form 8888), the refund object's lines 35b/c/d fields are populated with the user-supplied routing number, account type, and account number.

**Inputs.** $100,000 wages, $50,000 withholding; `35-direct-deposit` saved with routing $021000089$, checking, account $123456789012$.

**IRS hand calculation.** Per Form 1040 line 35 instructions and IRS routing-number rules: lines 35b/c/d carry exactly the values the user entered for the single-account direct-deposit case.

**Code path.** Refund building code reads `35-direct-deposit` form and copies the three fields onto the refund object. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `refund` is non-null | non-null | non-null | ✅ OK |
| 2 | `refund.directDeposit === true` | true | true | ✅ OK |
| 3 | `refund.routingNumber === '021000089'` | exact match | exact match | ✅ OK |
| 4 | `refund.accountType === 'checking'` | exact match | exact match | ✅ OK |
| 5 | `refund.accountNumber === '123456789012'` | exact match | exact match | ✅ OK |

---

---

## Test 31 (DIRECT line 32, ACTC alone) — `line32: Additional Child Tax Credit (line 28) alone contributes to line 32 total`

```powershell
npx playwright test --workers=1 --output=test-results-line32-31 `
  -g "ACTC.*alone contributes" line32-total-other-payments.spec.ts
```

**Purpose.** Closes suite-level gap #2. Verifies that the Additional Child Tax Credit alone, with no other line-32 contributors, propagates correctly from Schedule 8812 Part II-A → Form 1040 line 28 → line 32 subtotal.

**Inputs.**
- Single filer (DOB 1990-01-01 → age 35).
- 1 CTC qualifying child (relationship "Child", DOB 2015-06-15 → age 10, qualifiesChildTaxCredit=true).
- W-2: $20,000 wages, $0 withholding (recipientTIN 111-11-1111).
- No EIC claim (no `earned-income-credit-taxpayer` form saved).
- No education credits, no adoption, no Schedule 3 Part II credits.

**IRS hand calculation.** Per Schedule 8812 Part I + Part II-A and IRC §24(d):
- AGI = $20,000.
- Standard deduction (Single 2025) = $15,750 (Rev. Proc. 2024-40).
- Taxable income = $4,250; tax (Tax Table) ≈ $425.
- Child Tax Credit line 12 (after phase-out) = $2,200 per child (One Big Beautiful Bill Act of 2025).
- Nonrefundable CTC (Schedule 8812 line 14 / Form 1040 line 19) = min($2,200, $425) = $425.
- Remaining CTC for the refundable Additional Child Tax Credit = $2,200 − $425 = $1,775.
- Earned-income floor = max(0, $20,000 − $2,500) × 15% = $2,625 (IRC §24(d)(1)(B)).
- Per-child refundable cap = $1,700 × 1 child = $1,700.
- **ACTC (Schedule 8812 line 27 → Form 1040 line 28)** = min($1,775, $2,625, $1,700) = **$1,700**.
- **Line 32** = 0 + $1,700 + 0 + 0 + 0 = **$1,700**.

**Code path.** `computeSchedule8812` Part II-A at `TaxReturnComputeService.java`, then `computeLine31ThroughLine38` at line 27262 sums line 32. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | (truthy on `result`) | truthy | truthy | ✅ OK |
| 2 | `payments` is non-null | non-null | non-null | ✅ OK |
| 3 | `payments.additionalChildTaxCredit === 1700` | $1,700 | $1,700 | ✅ OK |
| 4 | `payments.totalOtherPaymentsAndRefundableCredits === 1700` | $1,700 | $1,700 | ✅ OK |

---

## Test 32 (DIRECT line 32, AOTC alone) — `line32: Refundable American Opportunity Credit (line 29) alone contributes to line 32 total`

```powershell
npx playwright test --workers=1 --output=test-results-line32-32 `
  -g "Refundable American Opportunity Credit.*alone contributes" line32-total-other-payments.spec.ts
```

**Purpose.** Closes suite-level gap #3. Verifies that the refundable portion of the American Opportunity Credit alone, with no other line-32 contributors, propagates from Form 8863 line 8 → Form 1040 line 29 → line 32 subtotal.

**Inputs.**
- Single filer (DOB 1990-01-01 → age 35; over 24 → no kiddie-tax refundable-AOTC bar per IRC §25A(g)(7) safe harbor).
- W-2: $40,000 wages, $0 withholding.
- Education credits form: filer is the student, $4,000 adjusted qualified education expenses, AOTC requested, half-time enrolled, not in first-four-years exhaustion, no felony drug conviction.
- No CTC qualifying child (so no ACTC), no EIC claim, no adoption, no Schedule 3 Part II.

**IRS hand calculation.** Per Form 8863 lines 1–8 and IRC §25A:
- 100%-tier expense = min($4,000, $2,000) = $2,000.
- 25%-tier expense = min(max(0, $4,000 − $2,000), $2,000) = $2,000.
- Per-student credit before phase-out = $2,000 + 25% × $2,000 = $2,500 (capped at $2,500).
- Modified AGI ≈ $40,000 < $80,000 phase-out start → full credit.
- Refundable portion (IRC §25A(i)(5)) = 40% × $2,500 = $1,000 → Form 1040 line 29.
- **Line 32** = 0 + 0 + $1,000 + 0 + 0 = **$1,000**.

**Code path.** `computeForm8863` AOTC branch, refundable share pre-set on `Payments.americanOpportunityCredit`, then `computeLine31ThroughLine38` sums. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | (truthy on `result`) | truthy | truthy | ✅ OK |
| 2 | `payments` is non-null | non-null | non-null | ✅ OK |
| 3 | `payments.americanOpportunityCredit === 1000` | $1,000 | $1,000 | ✅ OK |
| 4 | `payments.totalOtherPaymentsAndRefundableCredits === 1000` | $1,000 | $1,000 | ✅ OK |

---

## Test 33 (DIRECT line 32, ALL 5 sources) — `line32: all five sources (EIC + ACTC + AOTC + Adoption + Schedule 3) sum correctly`

```powershell
npx playwright test --workers=1 --output=test-results-line32-33 `
  -g "all five sources" line32-total-other-payments.spec.ts
```

**Purpose.** Closes suite-level gap #1. Mirrors the Java unit test `line32_allFiveSourcesContributeSimultaneously` (added alongside `XLS/Computations/32.md` 2026-06-12 to close Gap D from that document) end-to-end through REST + persistence + JSON serialization, per `feedback_java_unit_passing_doesnt_mean_e2e_passing`.

**Inputs.**
- Single filer (DOB 1990-01-01 → age 35).
- 1 CTC qualifying child (relationship "Child", DOB 2015-06-15 → age 10).
- W-2: $20,000 wages, $0 withholding.
- Earned Income Credit form with `claimsEIC=true` and `eicQualifyingChildren=[{ childSSN: '222-22-2221', relationship: 'son', monthsLivedWithTaxpayer: 12, childYearOfBirth: 2015 }]`. Relationship "son" is in the EIC_QUALIFYING_RELATIONSHIPS set per IRC §32(c)(3)(B); "child" is NOT and would silently drop the credit.
- Education credits form with $4,000 AOTC expenses (filer is the student).
- Adoption form: 1 adopted child, $15,000 qualified expenses, MAGI override $20,000 (well below $259,190 phase-out start).
- `31-other-payments`: `hasSection1341Credit=true`, `section1341RepaymentAmount=4000` (> $3,000 IRC §1341(a)(3) threshold), `section1341Credit=400`.

**IRS hand calculation.** Per the individual IRS rules already cited in Tests 22, 31, 32 plus IRC §32 (EIC), IRC §23 (Adoption Credit), IRC §1341 (Schedule 3 line 13b):
- **Line 27a (EIC)**: Single, 1 qualifying child, earned income $20,000. Phase-in end = $12,730; phase-out start = $23,350; $20,000 is in the plateau. Maximum 1-child credit = **$4,328** (Rev. Proc. 2024-40).
- **Line 28 (ACTC)**: Same hand calculation as Test 31 → **$1,700**.
- **Line 29 (refundable AOTC)**: Same hand calculation as Test 32 → **$1,000**.
- **Line 30 (Refundable Adoption Credit)**: 1 child, $15,000 expenses, MAGI below phase-out. Line 6 = min($17,280, $15,000) = $15,000; line 11a = $15,000 × (1 − 0) = $15,000; line 11b = min($15,000, $5,000 refundable cap) = **$5,000**.
- **Line 31 (Schedule 3 line 15)**: §1341 credit only → Schedule 3 line 13b = $400 → line 14 = $400 → line 15 = $400 → **$400**.
- **Line 32 = $4,328 + $1,700 + $1,000 + $5,000 + $400 = $12,428**.
- **Line 33 = $0 (line 25d) + $0 (line 26) + $12,428 (line 32) = $12,428**.

**Code path.** Five upstream computers (`computeLine27aEIC`, `computeSchedule8812` Part II-A, `computeForm8863`, `computeAdoptionBenefits` + `applyAdoptionCredit`, `applyOtherPaymentsFormToSchedule3` + `finalizeSchedule3Totals`) populate the five `Payments.*` fields. `computeLine31ThroughLine38` at line 27262 sums them. The non-negativity guardrail added 2026-06-12 (`XLS/Computations/32.md` Gap G closure) fires only when an addend is negative, which does not occur here. Code matches IRS rule.

**Per-assertion verdict.**

| # | Assertion | IRS expected | Code produces | Verdict |
|---|---|---|---|---|
| 1 | `payments` is non-null | non-null | non-null | ✅ OK |
| 2 | `payments.earnedIncomeCredit === 4328` | $4,328 | $4,328 | ✅ OK |
| 3 | `payments.additionalChildTaxCredit === 1700` | $1,700 | $1,700 | ✅ OK |
| 4 | `payments.americanOpportunityCredit === 1000` | $1,000 | $1,000 | ✅ OK |
| 5 | `payments.refundableAdoptionCredit === 5000` | $5,000 | $5,000 | ✅ OK |
| 6 | `payments.otherPaymentsSchedule3 === 400` | $400 | $400 | ✅ OK |
| 7 | `payments.totalOtherPaymentsAndRefundableCredits === 12428` | $12,428 | $12,428 | ✅ OK |
| 8 | `payments.totalPayments === 12428` | $12,428 | $12,428 | ✅ OK |

---

## Suite-level coverage gaps

The 33 tests above (30 original + 3 added 2026-06-12 to close the 5-source matrix gaps) collectively cover the most common line-32 scenarios. Of the 6 suite-level gaps originally cataloged, 3 are now closed and 3 remain — the latter three are policy-deferred (architectural cost out of proportion to the catch-rate they would provide).

| # | Path | Status | Coverage gap detail |
|---|---|---|---|
| 1 | All 5 sources contributing simultaneously (EIC + ACTC + AOTC refundable + Adoption + Schedule 3) | ✅ Closed 2026-06-12 | New Test 33 in `line32-total-other-payments.spec.ts` mirrors the Java unit test `line32_allFiveSourcesContributeSimultaneously` end-to-end through REST + persistence + JSON serialization. Hand-pinned to $12,428 (EIC $4,328 + ACTC $1,700 + AOTC $1,000 + Adoption $5,000 + §1341 $400). |
| 2 | ACTC alone contributing to line 32 | ✅ Closed 2026-06-12 | New Test 31 — Single + 1 CTC qualifying child + $20,000 wages → ACTC $1,700 → line 32 $1,700. |
| 3 | Refundable AOTC alone contributing to line 32 | ✅ Closed 2026-06-12 | New Test 32 — Single age 35 + $40,000 wages + $4,000 AOTC expenses → refundable AOTC $1,000 → line 32 $1,000. |
| 4 | Schedule 3 line 9 (Net Premium Tax Credit) contributing to line 32 via line 31 | ⏸ Policy-deferred | Requires a full Form 8962 + Form 1095-A fixture (taxpayer Modified AGI, household size, monthly premium / SLCSP / APTC for 12 months, family-size factor, applicable federal poverty line). Schedule 3 line 13b §1341 in Test 20 is the existing Schedule 3 → line 32 e2e coverage. Adding the PTC fixture is a follow-up scoped to the line 31 / Schedule 3 audit, not the line-32 audit. |
| 5 | Line 32 stored PDF field key for the renamed 2025 semantic names | ⏸ Policy-deferred | The recent `form-tax-return-1040.component.ts` fix renamed the line 32 / 31 / 30 / 29 PDF field keys to the 2025 semantic names. Verifying this in e2e requires saving the rendered PDF, parsing it with pdf-lib, and inspecting the AcroForm field map. That is an architectural addition (no other tax-return-PDF render spec exists in the catalog except `schedule1-pdf-render.spec.ts`). Manual save-as-PDF verification was performed and confirmed all four lines render. |
| 6 | Non-negativity guardrail (`Gap G` from 32.md) | ⏸ Policy-blocked | The backend `LOG.warnf` defensive check only fires when an upstream sign error occurs, which the current fixtures cannot produce without breaking unrelated code. Verifiable only by deliberately injecting a negative `Payments.*` value in a unit test that bypasses the upstream computers, which contradicts the project's "test through the public surface" convention. Manual code review only. |

The three remaining gaps (#4, #5, #6) are policy-deferred for the reasons given above. Closing them does not change the **IRS-canonical correctness** of any existing test in the suite; they are coverage holes around the PDF-export wiring (#5), the Net Premium Tax Credit route (#4), and a defensive-only log statement (#6).
